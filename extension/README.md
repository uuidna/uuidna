# uuidna — Claude Desktop Extension

The sealed ledger, one click into Claude Desktop: 1215 Lean-proven theorems as tools — try any claim
(VERIFIED/UNVERIFIED), address anything, decode the aura, walk the graduation wave. Every call receipted.

The extension is ONE manifest (standards-compression: what the .mcpb format already says, this package never
repeats) — the server is the published `npx -y @uuidna/uuidna`, zero runtime dependencies.

Pack: `npx @anthropic-ai/mcpb pack extension/` → `uuidna.mcpb` → double-click into Claude Desktop.
Free for the public interest (CC BY-NC-ND 4.0); commercial use is the measured two-coin contribution — uuidna.com/captain.
