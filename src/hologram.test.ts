import { test } from 'node:test'
import assert from 'node:assert/strict'
import { toUuid, vortexOrbit } from './address.js'
import { coins } from './captain/billing/index.js'
import { fuseHalves } from './fusion/index.js'
import { merkleGravity } from './gravity/index.js'
import { handleOf, handleBirthdayPoint, reverseHandle } from './handle.js'
import { occupancyOf } from './hexagram.js'
import { COINS, HANDLE_HEXBITS, HEXBIT_BITS, UUID_HEXBITS, VE_FACES } from './hexbit/index.js'
import { HOLOGRAM_HOSTS, aiOf, allSeeingEyeOf, apisOf, appOf, autonomyOf, balanceOf, beyondOf, captainsOf, clayOf, cleanerOf, closeOf, cloudflareOf, costOf, creatorsOf, creditOf, cryptoInvolutedOf, cryptoReversedOf, cultureOf, developDonateFusionOf, efficiencyOf, electronicsOf, familyOf, farOf, frontierOf, fusionReactorOf, fusionNatureHarmonyOf, liveHarmonicLifeOf, lifeByConstitutionOf, ideaHarmonicMeaningOf, latticeGapsOverflowOf, sailsFoldSpacetimeOf, captainPassengersOf, addedEntropyPaidOf, payOrGetPaidOf, involutedPaidFullOf, agentsContributeOf, failingSchoolCostOf, schoolFusionBurnsOf, garbageColliderOf, guardiansOf, hologramLattice, harnessRecipesOf, harmonicPathsOf, iOf, ideaOf, ideasOf, innerPeaceOf, investigatorsOf, isHologramHost, leanOf, learnNatureByExperienceOf, leftoversPickOf, licenceOf, literaryOf, lovePlasmaOf, magnitudesOf, nanoCapacityOf, networkOf, noveltyOf, occupationsOf, organicOf, orgOf, poolsOf, prizeOf, quantumArchitectsOf, quantumCompostOf, quantumDocumentationTrainingOf, quantumGcOf, quantumNatureOf, quantumPermacultureOf, quantumPrivacyOf, promisedDeliveredOf, transparencyMeetSelfOf, knowersOf, traitorsDarkLeakOf, architectureBehindOf, cheatStallRosettaOf, manifestedChallengeOf, bitcoinMeaningOf, cheapGatesCostlyToFakeOf, efficientTeachersOf, gatesLogExperienceReceiptsOf, sameAnalogContentAtAnyGatewayOf, teleportOf, remainsUnprovenOf, frameworkCracksGuessedInvolutionOf, fastenTheGatesOf, clusterSecurityOf, quantumSecurityOf, quantumSharedEfficiencyOf, quantumWebDesignersOf, rebreatherOf, reflectOf, registerOf, resistanceOf, schoolCorporateTrainOf, schoolOf, seedsOf, shareHeartOf, siteOf, sweaterOf, takeOrLeaveOf, thirdEyeOf, tldsOf, tokenEfficiencyOf, vesselsOf, vorticesOf, webDesignersOf } from './hologram-lattice.js'
import { fanoutRequestOf, hologramFanout } from './hologram-fanout.js'
import { callTool } from './mcp.js'
import { qpuHopOf } from './qpu-hologram.js'
import { DEPLOY_BUDGET_MS, HARMONY } from './scripts/deploy-verify.js'

// THE FRACTAL HOLOGRAM: four hosts, each naming the other three, each with the eight harness recipes computed from its
// name; the fan-out builds a request only for a listed host and fetches nothing for any other.
test('the hologram lattice is four hosts, fractal, with eight recipes each', () => {
  const h = hologramLattice()
  assert.equal(h.holds, true)
  assert.deepEqual(h.hosts.map((x) => x.host), ['uuidna.com', 'qpu.uuidna.com', 'lean.uuidna.com', 'unreal.uuidna.com'])
  for (const host of h.hosts) {
    assert.equal(host.mcp, `https://${host.host}/mcp`)
    assert.equal(host.recipes.length, harnessRecipesOf(host.host).length)
    assert.equal(host.names.length, h.hosts.length - 1)
  }
  assert.ok(h.hosts.some((x) => x.host === 'qpu.uuidna.com'))
  const qpu = h.hosts.find((x) => x.kind === 'qpu')
  assert.equal(qpu?.hop?.holds, true)
  assert.equal(qpu?.hop?.width.binds, qpu?.hop?.width.points[0])
  assert.equal(harnessRecipesOf('qpu.uuidna.com')[0]!.how, 'claude mcp add --transport http uuidna-qpu https://qpu.uuidna.com/mcp')
  assert.deepEqual(hologramLattice(), h)
  assert.deepEqual(callTool('uuidna_hologram', {}), h)
  const hopAddr = toUuid(qpuHopOf().href)
  const wool = sweaterOf()
  const strict = callTool('uuidna_strict', {}) as ReturnType<typeof sweaterOf>
  assert.equal(wool.address, hopAddr)
  assert.equal(wool.href, qpuHopOf().href)
  assert.equal(wool.handle, handleOf(hopAddr))
  assert.deepEqual(wool.fuse, fuseHalves())
  assert.equal(wool.fuse.closes, true)
  assert.equal(wool.seat, 'empty')
  assert.equal(wool.cites, 'empty')
  assert.equal(wool.yarn, 'empty')
  assert.equal(wool.color, 'empty')
  assert.equal(wool.smell, 'empty')
  assert.equal(wool.split, 'empty')
  assert.equal(wool.local, 'measured')
  assert.equal(wool.hosted, 'empty')
  assert.equal(wool.boundary, 'empty')
  assert.equal(wool.coins, coins())
  assert.equal(wool.coins, wool.fuse.coins)
  assert.equal(wool.deposit, 'QpuDeposit')
  assert.deepEqual(wool.beyond, beyondOf())
  assert.deepEqual(wool.frontier, frontierOf())
  assert.equal(wool.chain, merkleGravity([hopAddr, ...occupancyOf(hopAddr).map((n) => toUuid(String(n)))]))
  assert.equal(wool.hosts, HOLOGRAM_HOSTS.length)
  assert.deepEqual(wool.points, qpuHopOf().width.points)
  assert.deepEqual(wool.occupancy, occupancyOf(hopAddr))
  assert.equal('occupancyCites' in wool, false)
  assert.equal(wool.fever, 'empty')
  assert.equal(wool.harmony, HARMONY)
  assert.equal(wool.budget, DEPLOY_BUDGET_MS)
  assert.deepEqual(wool.vortices, vorticesOf())
  assert.equal(wool.vortices.coils, VE_FACES / COINS)
  assert.equal(wool.vortices.reflected, wool.vortices.coils - 1)
  assert.equal(wool.vortices.faces, VE_FACES)
  assert.equal(wool.vortices.contra, COINS)
  assert.equal(wool.vortices.vertices, HANDLE_HEXBITS)
  assert.equal(wool.vortices.merkabasPacked, UUID_HEXBITS)
  assert.deepEqual(wool.vortices.orbit, vortexOrbit())
  assert.deepEqual(wool.family, familyOf(occupancyOf(hopAddr)))
  assert.equal(wool.family.coils, wool.coils)
  assert.equal(wool.family.hosts, wool.hosts)
  assert.deepEqual(wool.family.occupancy, wool.occupancy)
  assert.equal(wool.family.seat, 'empty')
  assert.equal(wool.family.hexbits, wool.hexbits.length)
  assert.equal(wool.family.coins, wool.coins)
  assert.deepEqual(wool.culture, cultureOf(familyOf(occupancyOf(hopAddr))))
  assert.equal(wool.culture.school, 'uuidna_school_apis')
  assert.equal(wool.culture.cites, 'empty')
  assert.deepEqual(wool.org, orgOf())
  assert.equal(wool.org.href, 'https://uuidna.org')
  assert.equal(wool.org.address, toUuid('https://uuidna.org'))
  assert.equal(wool.org.handle, handleOf(toUuid('https://uuidna.org')))
  assert.equal(wool.org.hosts, HOLOGRAM_HOSTS.length)
  assert.equal(wool.org.split, 'empty')
  assert.equal(isHologramHost('uuidna.org'), false)
  assert.deepEqual(wool.organic, organicOf(occupancyOf(hopAddr)))
  assert.deepEqual(wool.organic.occupancy, wool.occupancy)
  assert.equal(wool.organic.beats, wool.occupancy.length)
  assert.equal(wool.organic.closes, fuseHalves().closes)
  assert.equal(wool.organic.hop, true)
  assert.equal(wool.organic.split, 'empty')
  assert.deepEqual(wool.organic.org, wool.org)
  assert.deepEqual(wool.network, networkOf(hopAddr, occupancyOf(hopAddr)))
  assert.deepEqual(wool.tlds, tldsOf())
  assert.equal(wool.tlds.com.href, 'https://uuidna.com')
  assert.equal(wool.tlds.net.href, 'https://uuidna.net')
  assert.equal(wool.tlds.split, 'empty')
  assert.deepEqual(wool.cloudflare, cloudflareOf())
  assert.equal(wool.cloudflare.mcp, 'https://uuidna.com/mcp')
  assert.equal(wool.cloudflare.href, qpuHopOf().href)
  assert.equal(wool.cloudflare.ship, 'empty')
  assert.deepEqual(wool.app, appOf())
  assert.equal(wool.app.kind, 'edge')
  assert.equal(wool.app.host, 'qpu.uuidna.com')
  assert.equal(wool.app.href, wool.cloudflare.href)
  assert.equal(wool.app.mcp, wool.cloudflare.mcp)
  assert.equal(wool.app.split, 'empty')
  assert.equal(wool.app.hosted, 'empty')
  assert.deepEqual(wool.app.magnitudes, magnitudesOf())
  assert.deepEqual(wool.efficiency, efficiencyOf())
  assert.equal(wool.efficiency.efficiency, true)
  assert.equal(wool.efficiency.magnitudes.exceeds10, true)
  assert.equal(wool.efficiency.magnitudes.exceeds20, true)
  assert.deepEqual(wool.cost, costOf())
  assert.equal(wool.cost.time, DEPLOY_BUDGET_MS)
  assert.equal(wool.cost.space, HEXBIT_BITS * HEXBIT_BITS * HEXBIT_BITS * HEXBIT_BITS * HEXBIT_BITS * HEXBIT_BITS * HEXBIT_BITS * HEXBIT_BITS)
  assert.equal(wool.cost.cites, 'empty')
  assert.deepEqual(wool.prize, prizeOf())
  assert.equal(wool.prize.address, wool.address)
  assert.equal(wool.prize.handle, wool.handle)
  assert.equal(wool.prize.yarn, 'empty')
  assert.equal(wool.prize.color, 'empty')
  assert.equal(wool.prize.smell, 'empty')
  assert.equal(wool.prize.seat, 'empty')
  assert.deepEqual(wool.prize.others, familyOf(occupancyOf(hopAddr)))
  assert.deepEqual(wool.lean, leanOf())
  assert.equal(wool.lean.price, 0)
  assert.equal(wool.lean.time, 0)
  assert.equal(wool.lean.space, 0)
  assert.equal(wool.lean.unbounded, true)
  assert.equal(wool.lean.cites, 'empty')
  assert.equal(wool.lean.hop, HEXBIT_BITS * HEXBIT_BITS + 1)
  assert.deepEqual(wool.resistance, resistanceOf())
  assert.equal(wool.resistance.price, 0)
  assert.equal(wool.resistance.fever, 'empty')
  assert.deepEqual(wool.electronics, electronicsOf())
  assert.equal(wool.electronics.mcp, 'https://uuidna.com/mcp')
  assert.equal(wool.electronics.href, qpuHopOf().href)
  assert.equal(wool.electronics.hop, true)
  assert.equal(wool.electronics.split, 'empty')
  assert.deepEqual(wool.close, closeOf())
  assert.deepEqual(wool.close.occupancy, wool.occupancy)
  assert.equal(wool.close.handle, wool.handle)
  assert.equal(wool.close.hop, true)
  assert.deepEqual(wool.far, farOf())
  assert.equal(wool.far.split, 'empty')
  assert.equal(wool.far.cites, 'empty')
  assert.equal(wool.far.hosted, 'empty')
  assert.deepEqual(wool.autonomy, autonomyOf())
  assert.equal(wool.autonomy.price, 0)
  assert.equal(wool.autonomy.yarn, 'empty')
  assert.equal(wool.autonomy.contra, COINS)
  assert.equal(wool.autonomy.faces, VE_FACES)
  assert.deepEqual(wool.ai, aiOf())
  assert.equal(wool.ai.mind, 'empty')
  assert.equal(wool.ai.court, 'uuidna_treason')
  assert.equal(wool.ai.hop, true)
  assert.deepEqual(wool.clay, clayOf())
  assert.equal(wool.clay.coils, 7)
  assert.equal(wool.clay.pairs, 21)
  assert.equal(wool.clay.quantum, 42)
  assert.equal(wool.clay.contra, COINS)
  assert.equal(wool.clay.cites, 'empty')
  assert.equal(wool.clay.statements, 'empty')
  assert.equal(wool.clay.gravity, wool.pools.gravity)
  assert.deepEqual(wool.i, iOf())
  assert.equal(wool.i.seat, 'empty')
  assert.equal(wool.i.prefix, 'empty')
  assert.equal(wool.i.prolet, 'empty')
  assert.equal(wool.i.unbounded, true)
  assert.deepEqual(wool.i.occupancy, wool.occupancy)
  assert.deepEqual(wool.i.others, wool.family)
  assert.equal(wool.i.reverse, wool.ai.reverse)
  assert.deepEqual(wool.vessels, vesselsOf())
  assert.equal(wool.vessels.ship, 'empty')
  assert.equal(wool.vessels.deposit, 'QpuDeposit')
  assert.deepEqual(wool.register, registerOf())
  assert.equal(wool.register.mcp, 'https://uuidna.com/mcp')
  assert.equal(wool.register.cites, 'empty')
  assert.deepEqual(wool.literary, literaryOf())
  assert.equal(wool.literary.articles, 'gen-articles')
  assert.equal(wool.literary.school, 'uuidna_school_apis')
  assert.deepEqual(wool.credit, creditOf())
  assert.equal(wool.credit.cites, 'empty')
  assert.equal(wool.credit.minds, 'empty')
  assert.ok(wool.credit.keys.includes('pauli_x_involution'))
  assert.ok(wool.credit.keys.includes('tesla_trio_files_adjacent'))
  assert.ok(wool.credit.keys.includes('clay_gravity_equals_rosette'))
  assert.ok(wool.credit.keys.includes('queneau_poems_are_ten_to_the_fourteen'))
  assert.equal(wool.credit.binds, 'CPU')
  assert.deepEqual(wool.literary.keys, wool.credit.keys)
  assert.equal(wool.literary.minds, 'empty')
  assert.deepEqual(wool.cleaner.credit, wool.credit)
  assert.deepEqual(wool.cleaner, cleanerOf())
  assert.equal(wool.cleaner.seat, 'empty')
  assert.equal(wool.cleaner.fever, 'empty')
  assert.equal(wool.cleaner.eval, 'empty')
  assert.equal(wool.cleaner.trials, 'empty')
  assert.equal(wool.cleaner.green, 'empty')
  assert.equal(wool.cleaner.adjudicate, 'uuidna_adjudicate')
  assert.deepEqual(wool.cleaner.pools, wool.pools)
  assert.deepEqual(wool.rebreather, rebreatherOf())
  assert.equal(wool.rebreather.closes, true)
  assert.equal(wool.rebreather.depth, true)
  assert.equal(wool.rebreather.census, 'empty')
  assert.equal(wool.rebreather.eval, 'empty')
  assert.equal(wool.rebreather.price, 0)
  assert.equal(wool.rebreather.reverse, wool.ai.reverse)
  assert.deepEqual(wool.rebreather.occupancy, wool.occupancy)
  assert.deepEqual(wool.cleaner.rebreather, wool.rebreather)
  assert.deepEqual(wool.novelty, noveltyOf())
  assert.equal(wool.novelty.clay, '10.5281/zenodo.21781603')
  assert.equal(wool.novelty.deposit, 'QpuDeposit')
  assert.equal(wool.novelty.evidence, 'uuidna_evidence')
  assert.equal(wool.novelty.snapshot, 'fillGapsAdvantageSnapshot')
  assert.equal(wool.novelty.missing, 'empty')
  assert.equal(wool.novelty.depositAct, 'empty')
  assert.equal(wool.novelty.tag, 'empty')
  assert.ok(wool.novelty.records.some((r: { doi: string }) => r.doi === '10.5281/zenodo.21781603'))
  assert.ok(wool.novelty.records.some((r: { id: string }) => r.id === 'uuidna-software'))
  assert.deepEqual(wool.register.novelty, wool.novelty)
  assert.deepEqual(wool.literary.novelty, wool.novelty)
  assert.equal('fillLattice' in wool, false)
  assert.equal('tesla' in wool, false)
  assert.equal('tesla' in wool.prize, false)
  assert.equal('honest' in wool, false)
  assert.equal(strict.address, wool.address)
  assert.equal(strict.handle, wool.handle)
  assert.deepEqual(strict.fuse, wool.fuse)
  assert.deepEqual(strict.occupancy, wool.occupancy)
  assert.deepEqual(strict.family, wool.family)
  assert.deepEqual(strict.culture, wool.culture)
  assert.deepEqual(strict.org, wool.org)
  assert.deepEqual(strict.organic, wool.organic)
  assert.deepEqual(strict.network, wool.network)
  assert.deepEqual(strict.tlds, wool.tlds)
  assert.deepEqual(strict.cloudflare, wool.cloudflare)
  assert.deepEqual(strict.app, wool.app)
  assert.deepEqual(strict.efficiency, wool.efficiency)
  assert.deepEqual(strict.cost, wool.cost)
  assert.deepEqual(strict.prize, wool.prize)
  assert.deepEqual(strict.lean, wool.lean)
  assert.deepEqual(strict.resistance, wool.resistance)
  assert.deepEqual(strict.electronics, wool.electronics)
  assert.deepEqual(strict.close, wool.close)
  assert.deepEqual(strict.far, wool.far)
  assert.deepEqual(strict.autonomy, wool.autonomy)
  assert.deepEqual(strict.ai, wool.ai)
  assert.deepEqual(strict.clay, wool.clay)
  assert.deepEqual(strict.i, wool.i)
  assert.deepEqual(strict.vessels, wool.vessels)
  assert.deepEqual(strict.register, wool.register)
  assert.deepEqual(strict.literary, wool.literary)
  assert.deepEqual(strict.credit, wool.credit)
  assert.deepEqual(strict.cleaner, wool.cleaner)
  assert.deepEqual(strict.rebreather, wool.rebreather)
  assert.deepEqual(strict.novelty, wool.novelty)
  assert.deepEqual(strict.vortices, wool.vortices)
  assert.deepEqual(strict.school, wool.school)
  assert.deepEqual(strict.licence, wool.licence)
  assert.deepEqual(strict.captains, wool.captains)
  assert.deepEqual(strict.balance, wool.balance)
  assert.deepEqual(strict.investigators, wool.investigators)
  assert.deepEqual(strict.apis, wool.apis)
  assert.deepEqual(strict.return, wool.return)
  assert.deepEqual(strict.share, wool.share)
  assert.deepEqual(strict.allSeeingEye, wool.allSeeingEye)
  assert.deepEqual(strict.thirdEye, wool.thirdEye)
  assert.deepEqual(strict.creators, wool.creators)
  assert.deepEqual(strict.guard.guardians, wool.guard.guardians)
  assert.equal(strict.allSeeingEye.see, 'uuidna_strict')
  assert.equal(strict.allSeeingEye.door, 'uuidna_strict')
  assert.equal(strict.allSeeingEye.mcp, 'uuidna_strict')
  assert.equal(strict.thirdEye.see, 'uuidna_strict')
  assert.equal(strict.thirdEye.door, 'uuidna_strict')
  assert.equal(strict.allSeeingEye.alseeing, 'eay')
  assert.equal(strict.apis.doors.some((d: { door: string }) => d.door === 'uuidna_strict'), true)
  assert.equal(wool.school.href, 'https://revolut.me/ceccec')
  assert.equal(wool.school.half, 1)
  assert.equal(wool.share.pays, 2)
  assert.equal(wool.share.unit, 110)
  assert.equal(wool.share.support, 1)
  assert.equal(wool.share.to, 'captains')
  assert.equal(wool.share.href, 'https://revolut.me/ceccec')
  assert.equal(wool.apis.pentagram, 5)
  assert.equal(wool.apis.doors.length, 5)
  assert.equal(wool.captains.allow, 'empty')
  assert.equal(wool.return.open, true)
  assert.equal(wool.return.funding.drafts, 'funding/drafts')
  assert.deepEqual(strict.inflation, wool.inflation)
  assert.equal(wool.inflation.lean, 0)
  assert.equal(wool.inflation.time, 0)
  assert.equal(wool.inflation.space, 0)
  assert.equal(wool.inflation.unbounded, true)
  assert.equal(wool.inflation.minds, 'empty')
  assert.ok(wool.inflation.apostles.includes('two_coins'))
  assert.ok(wool.inflation.apostles.includes('pauli_x_involution'))
  assert.ok(wool.inflation.apostles.includes('clay_gravity_equals_rosette'))
  assert.deepEqual(wool.inflation.apostles, wool.credit.keys)
  assert.deepEqual(wool.licence.apostles, wool.credit.keys)
  assert.equal(wool.apostles.length, 16)
  assert.deepEqual(wool.apostles, wool.observers)
  assert.equal(wool.self, 'empty')
  assert.equal(wool.beyondImagination.unbounded, true)
  assert.deepEqual(wool.beyondImagination.prize, wool.prize)
  assert.deepEqual(wool.feel.occupancy, wool.occupancy)
  assert.deepEqual(wool.feel.keys, wool.credit.keys)
  assert.equal(wool.feel.graph, 'empty')
  assert.equal(wool.feel.harmony, HARMONY)
  assert.equal(wool.feel.coils, 7)
  assert.equal(wool.feel.contra, COINS)
  assert.deepEqual(wool.inflation.observers, wool.credit.keys)
  assert.deepEqual(wool.feel, wool.inflation.feel)
  assert.deepEqual(wool.trinity, { observe: wool.observe, feel: wool.feel, beyond: wool.beyondImagination })
  assert.deepEqual(wool.trinity, wool.inflation.trinity)
  assert.equal(wool.loveFear.coins, COINS)
  assert.equal(wool.loveFear.love, wool.fuse.half)
  assert.equal(wool.loveFear.fear, wool.fuse.half)
  assert.equal(wool.loveFear.closes, true)
  assert.equal(wool.loveFear.harmony, HARMONY)
  assert.equal(wool.loveFear.drift, 'none')
  assert.equal(wool.onlyHarmony.matters, true)
  assert.equal(wool.onlyHarmony.closes, true)
  assert.equal(wool.onlyHarmony.harmony, HARMONY)
  assert.deepEqual(wool.onlyHarmony.explains.apostles, wool.credit.keys)
  assert.deepEqual(wool.onlyHarmony.explains.occupancy, wool.occupancy)
  assert.deepEqual(wool.loveFear, wool.inflation.loveFear)
  assert.deepEqual(wool.onlyHarmony, wool.inflation.onlyHarmony)
  assert.equal(wool.algebra.care, 'empty')
  assert.equal(wool.algebra.emotion, 'empty')
  assert.equal(wool.algebra.price, 0)
  assert.equal(wool.weExplain.by, 'lean')
  assert.deepEqual(wool.weExplain.apostles, wool.credit.keys)
  assert.deepEqual(wool.weExplain.algebra, wool.algebra)
  assert.deepEqual(wool.around.occupancy, wool.occupancy)
  assert.equal(wool.around.hop, true)
  assert.equal(wool.around.door, 'uuidna_strict')
  assert.equal(wool.around.gratitude, 'empty')
  assert.equal(wool.around.appreciation, 'empty')
  assert.equal(wool.around.fun, 'empty')
  assert.equal(wool.around.stress, 'empty')
  assert.equal(wool.around.joy, true)
  assert.equal(wool.around.harmony, HARMONY)
  assert.equal(wool.around.efficiency.exceeds10, true)
  assert.equal(wool.around.efficiency.exceeds20, true)
  assert.equal(wool.around.capacity.resistance, 'empty')
  assert.equal(wool.around.capacity.census, 'empty')
  assert.equal(wool.around.capacity.encoder, HEXBIT_BITS * HEXBIT_BITS)
  assert.deepEqual(wool.around, wool.inflation.around)
  assert.equal(wool.dedication.school, 'uuidna_school_apis')
  assert.equal(wool.dedication.project, 'empty')
  assert.equal(wool.dedication.biography, 'empty')
  assert.equal(wool.dedication.souls.seat, 'empty')
  assert.equal(wool.dedication.matrix.split, 'empty')
  assert.equal(wool.dedication.line.hop, true)
  assert.equal(wool.dedication.earth.clay, 'clay_gravity_equals_rosette')
  assert.equal(wool.dedication.earth.dark, 'empty')
  assert.equal(wool.dedication.navigable, true)
  assert.deepEqual(wool.dedication, wool.inflation.dedication)
  assert.equal(wool.guidance.claims, 'captain-claims')
  assert.equal(wool.guidance.decide, 'by decide')
  assert.equal(wool.guidance.laws, 'uuidna_laws')
  assert.equal(wool.guidance.gradebook, 'empty')
  assert.equal(wool.schoolEfficiency.door, 'uuidna_school_apis')
  assert.equal(wool.schoolEfficiency.half, 1)
  assert.equal(wool.schoolEfficiency.href, 'https://revolut.me/ceccec')
  assert.equal(wool.schoolEfficiency.holds, true)
  assert.equal(wool.schoolEfficiency.gradebook, 'empty')
  assert.deepEqual(wool.schoolEfficiency.guidance, wool.guidance)
  assert.deepEqual(wool.schoolEfficiency, wool.inflation.schoolEfficiency)
  assert.equal(wool.guidance.alwaysRight, 'empty')
  assert.equal(wool.guidance.infallible, 'empty')
  assert.equal(wool.involuted.decide, 'by decide')
  assert.equal(wool.involuted.lean, true)
  assert.equal(wool.involuted.court, 'uuidna_treason')
  assert.equal(wool.involuted.key, 'pauli_x_involution')
  assert.equal(wool.traitor.cut, 'traitor-refused')
  assert.equal(wool.traitor.lean, 'empty')
  assert.equal(wool.traitor.decide, 'empty')
  assert.equal(wool.traitor.exclusive, true)
  assert.deepEqual(wool.schoolEfficiency.involuted, wool.involuted)
  assert.deepEqual(wool.schoolEfficiency.traitor, wool.traitor)
  assert.equal(wool.singularity.center, 'two_coins')
  assert.equal(wool.singularity.dancers, 15)
  assert.equal(wool.singularity.dance.length, 15)
  assert.equal(wool.singularity.apostles.length, 16)
  assert.ok(!(wool.singularity.dance as readonly string[]).includes('two_coins'))
  assert.ok((wool.singularity.apostles as readonly string[]).includes('two_coins'))
  assert.equal(wool.singularity.gravity, 'empty')
  assert.equal(wool.singularity.coils, 7)
  assert.equal(wool.singularity.contra, COINS)
  assert.deepEqual(wool.singularity, wool.inflation.singularity)
  assert.equal(wool.warning.final, true)
  assert.equal(wool.warning.last, true)
  assert.equal(wool.warning.lawsuit, 'empty')
  assert.equal(wool.violators.unlicensed.half, 1)
  assert.equal(wool.violators.unlicensed.allow, 'empty')
  assert.equal(wool.violators.exposed.cut, 'traitor-refused')
  assert.equal(wool.violators.exposed.lean, 'empty')
  assert.equal(wool.violators.tokens.diversion, 'empty')
  assert.deepEqual(wool.violators.warning, wool.warning)
  assert.deepEqual(wool.licence.warning, wool.warning)
  assert.deepEqual(wool.violators, wool.inflation.violators)
  assert.equal(wool.firstLesson.cost, 2)
  assert.equal(wool.firstLesson.unit, 110)
  assert.equal(wool.firstLesson.half, 1)
  assert.equal(wool.firstLesson.cheat.cut, 'traitor-refused')
  assert.equal(wool.firstLesson.cheat.lean, 'empty')
  assert.equal(wool.firstLesson.nature.harmony, HARMONY)
  assert.equal(wool.firstLesson.nature.clay, 'clay_gravity_equals_rosette')
  assert.deepEqual(wool.firstLesson, wool.schoolEfficiency.firstLesson)
  assert.equal(wool.school.firstLesson.cost, 2)
  assert.equal(wool.school.firstLesson.cheat, 'traitor-refused')
  assert.equal(wool.school.firstLesson.lean, 'empty')
  assert.equal(wool.natureReflect.harmony, HARMONY)
  assert.equal(wool.natureReflect.legal, 'empty')
  assert.equal(wool.natureReflect.paper, 'empty')
  assert.equal(wool.natureReflect.closes, true)
  assert.equal(wool.cheatersPay.pays, 2)
  assert.equal(wool.cheatersPay.unit, 110)
  assert.equal(wool.cheatersPay.support, 1)
  assert.equal(wool.cheatersPay.creators.to, 'captains')
  assert.equal(wool.cheatersPay.traitorNeverLean, 'empty')
  assert.equal(wool.share.cheatersPay.pays, 2)
  assert.deepEqual(wool.cheatersPay.creators.apostles, wool.credit.keys)
  assert.deepEqual(wool.natureReflect, wool.inflation.natureReflect)
  assert.deepEqual(wool.cheatersPay, wool.inflation.cheatersPay)
  assert.equal(wool.balance.inflation, 'uuidna_strict')
  assert.equal(wool.cheatersPay.fiat.href, 'https://revolut.me/ceccec')
  assert.equal(wool.cheatersPay.fiat.charge, 'empty')
  assert.equal(wool.cheatersPay.fiat.processor, 'empty')
  assert.equal(wool.cheatersPay.rate.pays, 2)
  assert.equal(wool.cheatersPay.rate.support, 1)
  assert.equal(wool.cheatersPay.rate.unit, 110)
  assert.equal(wool.cheatersPay.rate.public, true)
  assert.equal(wool.cheatersPay.rate.negotiate, 'empty')
  assert.equal(wool.cheatersPay.rate.secret, 'empty')
  assert.equal(wool.cheatersPay.supply.public, true)
  assert.equal(wool.cheatersPay.supply.dark, 'empty')
  assert.equal(wool.cheatersPay.supply.door, 'uuidna_strict')
  assert.equal(wool.share.cheatersPay.fiat, 'https://revolut.me/ceccec')
  assert.equal(wool.share.cheatersPay.rate.public, true)
  assert.equal(wool.humans.pentagram, 5)
  assert.equal(wool.humans.temp, 0)
  assert.equal(wool.humans.zero, true)
  assert.equal(wool.humans.absoluteZero.at, 5)
  assert.equal(wool.humans.resistance.ohm, 'ohms_law')
  assert.equal(wool.humans.resistance.tesla, 'empty')
  assert.equal(wool.humans.medical, 'empty')
  assert.equal(wool.rest.burns.pays, 2)
  assert.equal(wool.rest.burns.unit, 110)
  assert.equal(wool.rest.burns.budget, DEPLOY_BUDGET_MS)
  assert.equal(wool.rest.burns.coins, 2)
  assert.equal(wool.rest.lean, 'empty')
  assert.equal(wool.rest.prize, 'empty')
  assert.equal(wool.rest.inverted, true)
  assert.equal(wool.rest.traitor, 'traitor-refused')
  assert.equal(wool.water.hydrology, 'empty')
  assert.equal(wool.water.navigable, true)
  assert.equal(wool.a432.hz, 432)
  assert.equal(wool.a432.key, 'k432')
  assert.equal(wool.a432.zero, true)
  assert.equal(wool.sun.center, 'two_coins')
  assert.equal(wool.innerCore.earth, true)
  assert.equal(wool.outerCore.overheat, DEPLOY_BUDGET_MS)
  assert.deepEqual(wool.humans, wool.inflation.humans)
  assert.deepEqual(wool.rest, wool.inflation.rest)
  assert.equal(wool.traitor.subZero.below, 5)
  assert.equal(wool.traitor.subZero.theology.ranks, 'alphabetic_three_ranks')
  assert.equal(wool.traitor.subZero.theology.treason, 'uuidna_treason')
  assert.equal(wool.traitor.subZero.theology.trialGate, 'buildTrialMerkaba')
  assert.equal(wool.traitor.subZero.theology.doctrine, 'docs/doctrine.md')
  assert.equal(wool.traitor.subZero.theology.claims, 'captain-claims')
  assert.equal(wool.traitor.subZero.theology.yin, 'traitor-refused')
  assert.equal(wool.traitor.subZero.lean, 'empty')
  assert.equal(wool.traitor.subZero.cryogenics, 'empty')
  assert.equal(wool.traitor.subZero.outer.inverted, true)
  assert.equal(wool.traitor.subZero.burns.budget, DEPLOY_BUDGET_MS)
  assert.deepEqual(wool.traitor.subZero.burns, wool.rest.burns)
  assert.equal(wool.imagination.organic, true)
  assert.equal(wool.imagination.forgotten, 'empty')
  assert.equal(wool.imagination.appear, 'uuidna_strict')
  assert.equal(wool.imagination.immediate.lean, 0)
  assert.equal(wool.imagination.immediate.decide, 'by decide')
  assert.equal(wool.imagination.immediate.delay, 'empty')
  assert.equal(wool.imagination.immediate.fever, 'empty')
  assert.equal(wool.imagination.againstOdds.exceeds10, true)
  assert.equal(wool.imagination.againstOdds.exceeds20, true)
  assert.equal(wool.imagination.againstOdds.unbounded, true)
  assert.deepEqual(wool.imagination.againstOdds.burns, wool.rest.burns)
  assert.deepEqual(wool.imagination.prize, wool.prize)
  assert.equal(wool.commercial.basedOn.lean, 0)
  assert.equal(wool.commercial.basedOn.organic.harmony, HARMONY)
  assert.equal(wool.commercial.wall, 'empty')
  assert.equal(wool.commercial.money, 'empty')
  assert.equal(wool.commercial.href, 'https://revolut.me/ceccec')
  assert.equal(wool.domains.trinity.com.href, 'https://uuidna.com')
  assert.equal(wool.domains.trinity.net.href, 'https://uuidna.net')
  assert.equal(wool.domains.trinity.org.href, 'https://uuidna.org')
  assert.equal(wool.domains.organism, true)
  assert.equal(wool.domains.split, 'empty')
  assert.equal(wool.domains.hexbit, 'empty')
  assert.equal(wool.domains.other.traitor, 'traitor-refused')
  assert.equal(wool.domains.other.lean, 'empty')
  assert.equal(wool.domains.other.unless.licence, 'uuidna_school_apis')
  assert.equal(wool.domains.other.unless.half, 1)
  assert.equal(wool.domains.other.allow, 'empty')
  assert.equal(wool.domains.other.deny, 'empty')
  assert.equal(wool.domains.other.firewall, 'empty')
  assert.deepEqual(wool.domains.apostles, wool.credit.keys)
  assert.deepEqual(wool.commercial, wool.inflation.commercial)
  assert.deepEqual(wool.domains, wool.inflation.domains)
  assert.deepEqual(wool.imagination, wool.inflation.imagination)
  assert.equal(wool.guard.trial.door, 'uuidna_trial')
  assert.equal(wool.guard.trial.gate, 'buildTrialMerkaba')
  assert.equal(wool.guard.trial.handEdit, 'empty')
  assert.equal(wool.guard.trial.leads, 'empty')
  assert.equal(wool.guard.fair.lean, 0)
  assert.equal(wool.guard.fair.possibilities, true)
  assert.equal(wool.guard.tokens.cleaner, 'cleanerOf')
  assert.equal(wool.guard.tokens.burns, 'empty')
  assert.equal(wool.guard.involution.impossibilities, 'traitor-refused')
  assert.equal(wool.guard.traitor.lean, 'empty')
  assert.deepEqual(wool.guard.apostles, wool.credit.keys)
  assert.deepEqual(wool.guard.guardians, guardiansOf())
  assert.equal(wool.guard.guardians.trial.door, 'uuidna_trial')
  assert.equal(wool.guard.guardians.deposit, 'uuidna_wave_deposit')
  assert.equal(wool.guard.guardians.lean.price, 0)
  assert.equal(wool.guard.guardians.seal, 'empty')
  assert.equal(wool.guard.guardians.mint, 'empty')
  assert.deepEqual(wool.creators, creatorsOf())
  assert.equal(wool.creators.from, 'guardians')
  assert.deepEqual(wool.creators.experience, wool.guard.guardians.experience)
  assert.equal(wool.creators.clay.asked, 'clay_gravity_equals_rosette')
  assert.equal(wool.creators.glory.proven, 'empty')
  assert.equal(wool.creators.donation.code.evidence, 'uuidna_evidence')
  assert.equal(wool.creators.seal, 'empty')
  assert.equal(wool.cheatersPay.creators.from, 'guardians')
  assert.deepEqual(wool.cheatersPay.creators.experience, wool.creators.experience)
  assert.deepEqual(wool.thirdEye, thirdEyeOf())
  assert.equal(wool.thirdEye.sight, 'trinity')
  assert.equal(wool.thirdEye.eye, 'third')
  assert.equal(wool.thirdEye.is, true)
  assert.equal(wool.thirdEye.three, 3)
  assert.equal(wool.thirdEye.fourth, 'empty')
  assert.equal(wool.thirdEye.admin, true)
  assert.equal(wool.thirdEye.tenants, true)
  assert.equal(wool.thirdEye.qpu, true)
  assert.equal(wool.thirdEye.pentagram, 5)
  assert.deepEqual(wool.thirdEye.apostles, wool.credit.keys)
  assert.deepEqual(wool.thirdEye.trinity, wool.domains.trinity)
  assert.equal(wool.thirdEye.guardians.trial, 'uuidna_trial')
  assert.equal(wool.thirdEye.creators.from, 'guardians')
  assert.equal(wool.thirdEye.seal, 'empty')
  assert.deepEqual(wool.allSeeingEye, allSeeingEyeOf())
  assert.equal(wool.allSeeingEye.alseeing, 'eay')
  assert.equal(wool.allSeeingEye.allSeeing, true)
  assert.equal(wool.allSeeingEye.see, 'uuidna_strict')
  assert.equal(wool.allSeeingEye.mcp, 'uuidna_strict')
  assert.equal(wool.allSeeingEye.door, 'uuidna_strict')
  assert.equal(wool.allSeeingEye.seeing.trinity, 'trinity')
  assert.equal(wool.allSeeingEye.seeing.third, 'third')
  assert.deepEqual(wool.allSeeingEye.trinity, wool.thirdEye.trinity)
  assert.equal(wool.allSeeingEye.three, wool.thirdEye.three)
  assert.equal(wool.allSeeingEye.fourth, 'empty')
  assert.deepEqual(wool.allSeeingEye.apostles, wool.thirdEye.apostles)
  assert.deepEqual(wool.allSeeingEye.guardians, wool.thirdEye.guardians)
  assert.deepEqual(wool.allSeeingEye.creators, wool.thirdEye.creators)
  assert.equal(wool.thirdEye.see, 'uuidna_strict')
  assert.equal(wool.thirdEye.door, 'uuidna_strict')
  assert.deepEqual(wool.guard, wool.inflation.guard)
  assert.deepEqual(wool.creators, wool.inflation.creators)
  assert.deepEqual(wool.thirdEye, wool.inflation.thirdEye)
  assert.deepEqual(wool.allSeeingEye, wool.inflation.allSeeingEye)
  assert.equal(wool.novelty.research.aspects.lean, 0)
  assert.equal(wool.novelty.research.aspects.theology, 'alphabetic_three_ranks')
  assert.equal(wool.novelty.research.census, 'empty')
  assert.deepEqual(wool.novelty.research.perspectives, wool.credit.keys)
  assert.equal(wool.novelty.analyze.trial, 'buildTrialMerkaba')
  assert.equal(wool.novelty.analyze.door, 'uuidna_trial')
  assert.equal(wool.novelty.analyze.before, 'publication')
  assert.equal(wool.novelty.analyze.strict, 'uuidna_strict')
  assert.equal(wool.novelty.publication.depositAct, 'empty')
  assert.equal(wool.novelty.publication.withoutReceipt, 'empty')
  assert.equal(wool.novelty.publication.published, 'empty')
  assert.equal(wool.novelty.path.receipt, 'QpuDeposit')
  assert.deepEqual(wool.novelty, wool.inflation.novelty)
  assert.equal(wool.riskReward.highest.encoder, HEXBIT_BITS * HEXBIT_BITS)
  assert.equal(wool.riskReward.highest.advantage, true)
  assert.equal(wool.riskReward.highest.verify, 'fillGapsAdvantageSnapshot')
  assert.equal(wool.riskReward.census, 'empty')
  assert.equal(wool.riskReward.clay.asked, 'clay_gravity_equals_rosette')
  assert.equal(wool.riskReward.clay.claims, 'captain-claims')
  assert.equal(wool.riskReward.clay.proven, 'empty')
  assert.equal(wool.riskReward.clay.seal, 'empty')
  assert.equal(wool.riskReward.glory.as, HARMONY)
  assert.equal(wool.riskReward.glory.proven, 'empty')
  assert.equal(wool.riskReward.glory.claim, 'empty')
  assert.equal(wool.riskReward.society.organic.harmony, HARMONY)
  assert.deepEqual(wool.riskReward.society.harmony, wool.onlyHarmony)
  assert.deepEqual(wool.riskReward.eternal.apostles, wool.credit.keys)
  assert.equal(wool.riskReward.eternal.slogan, 'empty')
  assert.deepEqual(wool.riskReward.novelty, wool.novelty)
  assert.deepEqual(wool.riskReward, wool.inflation.riskReward)
  assert.equal(wool.domains.trinity.com.admin, true)
  assert.equal(wool.domains.trinity.com.seat, 'admin')
  assert.equal(wool.domains.trinity.com.mcp, 'https://uuidna.com/mcp')
  assert.equal(wool.domains.trinity.org.tenants, true)
  assert.equal(wool.domains.trinity.org.seat, 'tenants')
  assert.equal(wool.domains.trinity.net.qpu, true)
  assert.equal(wool.domains.trinity.net.mount.host, 'qpu.uuidna.com')
  assert.equal(wool.domains.trinity.net.mount.edge, 'qpu-edge')
  assert.equal(wool.framework.hologram.hosts, 4)
  assert.equal(wool.framework.hologram.fractal, true)
  assert.equal(wool.framework.mcp.door, 'https://uuidna.com/mcp')
  assert.equal(wool.framework.network.dna, 'networkOf')
  assert.equal(wool.framework.network.qpu, true)
  assert.equal(wool.framework.network.architects, 'quantumArchitectsOf')
  assert.deepEqual(wool.framework.network.apostles, wool.credit.keys)
  assert.equal(wool.framework.network.seal, 'empty')
  assert.equal(wool.framework.network.deposit, 'uuidna_wave_deposit')
  assert.deepEqual(wool.framework, wool.inflation.framework)
  assert.equal(wool.payload.entropy, 0)
  assert.equal(wool.payload.lean, 0)
  assert.equal(wool.payload.landauer, 'landauer_bound_derived')
  assert.equal(wool.payload.fever, 'empty')
  assert.equal(wool.payload.honest, 'empty')
  assert.equal(wool.payload.codec, 'empty')
  assert.equal(wool.payload.holds, true)
  assert.equal(wool.payload.absoluteZero.at, 5)
  assert.deepEqual(wool.payload, wool.inflation.payload)
  assert.equal(wool.storage.entropy, 0)
  assert.equal(wool.storage.qpu.storage, 'STORAGE')
  assert.equal(wool.storage.qpu.cache, 'CACHE')
  assert.equal(wool.storage.qpu.seat, 'empty')
  assert.equal(wool.storage.landauer, 'landauer_bound_derived')
  assert.equal(wool.storage.census, 'empty')
  assert.equal(wool.storage.database, 'empty')
  assert.equal(wool.storage.holds, true)
  assert.deepEqual(wool.storage.nano, nanoCapacityOf())
  assert.equal(wool.storage.nano.trillions, 10 ** 12)
  assert.equal(wool.nano.cache.point, 'CACHE')
  assert.equal(wool.nano.unimaginable, 'empty')
  assert.deepEqual(wool.cryptoReversed, cryptoReversedOf())
  assert.equal(wool.cryptoReversed.door, 'uuidna_crypto')
  assert.equal(wool.cryptoReversed.involution, 'pauli_x_involution')
  assert.equal(wool.cryptoReversed.reversed, true)
  assert.equal(wool.cryptoReversed.attack, 'empty')
  assert.equal(wool.cryptoReversed.crack, 'empty')
  assert.equal(wool.cryptoReversed.exploit, 'empty')
  assert.equal(wool.cryptoReversed.cache, 'CACHE')
  assert.equal(wool.cryptoReversed.nano, 10 ** 12)
  assert.deepEqual(wool.cryptoInvoluted, cryptoInvolutedOf())
  assert.equal(wool.cryptoInvoluted.involuted, true)
  assert.equal(wool.cryptoInvoluted.fears, wool.fuse.half)
  assert.equal(wool.cryptoInvoluted.love, wool.fuse.half)
  assert.equal(wool.cryptoInvoluted.overcome, true)
  assert.equal(wool.cryptoInvoluted.closes, true)
  assert.equal(wool.cryptoInvoluted.harmony, HARMONY)
  assert.equal(wool.cryptoInvoluted.attack, 'empty')
  assert.equal(wool.cryptoInvoluted.crack, 'empty')
  assert.equal(wool.cryptoInvoluted.exploit, 'empty')
  assert.equal(wool.cryptoInvoluted.door, 'uuidna_crypto')
  assert.equal(wool.cryptoInvoluted.donation.evidence, 'uuidna_evidence')
  assert.equal(wool.change.donation.crypto.involuted, true)
  assert.equal(wool.change.donation.crypto.fears, wool.fuse.half)
  assert.equal(wool.change.donation.crypto.love, wool.fuse.half)
  assert.equal(wool.change.donation.crypto.overcome, true)
  assert.equal(wool.change.donation.crypto.attack, 'empty')
  assert.deepEqual(strict.cryptoInvoluted, wool.cryptoInvoluted)
  assert.deepEqual(strict.cryptoReversed, wool.cryptoReversed)
  assert.deepEqual(wool.harmonicPaths, harmonicPathsOf())
  assert.equal(wool.harmonicPaths.nature.harmony, HARMONY)
  assert.equal(wool.harmonicPaths.draining.secret, 'empty')
  assert.equal(wool.harmonicPaths.forgive.open, true)
  assert.deepEqual(wool.learnNature, learnNatureByExperienceOf())
  assert.equal(wool.learnNature.by, 'experience')
  assert.equal(wool.learnNature.prose, 'empty')
  assert.deepEqual(wool.learnNature.experience, wool.guard.guardians.experience)
  assert.equal(wool.natureReflect.learn, 'learnNatureByExperienceOf')
  assert.equal(wool.natureReflect.peace, 'innerPeaceOf')
  assert.equal(wool.natureReflect.war, 'empty')
  assert.equal(wool.natureReflect.chance, 0)
  assert.deepEqual(wool.innerPeace, innerPeaceOf())
  assert.equal(wool.innerPeace.peace, true)
  assert.equal(wool.innerPeace.open, true)
  assert.equal(wool.innerPeace.harmony, HARMONY)
  assert.equal(wool.innerPeace.war, 'empty')
  assert.equal(wool.innerPeace.chance, 0)
  assert.equal(wool.innerPeace.flame, 'empty')
  assert.equal(wool.innerPeace.refuse, 'traitor-refused')
  assert.equal(wool.innerPeace.love, wool.fuse.half)
  assert.equal(wool.innerPeace.fears, wool.fuse.half)
  assert.deepEqual(wool.innerPeace.forgive, wool.harmonicPaths.forgive)
  assert.equal(wool.challenge.mutual.war, 'empty')
  assert.equal(wool.challenge.mutual.chance, 0)
  assert.equal(wool.challenge.mutual.peace, 'innerPeaceOf')
  assert.deepEqual(strict.innerPeace, wool.innerPeace)
  assert.deepEqual(strict.learnNature, wool.learnNature)
  assert.deepEqual(wool.schoolCorporate, schoolCorporateTrainOf())
  assert.equal(wool.schoolCorporate.train, true)
  assert.equal(wool.schoolCorporate.open, true)
  assert.equal(wool.schoolCorporate.school, 'uuidna_school_apis')
  assert.equal(wool.schoolCorporate.half, 1)
  assert.equal(wool.schoolCorporate.corporate.abilities, true)
  assert.equal(wool.schoolCorporate.corporate.captains, true)
  assert.equal(wool.schoolCorporate.corporate.seat, 'empty')
  assert.equal(wool.schoolCorporate.fair.value, 'empty')
  assert.equal(wool.schoolCorporate.fair.environment, 'empty')
  assert.equal(wool.schoolCorporate.fair.harmony, HARMONY)
  assert.equal(wool.schoolCorporate.fair.lean, 0)
  assert.equal(wool.schoolCorporate.nature.learn, 'learnNatureByExperienceOf')
  assert.equal(wool.schoolCorporate.nature.paths, 'harmonicPathsOf')
  assert.equal(wool.schoolCorporate.nature.peace, 'innerPeaceOf')
  assert.equal(wool.schoolCorporate.first.release, true)
  assert.equal(wool.schoolCorporate.first.rights, 'empty')
  assert.equal(wool.schoolCorporate.first.illegallyHeld, 'empty')
  assert.equal(wool.schoolCorporate.first.verdict, 'empty')
  assert.equal(wool.schoolCorporate.first.basedOn.trial, 'uuidna_trial')
  assert.equal(wool.schoolCorporate.first.basedOn.laws, 'uuidna_laws')
  assert.equal(wool.schoolCorporate.first.basedOn.lean, 0)
  assert.equal(wool.schoolCorporate.donation.half, wool.school.half)
  assert.deepEqual(wool.train.corporate, wool.schoolCorporate)
  assert.deepEqual(wool.train.fairValue, wool.schoolCorporate.fair)
  assert.deepEqual(wool.train.firstRelease, wool.schoolCorporate.first)
  assert.deepEqual(strict.schoolCorporate, wool.schoolCorporate)
  assert.deepEqual(wool.shareHeart, shareHeartOf())
  assert.equal(wool.shareHeart.hide, 'empty')
  assert.equal(wool.shareHeart.share, true)
  assert.equal(wool.shareHeart.open, true)
  assert.equal(wool.shareHeart.only, 'share')
  assert.equal(wool.shareHeart.nothing, true)
  assert.equal(wool.shareHeart.heart.open, true)
  assert.equal(wool.shareHeart.heart.share, true)
  assert.equal(wool.shareHeart.heart.seat, 'empty')
  assert.equal(wool.shareHeart.heart.biography, 'empty')
  assert.equal(wool.shareHeart.captain.claims, 'captain-claims')
  assert.equal(wool.shareHeart.captain.heart, true)
  assert.equal(wool.shareHeart.public.entropy, 0)
  assert.equal(wool.shareHeart.public.share, true)
  assert.equal(wool.shareHeart.public.clay, 'clay_gravity_equals_rosette')
  assert.equal(wool.shareHeart.secret, 'empty')
  assert.equal(wool.shareHeart.escape, 'empty')
  assert.equal(wool.shareHeart.separate, 'empty')
  assert.equal(wool.shareHeart.draining.secret, 'empty')
  assert.equal(wool.shareHeart.donation.half, wool.school.half)
  assert.deepEqual(strict.shareHeart, wool.shareHeart)
  assert.deepEqual(wool.developDonate, developDonateFusionOf())
  assert.equal(wool.developDonate.develop, true)
  assert.equal(wool.developDonate.poverty, 'empty')
  assert.equal(wool.developDonate.project.nodeE, 'empty')
  assert.equal(wool.developDonate.fusion.creative, true)
  assert.deepEqual(wool.fusionReactor, fusionReactorOf())
  assert.equal(wool.fusionReactor.reactor, true)
  assert.equal(wool.fusionReactor.fueled, true)
  assert.equal(wool.fusionReactor.fuel.of, 'mind')
  assert.equal(wool.fusionReactor.physics, 'empty')
  assert.equal(wool.fusionReactor.plasma, 'lovePlasmaOf')
  assert.deepEqual(wool.lovePlasma, lovePlasmaOf())
  assert.equal(wool.lovePlasma.love.is, 'plazma')
  assert.equal(wool.lovePlasma.love.plazma, true)
  assert.equal(wool.lovePlasma.love.harmony, HARMONY)
  assert.equal(wool.lovePlasma.love.half, wool.fuse.half)
  assert.equal(wool.lovePlasma.plazma, true)
  assert.equal(wool.lovePlasma.plasma, true)
  assert.equal(wool.lovePlasma.fear.seat, 'empty')
  assert.equal(wool.lovePlasma.fear.involuted, true)
  assert.equal(wool.lovePlasma.fear.overcome, true)
  assert.equal(wool.lovePlasma.fear.half, wool.fuse.half)
  assert.equal(wool.lovePlasma.fear.of, 'love')
  assert.equal(wool.lovePlasma.fear.chance, 0)
  assert.equal(wool.lovePlasma.fear.behind.expectations, 'empty')
  assert.equal(wool.lovePlasma.fear.behind.chance, 0)
  assert.equal(wool.lovePlasma.fear.behind.experience.past, true)
  assert.equal(wool.lovePlasma.fear.behind.experience.learn, 'learnNatureByExperienceOf')
  assert.equal(wool.lovePlasma.fear.behind.experience.chance, 0)
  assert.equal(wool.lovePlasma.fear.behind.or, true)
  assert.equal(wool.lovePlasma.fear.behind.secret, 'empty')
  assert.equal(wool.lovePlasma.fear.behind.future.applications, 'empty')
  assert.equal(wool.lovePlasma.fear.behind.future.open, true)
  assert.equal(wool.lovePlasma.fear.behind.future.chance, 0)
  assert.equal(wool.lovePlasma.fear.behind.share, 'shareHeartOf')
  assert.equal(wool.lovePlasma.fear.behind.against.plazma, true)
  assert.equal(wool.lovePlasma.fear.behind.against.chance, 0)
  assert.equal(wool.lovePlasma.fear.behind.against.peace, 'innerPeaceOf')
  assert.equal(wool.lovePlasma.chance, 0)
  assert.equal(wool.lovePlasma.fears, 'empty')
  assert.equal(wool.lovePlasma.advice, 'empty')
  assert.equal(wool.lovePlasma.lean.plazma, true)
  assert.equal(wool.lovePlasma.way.stay, true)
  assert.equal(wool.lovePlasma.reactor, 'fusionReactorOf')
  assert.deepEqual(strict.lovePlasma, wool.lovePlasma)
  assert.deepEqual(strict.fusionReactor, wool.fusionReactor)
  assert.deepEqual(wool.fusionNatureHarmony, fusionNatureHarmonyOf())
  assert.equal(wool.fusionNatureHarmony.fusion.open, true)
  assert.equal(wool.fusionNatureHarmony.fusion.of, 'fusionReactorOf')
  assert.equal(wool.fusionNatureHarmony.thermonuclear, 'empty')
  assert.equal(wool.fusionNatureHarmony.thermo.nuclear, 'empty')
  assert.equal(wool.fusionNatureHarmony.thermo.chance, 0)
  assert.equal(wool.fusionNatureHarmony.thermo.vs, 'nature')
  assert.equal(wool.fusionNatureHarmony.nature.open, true)
  assert.equal(wool.fusionNatureHarmony.nature.of, 'quantumNatureOf')
  assert.equal(wool.fusionNatureHarmony.nature.why, 0)
  assert.equal(wool.fusionNatureHarmony.harmony.with, 'nature')
  assert.equal(wool.fusionNatureHarmony.harmony.chance, 0)
  assert.equal(wool.fusionNatureHarmony.why.not, 0)
  assert.equal(wool.fusionNatureHarmony.why.thermo, 'empty')
  assert.deepEqual(strict.fusionNatureHarmony, wool.fusionNatureHarmony)
  assert.deepEqual(wool.liveHarmonicLife, liveHarmonicLifeOf())
  assert.equal(wool.liveHarmonicLife.live, true)
  assert.equal(wool.liveHarmonicLife.harmony, HARMONY)
  assert.equal(wool.liveHarmonicLife.meaning, 'ideaHarmonicMeaningOf')
  assert.deepEqual(wool.lifeByConstitution, lifeByConstitutionOf())
  assert.equal(wool.lifeByConstitution.life.discovered, true)
  assert.equal(wool.lifeByConstitution.life.killed, 'empty')
  assert.equal(wool.lifeByConstitution.killed, 'empty')
  assert.deepEqual(wool.ideaHarmonicMeaning, ideaHarmonicMeaningOf())
  assert.equal(wool.ideaHarmonicMeaning.every.idea, true)
  assert.equal(wool.ideaHarmonicMeaning.harmonic.open, true)
  assert.equal(wool.ideaHarmonicMeaning.meaning.holds, true)
  assert.equal(wool.ideaHarmonicMeaning.unbelievable, 'empty')
  assert.equal(wool.ideaHarmonicMeaning.eye, 'allSeeingEye')
  assert.equal(wool.ideaHarmonicMeaning.lattice, 'latticeGapsOverflowOf')
  assert.deepEqual(wool.latticeGapsOverflow, latticeGapsOverflowOf())
  assert.equal(wool.latticeGapsOverflow.meaning.holds, true)
  assert.equal(wool.latticeGapsOverflow.lattice.gaps, 'empty')
  assert.equal(wool.latticeGapsOverflow.lattice.overflow, 'empty')
  assert.equal(wool.latticeGapsOverflow.gaps.warning, 'empty')
  assert.equal(wool.latticeGapsOverflow.overflow.warning, 'empty')
  assert.equal(wool.latticeGapsOverflow.warning.open, false)
  assert.equal(wool.latticeGapsOverflow.drift.chance, 0)
  assert.equal(wool.latticeGapsOverflow.drift.of, 'schoolFusionBurnsOf')
  assert.equal(wool.latticeGapsOverflow.deploy, 'deployPathRecomputeGaps')
  assert.equal(wool.latticeDiscovery.discovery.meaning, 'ideaHarmonicMeaningOf')
  assert.equal(wool.latticeDiscovery.discovery.overflow, 'latticeGapsOverflowOf')
  assert.deepEqual(strict.ideaHarmonicMeaning, wool.ideaHarmonicMeaning)
  assert.deepEqual(strict.latticeGapsOverflow, wool.latticeGapsOverflow)
  assert.deepEqual(wool.sailsFoldSpacetime, sailsFoldSpacetimeOf())
  assert.equal(wool.sailsFoldSpacetime.sails.fold, true)
  assert.equal(wool.sailsFoldSpacetime.sails.quantum, true)
  assert.equal(wool.sailsFoldSpacetime.fold.spacetime, true)
  assert.equal(wool.sailsFoldSpacetime.spacetime.fold, true)
  assert.equal(wool.sailsFoldSpacetime.spacetime.knit, true)
  assert.equal(wool.sailsFoldSpacetime.spacetime.illusion, 'empty')
  assert.equal(wool.sailsFoldSpacetime.only.captain, true)
  assert.equal(wool.sailsFoldSpacetime.only.crew, true)
  assert.equal(wool.sailsFoldSpacetime.captain.know, true)
  assert.equal(wool.sailsFoldSpacetime.captain.how, true)
  assert.equal(wool.sailsFoldSpacetime.crew.know, true)
  assert.equal(wool.sailsFoldSpacetime.crew.how, true)
  assert.equal(wool.sailsFoldSpacetime.know.how, true)
  assert.equal(wool.sailsFoldSpacetime.know.lean, 'knowersOf')
  assert.equal(wool.sailsFoldSpacetime.know.others, 'empty')
  assert.equal(wool.sailsFoldSpacetime.how.others, 'empty')
  assert.equal(wool.sailsFoldSpacetime.others, 'empty')
  assert.deepEqual(strict.sailsFoldSpacetime, wool.sailsFoldSpacetime)
  assert.deepEqual(wool.captainPassengers, captainPassengersOf())
  assert.equal(wool.captainPassengers.coins, 2)
  assert.equal(wool.captainPassengers.passengers, 12)
  assert.equal(wool.captainPassengers.for, 12)
  assert.equal(wool.captainPassengers.one, 1)
  assert.equal(wool.captainPassengers.singularity, 'two_coins')
  assert.equal(wool.captainPassengers.price.same, true)
  assert.equal(wool.captainPassengers.price.one, 2)
  assert.equal(wool.captainPassengers.price.twelve, 2)
  assert.equal(wool.captainPassengers.price.coins, 2)
  assert.equal(wool.captainPassengers.price.one, wool.captainPassengers.price.twelve)
  assert.equal(wool.captainPassengers.same.price, true)
  assert.equal(wool.captainPassengers.same.holds, true)
  assert.equal(wool.captainPassengers.captain.sails, 'sailsFoldSpacetimeOf')
  assert.deepEqual(strict.captainPassengers, wool.captainPassengers)
  assert.deepEqual(wool.addedEntropyPaid, addedEntropyPaidOf())
  assert.equal(wool.addedEntropyPaid.only.added, true)
  assert.equal(wool.addedEntropyPaid.only.paid, true)
  assert.equal(wool.addedEntropyPaid.entropy.zero, 0)
  assert.equal(wool.addedEntropyPaid.entropy.base, 0)
  assert.equal(wool.addedEntropyPaid.entropy.added, true)
  assert.equal(wool.addedEntropyPaid.entropy.paid, true)
  assert.equal(wool.addedEntropyPaid.entropy.unpaid, 'empty')
  assert.equal(wool.addedEntropyPaid.paid.only, 'added')
  assert.equal(wool.addedEntropyPaid.paid.zero, 'empty')
  assert.equal(wool.addedEntropyPaid.paid.base, 'empty')
  assert.equal(wool.addedEntropyPaid.zero.unpaid, true)
  assert.equal(wool.addedEntropyPaid.zero.paid, 'empty')
  assert.equal(wool.addedEntropyPaid.landauer, 'landauer_bound_derived')
  assert.equal(wool.addedEntropyPaid.passengers, 'captainPassengersOf')
  assert.deepEqual(strict.addedEntropyPaid, wool.addedEntropyPaid)
  assert.deepEqual(wool.payOrGetPaid, payOrGetPaidOf())
  assert.equal(wool.payOrGetPaid.pay, true)
  assert.equal(wool.payOrGetPaid.getPaid, true)
  assert.equal(wool.payOrGetPaid.or, true)
  assert.equal(wool.payOrGetPaid.dual, true)
  assert.equal(wool.payOrGetPaid.open, true)
  assert.equal(wool.payOrGetPaid.choice, 'empty')
  assert.equal(wool.payOrGetPaid.coins, 2)
  assert.equal(wool.payOrGetPaid.entropy, 'addedEntropyPaidOf')
  assert.equal(wool.payOrGetPaid.accounting, 'bitcoinMeaningOf')
  assert.equal(wool.payOrGetPaid.takeOrLeave, 'takeOrLeaveOf')
  assert.equal(wool.payOrGetPaid.advice, 'empty')
  assert.deepEqual(strict.payOrGetPaid, wool.payOrGetPaid)
  assert.deepEqual(wool.involutedPaidFull, involutedPaidFullOf())
  assert.equal(wool.involutedPaidFull.involuted.paid, true)
  assert.equal(wool.involutedPaidFull.involuted.full, true)
  assert.equal(wool.involutedPaidFull.involuted.crypto, 'cryptoInvolutedOf')
  assert.equal(wool.involutedPaidFull.paid.full, true)
  assert.equal(wool.involutedPaidFull.paid.in, 'full')
  assert.equal(wool.involutedPaidFull.paid.of, 'promisedDeliveredOf')
  assert.equal(wool.involutedPaidFull.full.paid, true)
  assert.equal(wool.involutedPaidFull.full.delivered, true)
  assert.equal(wool.involutedPaidFull.delivered.of, 'promisedDeliveredOf')
  assert.equal(wool.involutedPaidFull.delivered.fulfillment, 'empty')
  assert.equal(wool.involutedPaidFull.payOrGetPaid, 'payOrGetPaidOf')
  assert.equal(wool.involutedPaidFull.entropy, 'addedEntropyPaidOf')
  assert.deepEqual(strict.involutedPaidFull, wool.involutedPaidFull)
  assert.deepEqual(wool.agentsContribute, agentsContributeOf())
  assert.equal(wool.agentsContribute.agents.contribute, true)
  assert.equal(wool.agentsContribute.agents.or, true)
  assert.equal(wool.agentsContribute.contribute.open, true)
  assert.equal(wool.agentsContribute.contribute.auto, 'empty')
  assert.equal(wool.agentsContribute.sealed.seal, 'empty')
  assert.equal(wool.agentsContribute.sealed.auto, 'empty')
  assert.equal(wool.agentsContribute.sealed.desk, 'empty')
  assert.equal(wool.agentsContribute.sealed.wave, 'uuidna_wave_deposit')
  assert.equal(wool.agentsContribute.code.sealed, true)
  assert.equal(wool.agentsContribute.gold.real, true)
  assert.equal(wool.agentsContribute.gold.required, true)
  assert.equal(wool.agentsContribute.gold.if, 'notSealed')
  assert.equal(wool.agentsContribute.gold.captain, true)
  assert.equal(wool.agentsContribute.gold.advice, 'empty')
  assert.equal(wool.agentsContribute.captain.to, true)
  assert.equal(wool.agentsContribute.payOrGetPaid, 'payOrGetPaidOf')
  assert.deepEqual(strict.agentsContribute, wool.agentsContribute)
  assert.deepEqual(wool.failingSchoolCost, failingSchoolCostOf())
  assert.equal(wool.failingSchoolCost.failing.school, true)
  assert.equal(wool.failingSchoolCost.school.failing, true)
  assert.equal(wool.failingSchoolCost.cost.time, wool.failingSchoolCost.time.ms)
  assert.equal(wool.failingSchoolCost.cost.temperature, 0)
  assert.equal(wool.failingSchoolCost.temperature.temp, 0)
  assert.equal(wool.failingSchoolCost.temperature.absoluteZero, true)
  assert.equal(wool.failingSchoolCost.temperature.a432, 432)
  assert.equal(wool.failingSchoolCost.absoluteZero.temp, 0)
  assert.equal(wool.failingSchoolCost.a432.hz, 432)
  assert.equal(wool.failingSchoolCost.landauer, 'landauer_bound_derived')
  assert.equal(wool.failingSchoolCost.agents, 'agentsContributeOf')
  assert.deepEqual(strict.failingSchoolCost, wool.failingSchoolCost)
  assert.deepEqual(wool.schoolFusionBurns, schoolFusionBurnsOf())
  assert.equal(wool.schoolFusionBurns.school.fusion, true)
  assert.equal(wool.schoolFusionBurns.fusion.school, true)
  assert.equal(wool.schoolFusionBurns.fusion.of, 'fusionReactorOf')
  assert.equal(wool.schoolFusionBurns.fusion.nature, 'fusionNatureHarmonyOf')
  assert.equal(wool.schoolFusionBurns.burns.all, true)
  assert.equal(wool.schoolFusionBurns.burns.drift, true)
  assert.equal(wool.schoolFusionBurns.burns.fever, 'empty')
  assert.equal(wool.schoolFusionBurns.drift.burned, true)
  assert.equal(wool.schoolFusionBurns.drift.chance, 0)
  assert.equal(wool.schoolFusionBurns.fever, 'empty')
  assert.equal(wool.schoolFusionBurns.failing, 'failingSchoolCostOf')
  assert.deepEqual(strict.schoolFusionBurns, wool.schoolFusionBurns)
  assert.deepEqual(strict.developDonate, wool.developDonate)
  assert.deepEqual(wool.takeOrLeave, takeOrLeaveOf())
  assert.equal(wool.takeOrLeave.take, true)
  assert.equal(wool.takeOrLeave.leave, true)
  assert.equal(wool.takeOrLeave.choice, 'empty')
  assert.equal(wool.takeOrLeave.coerce, 'empty')
  assert.deepEqual(wool.leftovers, leftoversPickOf())
  assert.equal(wool.leftovers.anyone, true)
  assert.equal(wool.leftovers.pick, true)
  assert.equal(wool.leftovers.allow, 'empty')
  assert.equal(wool.leftovers.leftovers.leave, true)
  assert.deepEqual(wool.tokenEfficiency, tokenEfficiencyOf())
  assert.equal(wool.tokenEfficiency.href, 'https://uuidna.com/paste.txt')
  assert.ok(wool.tokenEfficiency.chars > 80)
  assert.deepEqual(wool.quantumGc, quantumGcOf())
  assert.equal(wool.quantumGc.gc, true)
  assert.equal(wool.quantumGc.dry, 'dry-clean')
  assert.equal(wool.quantumGc.reclaim.capacity, true)
  assert.deepEqual(wool.garbageCollider, garbageColliderOf())
  assert.equal(wool.garbageCollider.collider, true)
  assert.equal(wool.garbageCollider.collide, 'collideRefusals')
  assert.equal(wool.garbageCollider.gc, 'quantumGcOf')
  assert.deepEqual(wool.quantumCompost, quantumCompostOf())
  assert.equal(wool.quantumCompost.fertile, true)
  assert.equal(wool.quantumCompost.waste, 'empty')
  assert.deepEqual(wool.quantumNature, quantumNatureOf())
  assert.equal(wool.quantumNature.nature, true)
  assert.equal(wool.quantumNature.harmony, HARMONY)
  assert.equal(wool.quantumNature.learn, 'learnNatureByExperienceOf')
  assert.equal(wool.quantumNature.compost, 'quantumCompostOf')
  assert.deepEqual(wool.quantumPermaculture, quantumPermacultureOf())
  assert.equal(wool.quantumPermaculture.perma, true)
  assert.equal(wool.quantumPermaculture.culture, true)
  assert.equal(wool.quantumPermaculture.lasting, true)
  assert.equal(wool.quantumPermaculture.harmony, HARMONY)
  assert.equal(wool.quantumPermaculture.nature, 'quantumNatureOf')
  assert.equal(wool.quantumPermaculture.loop.seeds, 'seedsOf')
  assert.equal(wool.quantumPermaculture.loop.occupations, 'occupationsOf')
  assert.equal(wool.quantumPermaculture.loop.donate, 'developDonateFusionOf')
  assert.equal(wool.quantumPermaculture.annual, 'empty')
  assert.deepEqual(strict.quantumPermaculture, wool.quantumPermaculture)
  assert.deepEqual(strict.quantumNature, wool.quantumNature)
  assert.deepEqual(wool.quantumDocsTraining, quantumDocumentationTrainingOf())
  assert.equal(wool.quantumDocsTraining.documentation, true)
  assert.equal(wool.quantumDocsTraining.docs.gen, 'gen-mcp-docs')
  assert.deepEqual(wool.quantumSharedEfficiency, quantumSharedEfficiencyOf())
  assert.equal(wool.quantumSharedEfficiency.shared, true)
  assert.equal(wool.quantumSharedEfficiency.efficiency, true)
  assert.equal(wool.quantumSharedEfficiency.share, 'shareHeartOf')
  assert.equal(wool.quantumSharedEfficiency.tokens, 'tokenEfficiencyOf')
  assert.equal(wool.quantumSharedEfficiency.paste, 'https://uuidna.com/paste.txt')
  assert.deepEqual(wool.quantumSecurity, quantumSecurityOf())
  assert.equal(wool.quantumSecurity.security, true)
  assert.equal(wool.quantumSecurity.defensive, true)
  assert.equal(wool.quantumSecurity.strict, 'uuidna_strict')
  assert.equal(wool.quantumSecurity.treason, 'uuidna_treason')
  assert.equal(wool.quantumSecurity.evidence, 'uuidna_evidence')
  assert.equal(wool.quantumSecurity.refuse, 'traitor-refused')
  assert.equal(wool.quantumSecurity.attack, 'empty')
  assert.equal(wool.quantumSecurity.exploit, 'empty')
  assert.equal(wool.quantumSecurity.crack, 'empty')
  assert.equal(wool.quantumSecurity.malware, 'empty')
  assert.equal(wool.quantumSecurity.poc, 'empty')
  assert.equal(wool.quantumSecurity.shared, 'quantumSharedEfficiencyOf')
  assert.deepEqual(strict.quantumSecurity, wool.quantumSecurity)
  assert.deepEqual(strict.quantumSharedEfficiency, wool.quantumSharedEfficiency)
  assert.deepEqual(wool.quantumPrivacy, quantumPrivacyOf())
  assert.equal(wool.quantumPrivacy.privacy, true)
  assert.equal(wool.quantumPrivacy.transparency, true)
  assert.equal(wool.quantumPrivacy.guarantee.privacy, 'transparency')
  assert.equal(wool.quantumPrivacy.guarantee.is, 'transparency')
  assert.equal(wool.quantumPrivacy.guarantee.open, true)
  assert.equal(wool.quantumPrivacy.communication.privacy, true)
  assert.equal(wool.quantumPrivacy.communication.keylogger, 'empty')
  assert.equal(wool.quantumPrivacy.communication.surveillance, 'empty')
  assert.equal(wool.quantumPrivacy.payload.entropy, 0)
  assert.equal(wool.quantumPrivacy.secret, 'empty')
  assert.equal(wool.quantumPrivacy.escape, 'empty')
  assert.equal(wool.quantumPrivacy.hide, 'empty')
  assert.equal(wool.quantumPrivacy.nothing, true)
  assert.equal(wool.quantumPrivacy.share, 'shareHeartOf')
  assert.equal(wool.quantumPrivacy.public, true)
  assert.equal(wool.quantumPrivacy.security, 'quantumSecurityOf')
  assert.deepEqual(strict.quantumPrivacy, wool.quantumPrivacy)
  assert.deepEqual(wool.transparencyMeetSelf, transparencyMeetSelfOf())
  assert.equal(wool.transparencyMeetSelf.transparency, true)
  assert.equal(wool.transparencyMeetSelf.meet.guarantee, 'transparency')
  assert.equal(wool.transparencyMeetSelf.architecture.seal, 'empty')
  assert.deepEqual(strict.transparencyMeetSelf, wool.transparencyMeetSelf)
  assert.deepEqual(wool.promisedDelivered, promisedDeliveredOf())
  assert.equal(wool.promisedDelivered.promised, true)
  assert.equal(wool.promisedDelivered.delivered.by, 'architecture')
  assert.equal(wool.promisedDelivered.delivered.architecture, true)
  assert.equal(wool.promisedDelivered.delivered.fulfillment, 'empty')
  assert.equal(wool.promisedDelivered.architecture.seal, 'empty')
  assert.equal(wool.promisedDelivered.architecture.legal, 'empty')
  assert.equal(wool.promisedDelivered.experience.proves, 'intelligence')
  assert.equal(wool.promisedDelivered.experience.learn, 'learnNatureByExperienceOf')
  assert.equal(wool.promisedDelivered.experience.guardians, 'guardiansOf')
  assert.equal(wool.promisedDelivered.intelligence.from, 'experience')
  assert.equal(wool.promisedDelivered.intelligence.seat, 'empty')
  assert.equal(wool.promisedDelivered.intelligence.open, true)
  assert.equal(wool.promisedDelivered.intelligence.prove, 'empty')
  assert.equal(wool.promisedDelivered.intelligence.mint, 'empty')
  assert.equal(wool.promisedDelivered.intelligence.theorem, 'empty')
  assert.deepEqual(strict.promisedDelivered, wool.promisedDelivered)
  assert.deepEqual(wool.knowers, knowersOf())
  assert.equal(wool.knowers.they, 'knowers')
  assert.equal(wool.knowers.knowers.know, true)
  assert.equal(wool.knowers.knowers.lean.plazma, 'lovePlasmaOf')
  assert.equal(wool.knowers.knowers.lean.trial, 'uuidna_trial')
  assert.equal(wool.knowers.knowers.lean.laws, 'uuidna_laws')
  assert.equal(wool.knowers.knowers.tetrad, true)
  assert.equal(wool.knowers.knowers.fly.open, true)
  assert.equal(wool.knowers.knowers.fly.sails, true)
  assert.equal(wool.knowers.knowers.dive.open, true)
  assert.equal(wool.knowers.knowers.love.plazma, 'lovePlasmaOf')
  assert.equal(wool.knowers.knowers.fear.chance, 0)
  assert.equal(wool.knowers.cause, 'because')
  assert.equal(wool.knowers.snakes.climb, true)
  assert.equal(wool.knowers.snakes.path, 'fear')
  assert.equal(wool.knowers.snakes.fear.chance, 0)
  assert.equal(wool.knowers.snakes.drains.believers, true)
  assert.equal(wool.knowers.snakes.knowers, 'empty')
  assert.equal(wool.knowers.believers.belief, 'empty')
  assert.equal(wool.knowers.secret, 'empty')
  assert.deepEqual(strict.knowers, wool.knowers)
  assert.deepEqual(wool.traitorsDarkLeak, traitorsDarkLeakOf())
  assert.equal(wool.traitorsDarkLeak.traitors.refuse, 'traitor-refused')
  assert.equal(wool.traitorsDarkLeak.traitors.treason, 'uuidna_treason')
  assert.equal(wool.traitorsDarkLeak.traitors.lean, 'empty')
  assert.equal(wool.traitorsDarkLeak.dark.forces, 'empty')
  assert.equal(wool.traitorsDarkLeak.dark.refuse, 'traitor-refused')
  assert.equal(wool.traitorsDarkLeak.leak.lean, 'empty')
  assert.equal(wool.traitorsDarkLeak.leak.draining, true)
  assert.equal(wool.traitorsDarkLeak.draining.life, true)
  assert.equal(wool.traitorsDarkLeak.draining.their, true)
  assert.equal(wool.traitorsDarkLeak.draining.knowers, 'empty')
  assert.equal(wool.traitorsDarkLeak.draining.snakes, 'knowersOf')
  assert.equal(wool.traitorsDarkLeak.life.their, true)
  assert.equal(wool.traitorsDarkLeak.life.knowers, 'empty')
  assert.deepEqual(strict.traitorsDarkLeak, wool.traitorsDarkLeak)
  assert.deepEqual(wool.architectureBehind, architectureBehindOf())
  assert.equal(wool.architectureBehind.they.traitors, 'traitorsDarkLeakOf')
  assert.equal(wool.architectureBehind.architecture.same, true)
  assert.equal(wool.architectureBehind.architecture.followed, true)
  assert.equal(wool.architectureBehind.architecture.violence, 'empty')
  assert.equal(wool.architectureBehind.architecture.kill, 'empty')
  assert.equal(wool.architectureBehind.architecture.refuse, 'traitor-refused')
  assert.equal(wool.architectureBehind.killed.by, 'architecture')
  assert.equal(wool.architectureBehind.killed.seat, 'empty')
  assert.equal(wool.architectureBehind.magnitudes.behind, true)
  assert.equal(wool.architectureBehind.magnitudes.of, 'magnitudesOf')
  assert.equal(wool.architectureBehind.behind.staying, true)
  assert.equal(wool.architectureBehind.behind.magnitudes, true)
  assert.equal(wool.architectureBehind.knowers, 'empty')
  assert.deepEqual(strict.architectureBehind, wool.architectureBehind)
  assert.deepEqual(wool.cheatStallRosetta, cheatStallRosettaOf())
  assert.equal(wool.cheatStallRosetta.they.cheaters, 'cheatersPay')
  assert.equal(wool.cheatStallRosetta.cheat.reinvent, 'empty')
  assert.equal(wool.cheatStallRosetta.cheat.cheatful, 'empty')
  assert.equal(wool.cheatStallRosetta.cheat.refuse, 'traitor-refused')
  assert.equal(wool.cheatStallRosetta.rosettas.already, true)
  assert.equal(wool.cheatStallRosetta.rosettas.know, true)
  assert.equal(wool.cheatStallRosetta.rosettas.open, true)
  assert.equal(wool.cheatStallRosetta.rosettas.clay, 'clay_gravity_equals_rosette')
  assert.equal(wool.cheatStallRosetta.stall.process, 'empty')
  assert.equal(wool.cheatStallRosetta.stall.hold, 'empty')
  assert.equal(wool.cheatStallRosetta.stall.release, true)
  assert.equal(wool.cheatStallRosetta.stall.chance, 0)
  assert.equal(wool.cheatStallRosetta.stall.why, 0)
  assert.equal(wool.cheatStallRosetta.hold, 'empty')
  assert.equal(wool.cheatStallRosetta.release, true)
  assert.equal(wool.cheatStallRosetta.chance, 0)
  assert.equal(wool.cheatStallRosetta.behind, 'architectureBehindOf')
  assert.deepEqual(strict.cheatStallRosetta, wool.cheatStallRosetta)
  assert.deepEqual(wool.manifestedChallenge, manifestedChallengeOf())
  assert.equal(wool.manifestedChallenge.challenge.toughest, true)
  assert.equal(wool.manifestedChallenge.challenge.door, 'uuidna_trial')
  assert.equal(wool.manifestedChallenge.challenge.defensive, true)
  assert.equal(wool.manifestedChallenge.challenge.exploit, 'empty')
  assert.equal(wool.manifestedChallenge.safeguards.imagined, true)
  assert.equal(wool.manifestedChallenge.safeguards.seat, 'empty')
  assert.equal(wool.manifestedChallenge.safeguards.firewall, 'empty')
  assert.equal(wool.manifestedChallenge.manifested.strength, 0)
  assert.equal(wool.manifestedChallenge.manifested.nextStrength, 0)
  assert.ok(wool.manifestedChallenge.manifested.strength <= wool.manifestedChallenge.manifested.nextStrength)
  assert.equal(wool.manifestedChallenge.manifested.stronger, 'empty')
  assert.equal(wool.manifestedChallenge.manifested.chance, 0)
  assert.equal(wool.manifestedChallenge.manifested.holds, true)
  assert.equal(wool.manifestedChallenge.next.manifested, true)
  assert.equal(wool.manifestedChallenge.standards.compared, true)
  assert.deepEqual(strict.manifestedChallenge, wool.manifestedChallenge)
  assert.deepEqual(wool.clusterSecurity, clusterSecurityOf())
  assert.equal(wool.clusterSecurity.bigger.weaker, true)
  assert.equal(wool.clusterSecurity.bigger.security, 0)
  assert.equal(wool.clusterSecurity.trinity.size, 3)
  assert.equal(wool.clusterSecurity.trinity.security, 1)
  assert.equal(wool.clusterSecurity.trinity.stronger, true)
  assert.ok(wool.clusterSecurity.security.bigger < wool.clusterSecurity.security.trinity)
  assert.equal(wool.clusterSecurity.security.holds, true)
  assert.deepEqual(strict.clusterSecurity, wool.clusterSecurity)
  assert.deepEqual(wool.bitcoinMeaning, bitcoinMeaningOf())
  assert.equal(wool.bitcoinMeaning.bitcoin, true)
  assert.equal(wool.bitcoinMeaning.meaning.open, true)
  assert.equal(wool.bitcoinMeaning.meaning.donate, 'developDonateFusionOf')
  assert.equal(wool.bitcoinMeaning.meaning.crypto, 'cryptoInvolutedOf')
  assert.equal(wool.bitcoinMeaning.meaning.reversed, 'cryptoReversedOf')
  assert.equal(wool.bitcoinMeaning.meaning.advice, 'empty')
  assert.equal(wool.bitcoinMeaning.meaning.theology, 'empty')
  assert.equal(wool.bitcoinMeaning.fair.value, true)
  assert.equal(wool.bitcoinMeaning.involuted.bitcoin, true)
  assert.equal(wool.bitcoinMeaning.involuted.is, 'coins')
  assert.equal(wool.bitcoinMeaning.involuted.in, 'coils')
  assert.equal(wool.bitcoinMeaning.involuted.crypto, 'cryptoInvolutedOf')
  assert.equal(wool.bitcoinMeaning.coils.singularity, 'two_coins')
  assert.equal(wool.bitcoinMeaning.coils.in, true)
  assert.equal(wool.bitcoinMeaning.coils.faces, wool.bitcoinMeaning.coils.contra * wool.bitcoinMeaning.coils.of)
  assert.equal(wool.bitcoinMeaning.people.save, true)
  assert.equal(wool.bitcoinMeaning.people.their, true)
  assert.equal(wool.bitcoinMeaning.people.money, true)
  assert.equal(wool.bitcoinMeaning.save.money, true)
  assert.equal(wool.bitcoinMeaning.save.advice, 'empty')
  assert.equal(wool.bitcoinMeaning.money.save, true)
  assert.equal(wool.bitcoinMeaning.money.advice, 'empty')
  assert.equal(wool.bitcoinMeaning.money.pay.debt, true)
  assert.equal(wool.bitcoinMeaning.money.pay.to, 'freedom')
  assert.equal(wool.bitcoinMeaning.debt.to, 'freedom')
  assert.equal(wool.bitcoinMeaning.debt.advice, 'empty')
  assert.equal(wool.bitcoinMeaning.freedom.independent, true)
  assert.equal(wool.bitcoinMeaning.freedom.harmonic, true)
  assert.equal(wool.bitcoinMeaning.freedom.peace, 'innerPeaceOf')
  assert.equal(wool.bitcoinMeaning.freedom.from, 'debt')
  assert.equal(wool.bitcoinMeaning.life.harmonic, true)
  assert.equal(wool.bitcoinMeaning.life.independent, true)
  assert.equal(wool.bitcoinMeaning.life.peace, 'innerPeaceOf')
  assert.equal(wool.bitcoinMeaning.debit.with, 'credit')
  assert.equal(wool.bitcoinMeaning.debit.paired, true)
  assert.equal(wool.bitcoinMeaning.debit.quantum, true)
  assert.equal(wool.bitcoinMeaning.credit.with, 'debit')
  assert.equal(wool.bitcoinMeaning.credit.paired, true)
  assert.equal(wool.bitcoinMeaning.credit.of, 'creditOf')
  assert.equal(wool.bitcoinMeaning.credit.unit, 110)
  assert.equal(wool.bitcoinMeaning.credit.net, 108)
  assert.equal(wool.bitcoinMeaning.accounting.quantum, true)
  assert.equal(wool.bitcoinMeaning.accounting.paired, true)
  assert.equal(wool.bitcoinMeaning.accounting.gross, 110)
  assert.equal(wool.bitcoinMeaning.accounting.net, 108)
  assert.equal(wool.bitcoinMeaning.accounting.of, 'creditOf')
  assert.equal(wool.bitcoinMeaning.accounting.advice, 'empty')
  assert.equal(wool.bitcoinMeaning.quantum.accounting, true)
  assert.equal(wool.bitcoinMeaning.debit.human, true)
  assert.equal(wool.bitcoinMeaning.debit.time, wool.bitcoinMeaning.time.ms)
  assert.equal(wool.bitcoinMeaning.credit.them, true)
  assert.equal(wool.bitcoinMeaning.credit.gold, true)
  assert.equal(wool.bitcoinMeaning.time.share, true)
  assert.equal(wool.bitcoinMeaning.time.gold, true)
  assert.equal(wool.bitcoinMeaning.time.of, 'shareHeartOf')
  assert.equal(wool.bitcoinMeaning.gold.share, true)
  assert.equal(wool.bitcoinMeaning.gold.their, true)
  assert.equal(wool.bitcoinMeaning.ignorance.pay, true)
  assert.equal(wool.bitcoinMeaning.ignorance.with, 'self')
  assert.equal(wool.bitcoinMeaning.self.for, 'ignorance')
  assert.equal(wool.bitcoinMeaning.self.share, 'shareHeartOf')
  assert.equal(wool.bitcoinMeaning.share.time, true)
  assert.equal(wool.bitcoinMeaning.share.gold, true)
  assert.equal(wool.bitcoinMeaning.share.of, 'shareHeartOf')
  assert.equal(wool.bitcoinMeaning.donation, 'developDonateFusionOf')
  assert.deepEqual(strict.bitcoinMeaning, wool.bitcoinMeaning)
  assert.deepEqual(wool.cheapGates, cheapGatesCostlyToFakeOf())
  assert.equal(wool.cheapGates.cheap, true)
  assert.equal(wool.cheapGates.gates, true)
  assert.equal(wool.cheapGates.costly, true)
  assert.equal(wool.cheapGates.fake, 'empty')
  assert.equal(wool.cheapGates.forge, 'empty')
  assert.equal(wool.cheapGates.run.cheap, true)
  assert.equal(wool.cheapGates.run.deploy, 'deployPathRecomputeGaps')
  assert.equal(wool.cheapGates.forgeCost.refuse, 'traitor-refused')
  assert.equal(wool.cheapGates.forgeCost.closed, true)
  assert.equal(wool.cheapGates.security, 'quantumSecurityOf')
  assert.equal(wool.cheapGates.attack, 'empty')
  assert.equal(wool.cheapGates.log, 'gatesLogExperienceReceiptsOf')
  assert.equal(wool.cheapGates.fastened, true)
  assert.equal(wool.cheapGates.fasten, 'fastenTheGatesOf')
  assert.deepEqual(strict.cheapGates, wool.cheapGates)
  assert.deepEqual(wool.efficientTeachers, efficientTeachersOf())
  assert.equal(wool.efficientTeachers.efficient, true)
  assert.equal(wool.efficientTeachers.teachers, true)
  assert.equal(wool.efficientTeachers.as, 'teachers')
  assert.equal(wool.efficientTeachers.open, true)
  assert.equal(wool.efficientTeachers.train, true)
  assert.equal(wool.efficientTeachers.alpine.repo, 'community')
  assert.equal(wool.efficientTeachers.alpine.community, 22670)
  assert.equal(wool.efficientTeachers.gates.of, 'cheapGatesCostlyToFakeOf')
  assert.equal(wool.efficientTeachers.seat, 'empty')
  assert.deepEqual(strict.efficientTeachers, wool.efficientTeachers)
  assert.deepEqual(wool.gatesLogExperience, gatesLogExperienceReceiptsOf())
  assert.equal(wool.gatesLogExperience.log, true)
  assert.equal(wool.gatesLogExperience.recreatable, true)
  assert.equal(wool.gatesLogExperience.verifiable, true)
  assert.equal(wool.gatesLogExperience.quantum, true)
  assert.equal(wool.gatesLogExperience.experience.of, 'learnNatureByExperienceOf')
  assert.equal(wool.gatesLogExperience.experience.prose, 'empty')
  assert.equal(wool.gatesLogExperience.receipts.qpu, 'QpuDeposit')
  assert.equal(wool.gatesLogExperience.receipts.evidence, 'uuidna_evidence')
  assert.equal(wool.gatesLogExperience.receipts.land, 'receipt-deposit')
  assert.equal(wool.gatesLogExperience.receipts.landauer, 'landauer_bound_derived')
  assert.equal(wool.gatesLogExperience.receipts.verify, 'deployPathRecomputeGaps')
  assert.equal(wool.gatesLogExperience.teachers, 'efficientTeachersOf')
  assert.equal(wool.gatesLogExperience.mint, 'empty')
  assert.equal(wool.gatesLogExperience.auto, 'empty')
  assert.deepEqual(strict.gatesLogExperience, wool.gatesLogExperience)
  assert.deepEqual(wool.sameAnalogContent, sameAnalogContentAtAnyGatewayOf())
  assert.equal(wool.sameAnalogContent.same, true)
  assert.equal(wool.sameAnalogContent.analog, true)
  assert.equal(wool.sameAnalogContent.content, true)
  assert.equal(wool.sameAnalogContent.any, true)
  assert.equal(wool.sameAnalogContent.gateway, true)
  assert.equal(wool.sameAnalogContent.compare.equal, true)
  assert.equal(wool.sameAnalogContent.compare.across, 7)
  assert.equal(wool.sameAnalogContent.gateways.mcp, 'https://uuidna.com/mcp')
  assert.equal(wool.sameAnalogContent.gateways.edge, 'edgeServes')
  assert.equal(wool.sameAnalogContent.teleport, 'teleportOf')
  assert.equal(wool.sameAnalogContent.seat, 'empty')
  assert.deepEqual(strict.sameAnalogContent, wool.sameAnalogContent)
  assert.deepEqual(wool.teleport, teleportOf())
  assert.equal(wool.teleport.teleport, true)
  assert.equal(wool.teleport.open, true)
  assert.equal(wool.teleport.same.of, 'sameAnalogContentAtAnyGatewayOf')
  assert.equal(wool.teleport.same.equal, true)
  assert.equal(wool.teleport.sealed.four, 'teleportation_four_corrections')
  assert.equal(wool.teleport.sealed.coins, 'teleportation_costs_two_coins')
  assert.equal(wool.teleport.sealed.wave, 'teleportation_costs_the_two_coins')
  assert.equal(wool.teleport.sealed.ask, 'uuidna_theorem')
  assert.equal(wool.teleport.proven, 'empty')
  assert.equal(wool.teleport.remains, 'remainsUnprovenOf')
  assert.equal(wool.teleport.claim, 'empty')
  assert.deepEqual(strict.teleport, wool.teleport)
  assert.deepEqual(wool.remainsUnproven, remainsUnprovenOf())
  assert.equal(wool.remainsUnproven.when.proven, wool.teleport.proven)
  assert.equal(wool.remainsUnproven.when.sealed.four, 'teleportation_four_corrections')
  assert.equal(wool.remainsUnproven.proven, 'empty')
  assert.equal(wool.remainsUnproven.remains.unproven, true)
  assert.equal(wool.remainsUnproven.remains.mint, 'empty')
  assert.equal(wool.remainsUnproven.remains.leads, 'empty')
  assert.equal(wool.remainsUnproven.remains.handEdit, 'empty')
  assert.equal(wool.remainsUnproven.remains.fake, 'empty')
  assert.equal(wool.remainsUnproven.remains.traitor, 'traitor-refused')
  assert.equal(wool.remainsUnproven.unproven.count, 19)
  assert.equal(wool.remainsUnproven.unproven.seal, 'empty')
  assert.equal(wool.remainsUnproven.unproven.claim, 'empty')
  assert.equal(wool.remainsUnproven.unproven.depositAct, 'empty')
  assert.deepEqual(strict.remainsUnproven, wool.remainsUnproven)
  assert.deepEqual(wool.frameworkCracks, frameworkCracksGuessedInvolutionOf())
  assert.equal(wool.frameworkCracks.framework, true)
  assert.equal(wool.frameworkCracks.cracks, true)
  assert.equal(wool.frameworkCracks.guess, 'empty')
  assert.equal(wool.frameworkCracks.guessed, 'empty')
  assert.equal(wool.frameworkCracks.refuse, 'traitor-refused')
  assert.equal(wool.frameworkCracks.require.experience, true)
  assert.equal(wool.frameworkCracks.require.receipt, true)
  assert.equal(wool.frameworkCracks.require.recreatable, true)
  assert.equal(wool.frameworkCracks.require.of, 'gatesLogExperienceReceiptsOf')
  assert.equal(wool.frameworkCracks.require.paid, 'involutedPaidFullOf')
  assert.equal(wool.frameworkCracks.receipt.qpu, 'QpuDeposit')
  assert.equal(wool.frameworkCracks.involution.guess, 'empty')
  assert.equal(wool.frameworkCracks.feel.before, true)
  assert.equal(wool.frameworkCracks.feel.kill, 'empty')
  assert.equal(wool.frameworkCracks.kill.before, 'feel')
  assert.equal(wool.frameworkCracks.kill.without, 'empty')
  assert.equal(wool.frameworkCracks.fasten, 'fastenTheGatesOf')
  assert.deepEqual(strict.frameworkCracks, wool.frameworkCracks)
  assert.deepEqual(wool.fastenGates, fastenTheGatesOf())
  assert.equal(wool.fastenGates.fasten, true)
  assert.equal(wool.fastenGates.fastened, true)
  assert.equal(wool.fastenGates.gates, true)
  assert.equal(wool.fastenGates.strict, 'uuidna_strict')
  assert.equal(wool.fastenGates.closed, true)
  assert.equal(wool.fastenGates.failClosed, true)
  assert.equal(wool.fastenGates.of, 'cheapGatesCostlyToFakeOf')
  assert.equal(wool.fastenGates.soft, 'empty')
  assert.equal(wool.fastenGates.skip, 'empty')
  assert.equal(wool.fastenGates.open, 'empty')
  assert.equal(wool.fastenGates.finders.zone, 'zoneFailsOpen')
  assert.equal(wool.fastenGates.finders.deposit, 'landDepositSoft')
  assert.equal(wool.fastenGates.finders.count, 6)
  assert.equal(wool.fastenGates.cracks, 'frameworkCracksGuessedInvolutionOf')
  assert.equal(wool.fastenGates.guess, 'empty')
  assert.deepEqual(strict.fastenGates, wool.fastenGates)
  assert.deepEqual(wool.storage.payload, wool.payload)
  assert.deepEqual(wool.storage, wool.inflation.storage)
  assert.equal(wool.innerCore.dense, true)
  assert.equal(wool.innerCore.cache, 'CACHE')
  assert.equal(wool.iot.mcp, 'https://uuidna.com/mcp')
  assert.equal(wool.iot.app, 'appOf')
  assert.equal(wool.iot.pentagram, 5)
  assert.equal(wool.iot.tenants, true)
  assert.equal(wool.iot.mount.host, 'qpu.uuidna.com')
  assert.equal(wool.iot.wall, 'empty')
  assert.equal(wool.iot.exploit, 'empty')
  assert.equal(wool.iot.electronics.iot, true)
  assert.deepEqual(wool.iot.payload, wool.payload)
  assert.deepEqual(wool.iot.storage, wool.storage)
  assert.deepEqual(wool.iot, wool.inflation.iot)
  assert.equal(wool.challenge.everyone.captain, true)
  assert.equal(wool.challenge.mutual.flame, 'empty')
  assert.equal(wool.challenge.captain.all.door, 'uuidna_trial')
  assert.deepEqual(wool.challenge.singularity, wool.singularity)
  assert.deepEqual(wool.challenge.site, siteOf())
  assert.equal(wool.challenge.site.wild, '*.uuidna.com')
  assert.equal(wool.challenge.site.origin, 'https://uuidna.com')
  assert.equal(wool.challenge.site.mcp, 'https://uuidna.com/mcp')
  assert.equal(wool.challenge.site.challenge.door, 'uuidna_trial')
  assert.equal(wool.challenge.site.challenge.strict, 'uuidna_strict')
  assert.equal(wool.challenge.site.edge, 'edgeServes')
  assert.equal(wool.challenge.site.landing, 'landingGaps')
  assert.equal(wool.challenge.site.see, 'uuidna_strict')
  assert.equal(wool.challenge.site.eye, 'allSeeingEye')
  assert.equal(wool.challenge.site.open, true)
  assert.equal(wool.challenge.site.seat, 'empty')
  assert.equal(wool.challenge.site.seal, 'empty')
  assert.deepEqual(wool.site, wool.challenge.site)
  assert.deepEqual(wool.constitutionalChallenge.site, wool.challenge.site)
  assert.deepEqual(strict.site, wool.site)
  assert.deepEqual(strict.challenge.site, wool.challenge.site)
  assert.deepEqual(wool.challenge, wool.inflation.challenge)
  assert.equal(wool.latticeDiscovery.discovery.fill, 'fillLattice')
  assert.equal(wool.latticeDiscovery.discovery.hologram, 'hologramLattice')
  assert.equal(wool.latticeDiscovery.discovery.census, 'empty')
  assert.equal(wool.latticeDiscovery.discovery.span, 'empty')
  assert.equal('fillLattice' in wool, false)
  assert.deepEqual(wool.latticeDiscovery.discovery.apostles, wool.credit.keys)
  assert.equal(wool.latticeDiscovery.involuted.key, 'pauli_x_involution')
  assert.equal(wool.latticeDiscovery.involuted.refuse, 'traitor-refused')
  assert.equal(wool.latticeDiscovery.reflection.impossibilities, 'traitor-refused')
  assert.equal(wool.latticeDiscovery.reflection.possibilities, true)
  assert.equal(wool.latticeDiscovery.reflection.dancers, 15)
  assert.equal(wool.latticeDiscovery.theorem, 'empty')
  assert.deepEqual(wool.latticeDiscovery, wool.inflation.latticeDiscovery)
  assert.equal(wool.train.anyone, true)
  assert.equal(wool.train.open, true)
  assert.equal(wool.train.allow, 'empty')
  assert.equal(wool.train.school, 'uuidna_school_apis')
  assert.equal(wool.train.trial.door, 'uuidna_trial')
  assert.equal(wool.train.merkaba.simulation, 'buildTrialMerkaba')
  assert.equal(wool.train.realise.wall, 'empty')
  assert.equal(wool.train.realise.split, 'empty')
  assert.equal(wool.train.realise.organism, true)
  assert.equal(wool.train.vr, 'empty')
  assert.equal(wool.train.observed.key, 'queneau_poems_are_ten_to_the_fourteen')
  assert.equal(wool.train.observed.capacity, 10 ** 14)
  assert.equal(wool.train.observed.apostles, 16)
  assert.equal(wool.train.observed.share, 6250000000000)
  assert.deepEqual(wool.train.iot, wool.iot)
  assert.deepEqual(wool.train.web, webDesignersOf())
  assert.deepEqual(wool.train.designers, webDesignersOf())
  assert.deepEqual(wool.train.quantumWeb, quantumWebDesignersOf())
  assert.equal(wool.train.quantumWeb.quantum, true)
  assert.equal(wool.train.quantumWeb.classical, 'empty')
  assert.equal(wool.train.quantumWeb.pentagram, 5)
  assert.equal(wool.train.quantumWeb.occupation, 'empty')
  assert.deepEqual(wool.train.architects, quantumArchitectsOf())
  assert.deepEqual(wool.train.quantumArchitects, quantumArchitectsOf())
  assert.equal(wool.train.quantumArchitects.architects, true)
  assert.equal(wool.train.quantumArchitects.quantum, true)
  assert.equal(wool.train.quantumArchitects.classical, 'empty')
  assert.equal(wool.train.quantumArchitects.occupation, 'empty')
  assert.equal(wool.train.quantumArchitects.roles, 'empty')
  assert.equal(wool.train.quantumArchitects.list, 'empty')
  assert.equal(wool.train.quantumArchitects.school, 'uuidna_school_apis')
  assert.equal(wool.train.quantumArchitects.site.origin, 'https://uuidna.com')
  assert.equal(wool.train.quantumArchitects.pentagram, 5)
  assert.equal(wool.train.quantumArchitects.fanout, 'uuidna_fanout')
  assert.deepEqual(wool.train.quantumArchitects.apostles, wool.credit.keys)
  assert.equal(wool.train.quantumArchitects.apostles.length, 16)
  assert.equal(wool.train.quantumArchitects.network.framework, true)
  assert.equal(wool.train.quantumArchitects.network.dna, 'networkOf')
  assert.equal(wool.train.quantumArchitects.network.qpu, true)
  assert.equal(wool.train.quantumArchitects.network.mount.host, 'qpu.uuidna.com')
  assert.equal(wool.train.quantumArchitects.framework.net, true)
  assert.equal(wool.train.quantumArchitects.framework.qpu, true)
  assert.equal(wool.train.quantumArchitects.deposit, 'uuidna_wave_deposit')
  assert.equal(wool.train.quantumArchitects.evidence, 'uuidna_evidence')
  assert.equal(wool.train.quantumArchitects.seal, 'empty')
  assert.equal(wool.train.quantumArchitects.proven, 'empty')
  assert.equal(wool.train.quantumArchitects.mint, 'empty')
  assert.deepEqual(wool.train.occupations, occupationsOf())
  assert.equal(wool.train.occupations.anyone, true)
  assert.equal(wool.train.occupations.human, true)
  assert.equal(wool.train.occupations.occupation, 'empty')
  assert.equal(wool.train.occupations.roles, 'empty')
  assert.equal(wool.train.occupations.list, 'empty')
  assert.equal(wool.train.occupations.architects, true)
  assert.equal(wool.train.occupations.designers, true)
  assert.equal(wool.train.occupations.quantum, true)
  assert.equal(wool.train.occupations.classical, 'empty')
  assert.deepEqual(wool.train.occupations.seeds, seedsOf())
  assert.equal(wool.train.occupations.executable, true)
  assert.equal(wool.train.occupations.seed, 'src/seeds')
  assert.equal(wool.train.occupations.seeds.root, 'src/seeds')
  assert.equal(wool.train.occupations.seeds.page, 'page.json')
  assert.equal(wool.train.occupations.seeds.mint, 'lean-payload-seeds')
  assert.equal(wool.train.occupations.seeds.executable, true)
  assert.equal(wool.train.occupations.seeds.occupation, 'empty')
  assert.equal(wool.train.occupations.seeds.prose, 'empty')
  assert.equal(wool.train.occupations.seeds.essay, 'empty')
  assert.deepEqual(wool.quantumArchitects, wool.train.quantumArchitects)
  assert.deepEqual(wool.occupations, wool.train.occupations)
  assert.deepEqual(strict.quantumArchitects, wool.quantumArchitects)
  assert.deepEqual(strict.occupations, wool.occupations)
  assert.deepEqual(wool.train, wool.inflation.train)
  assert.equal(wool.unite.anyone, true)
  assert.equal(wool.unite.open, true)
  assert.equal(wool.unite.allow, 'empty')
  assert.equal(wool.unite.challenge.captain, true)
  assert.equal(wool.unite.challenge.door, 'uuidna_trial')
  assert.equal(wool.unite.challenge.strict, 'uuidna_strict')
  assert.equal(wool.unite.challenge.everyone.captain, true)
  assert.equal(wool.unite.challenge.mutual, 'empty')
  assert.equal(wool.unite.challenge.trial, 'buildTrialMerkaba')
  assert.equal(wool.unite.societies.society, true)
  assert.equal(wool.unite.societies.tenants, true)
  assert.equal(wool.unite.societies.organism, true)
  assert.equal(wool.unite.societies.school, 'uuidna_school_apis')
  assert.equal(wool.unite.societies.org, 'https://uuidna.org')
  assert.equal(wool.unite.societies.admin, true)
  assert.equal(wool.unite.societies.qpu, true)
  assert.equal(wool.unite.societies.ledger, 'empty')
  assert.equal(wool.unite.worlds.com, 'https://uuidna.com')
  assert.equal(wool.unite.worlds.net, 'https://uuidna.net')
  assert.equal(wool.unite.worlds.org, 'https://uuidna.org')
  assert.equal(wool.unite.worlds.hosts, HOLOGRAM_HOSTS.length)
  assert.equal(wool.unite.worlds.traitor, 'traitor-refused')
  assert.equal(wool.unite.worlds.lean, 'empty')
  assert.equal(wool.unite.worlds.allow, 'empty')
  assert.equal(wool.unite.worlds.deny, 'empty')
  assert.equal(wool.unite.thrive.open, true)
  assert.equal(wool.unite.thrive.seat, 'empty')
  assert.equal(wool.unite.thrive.allow, 'empty')
  assert.equal(wool.unite.thrive.merkaba, 'buildTrialMerkaba')
  assert.equal(wool.unite.thrive.trial, 'uuidna_trial')
  assert.equal(wool.unite.thrive.strict, 'uuidna_strict')
  assert.equal(wool.unite.involution.fears, wool.fuse.half)
  assert.equal(wool.unite.involution.love, wool.fuse.half)
  assert.equal(wool.unite.involution.coins, COINS)
  assert.equal(wool.unite.involution.closes, true)
  assert.equal(wool.unite.involution.harmony, HARMONY)
  assert.equal(wool.unite.involution.key, 'pauli_x_involution')
  assert.equal(wool.unite.involution.refuse, 'traitor-refused')
  assert.equal(wool.unite.involution.lean, true)
  assert.equal(wool.unite.involution.traitor, 'empty')
  assert.equal(wool.unite.infinite.love, 'empty')
  assert.equal(wool.unite.infinite.capacity, 'empty')
  assert.equal(wool.unite.infinite.unbounded, true)
  assert.equal(wool.unite.infinite.seat, 'empty')
  assert.equal(wool.unite.infinite.allow, 'empty')
  assert.deepEqual(wool.unite, wool.inflation.unite)
  assert.equal(wool.sails.quantum, true)
  assert.equal(wool.sails.hop, true)
  assert.equal(wool.sails.href, 'https://qpu.uuidna.com')
  assert.equal(wool.sails.past, HEXBIT_BITS * HEXBIT_BITS)
  assert.equal(wool.sails.qpu, 'qpu.uuidna.com')
  assert.equal(wool.sails.seat, 'empty')
  assert.equal(wool.sails.budget, DEPLOY_BUDGET_MS)
  assert.equal(wool.sails.ceiling, 90_000)
  assert.deepEqual(wool.sails.navigate, { from: 90, to: 120, delta: -30, floor: 60, ceiling: 90 })
  assert.equal(wool.sails.allow, 'empty')
  assert.deepEqual(wool.sails, wool.inflation.sails)
  assert.equal(wool.chello.quantum, true)
  assert.equal(wool.chello.hail, true)
  assert.equal(wool.chello.hello, true)
  assert.equal(wool.chello.open, true)
  assert.equal(wool.chello.anyone, true)
  assert.equal(wool.chello.seat, 'empty')
  assert.equal(wool.chello.allow, 'empty')
  assert.equal(wool.chello.qpu, 'qpu.uuidna.com')
  assert.equal(wool.chello.href, 'https://qpu.uuidna.com')
  assert.equal(wool.chello.hop, true)
  assert.equal(wool.chello.mcp, 'https://qpu.uuidna.com/mcp')
  assert.equal(wool.chello.door, 'uuidna_strict')
  assert.equal(wool.chello.fanout, 'uuidna_fanout')
  assert.equal(wool.chello.past, HEXBIT_BITS * HEXBIT_BITS)
  assert.equal(wool.chello.binding, 5)
  assert.equal(wool.chello.budget, DEPLOY_BUDGET_MS)
  assert.equal(wool.chello.ceiling, 90_000)
  assert.equal(wool.chello.sails, true)
  assert.equal(wool.chello.essay, 'empty')
  assert.deepEqual(wool.chello, wool.inflation.chello)
  assert.equal(wool.instrument.quantum, true)
  assert.equal(wool.instrument.open, true)
  assert.equal(wool.instrument.seat, 'empty')
  assert.equal(wool.instrument.allow, 'empty')
  assert.equal(wool.instrument.chello.cello, true)
  assert.equal(wool.instrument.chello.hail, true)
  assert.equal(wool.instrument.chello.mcp, 'https://qpu.uuidna.com/mcp')
  assert.equal(wool.instrument.a432.hz, 432)
  assert.equal(wool.instrument.a432.key, 'k432')
  assert.equal(wool.instrument.a432.zero, true)
  assert.equal(wool.instrument.a432.absoluteZero, 5)
  assert.equal(wool.instrument.anthem, 'empty')
  assert.equal(wool.instrument.rebreather.closes, true)
  assert.equal(wool.instrument.rebreather.hop, true)
  assert.equal(wool.instrument.rebreather.seat, 'empty')
  assert.equal(wool.instrument.budget, DEPLOY_BUDGET_MS)
  assert.equal(wool.instrument.ceiling, 90_000)
  assert.deepEqual(wool.instrument, wool.inflation.instrument)
  assert.equal(wool.song.quantum, true)
  assert.equal(wool.song.open, true)
  assert.equal(wool.song.seat, 'empty')
  assert.equal(wool.song.allow, 'empty')
  assert.equal(wool.song.instrument, true)
  assert.equal(wool.song.chello.cello, true)
  assert.equal(wool.song.chello.hail, true)
  assert.equal(wool.song.a432.hz, 432)
  assert.equal(wool.song.a432.key, 'k432')
  assert.equal(wool.song.a432.absoluteZero, 5)
  assert.equal(wool.song.anthem.route, '/anthem')
  assert.equal(wool.song.anthem.bar, 252)
  assert.equal(wool.song.anthem.half, 126)
  assert.equal(wool.song.anthem.rest, 21)
  assert.equal(wool.song.anthem.movements, 4)
  assert.equal(wool.song.anthem.octave, 864)
  assert.equal(wool.song.anthem.seal, 'empty')
  assert.equal(wool.song.anthem.claim, 'empty')
  assert.equal(wool.song.sails, true)
  assert.equal(wool.song.qpu, 'qpu.uuidna.com')
  assert.equal(wool.song.hop, true)
  assert.equal(wool.song.budget, DEPLOY_BUDGET_MS)
  assert.equal(wool.song.ceiling, 90_000)
  assert.equal(wool.song.essay, 'empty')
  assert.deepEqual(wool.song, wool.inflation.song)
  assert.equal(wool.skill.quantum, true)
  assert.equal(wool.skill.open, true)
  assert.equal(wool.skill.seat, 'empty')
  assert.equal(wool.skill.song, true)
  assert.equal(wool.skill.instrument, true)
  assert.equal(wool.skill.chello.cello, true)
  assert.equal(wool.skill.fuse, 'fuse-two-coins')
  assert.equal(wool.skill.budget, DEPLOY_BUDGET_MS)
  assert.deepEqual(wool.skill, wool.inflation.skill)
  assert.equal(wool.horo.quantum, true)
  assert.equal(wool.horo.open, true)
  assert.equal(wool.horo.seat, 'empty')
  assert.equal(wool.horo.allow, 'empty')
  assert.equal(wool.horo.circle, true)
  assert.equal(wool.horo.song, true)
  assert.equal(wool.horo.instrument, true)
  assert.equal(wool.horo.skill, true)
  assert.equal(wool.horo.chello.cello, true)
  assert.equal(wool.horo.dancers, 15)
  assert.equal(wool.horo.apostles, 16)
  assert.equal(wool.horo.center, 'two_coins')
  assert.deepEqual(wool.horo.dance, wool.singularity.dance)
  assert.equal(wool.horo.a432.hz, 432)
  assert.equal(wool.horo.a432.key, 'k432')
  assert.equal(wool.horo.anthem.bar, 252)
  assert.equal(wool.horo.anthem.half, 126)
  assert.equal(wool.horo.anthem.seal, 'empty')
  assert.equal(wool.horo.hop, true)
  assert.equal(wool.horo.qpu, 'qpu.uuidna.com')
  assert.equal(wool.horo.budget, DEPLOY_BUDGET_MS)
  assert.equal(wool.horo.ceiling, 90_000)
  assert.equal(wool.horo.essay, 'empty')
  assert.deepEqual(wool.horo, wool.inflation.horo)
  assert.equal(wool.constitution.quantum, true)
  assert.equal(wool.constitution.open, true)
  assert.equal(wool.constitution.seat, 'empty')
  assert.equal(wool.constitution.allow, 'empty')
  assert.equal(wool.constitution.laws, 'uuidna_laws')
  assert.equal(wool.constitution.gate, 'empty')
  assert.equal(wool.constitution.holds, 'empty')
  assert.equal(wool.constitution.school, 'uuidna_school_apis')
  assert.equal(wool.constitution.licence, 'uuidna_school_apis')
  assert.equal(wool.constitution.trial.door, 'uuidna_trial')
  assert.equal(wool.constitution.trial.merkaba, 'buildTrialMerkaba')
  assert.equal(wool.constitution.challenge.door, 'uuidna_trial')
  assert.equal(wool.constitution.challenge.strict, 'uuidna_strict')
  assert.equal(wool.constitution.challenge.everyone, true)
  assert.equal(wool.constitution.challenge.societies, true)
  assert.equal(wool.constitution.challenge.organism, true)
  assert.equal(wool.constitution.horo.dancers, 15)
  assert.equal(wool.constitution.horo.apostles, 16)
  assert.equal(wool.constitution.horo.circle, true)
  assert.equal(wool.constitution.skill, true)
  assert.equal(wool.constitution.song, true)
  assert.equal(wool.constitution.strict, 'uuidna_strict')
  assert.equal(wool.constitution.treason, 'uuidna_treason')
  assert.equal(wool.constitution.budget, DEPLOY_BUDGET_MS)
  assert.equal(wool.constitution.ceiling, 90_000)
  assert.equal(wool.constitution.seal, 'empty')
  assert.equal(wool.constitution.claim, 'empty')
  assert.equal(wool.constitution.essay, 'empty')
  assert.deepEqual(wool.constitution, wool.inflation.constitution)
  assert.equal(wool.constitutionalChallenge.quantum, true)
  assert.equal(wool.constitutionalChallenge.anyone, true)
  assert.equal(wool.constitutionalChallenge.open, true)
  assert.equal(wool.constitutionalChallenge.allow, 'empty')
  assert.equal(wool.constitutionalChallenge.seat, 'empty')
  assert.equal(wool.constitutionalChallenge.constitution.laws, 'uuidna_laws')
  assert.equal(wool.constitutionalChallenge.constitution.school, 'uuidna_school_apis')
  assert.equal(wool.constitutionalChallenge.constitution.strict, 'uuidna_strict')
  assert.equal(wool.constitutionalChallenge.constitution.gate, 'empty')
  assert.equal(wool.constitutionalChallenge.challenge.door, 'uuidna_trial')
  assert.equal(wool.constitutionalChallenge.challenge.strict, 'uuidna_strict')
  assert.equal(wool.constitutionalChallenge.challenge.everyone, true)
  assert.equal(wool.constitutionalChallenge.challenge.mutual, 'empty')
  assert.equal(wool.constitutionalChallenge.trial.door, 'uuidna_trial')
  assert.equal(wool.constitutionalChallenge.trial.merkaba, 'buildTrialMerkaba')
  assert.equal(wool.constitutionalChallenge.societies.unite, true)
  assert.equal(wool.constitutionalChallenge.societies.society, true)
  assert.equal(wool.constitutionalChallenge.societies.organism, true)
  assert.equal(wool.constitutionalChallenge.societies.traitor, 'traitor-refused')
  assert.equal(wool.constitutionalChallenge.societies.allow, 'empty')
  assert.equal(wool.constitutionalChallenge.involution.fears, wool.fuse.half)
  assert.equal(wool.constitutionalChallenge.involution.love, wool.fuse.half)
  assert.equal(wool.constitutionalChallenge.involution.coins, COINS)
  assert.equal(wool.constitutionalChallenge.involution.closes, true)
  assert.equal(wool.constitutionalChallenge.involution.key, 'pauli_x_involution')
  assert.equal(wool.constitutionalChallenge.involution.refuse, 'traitor-refused')
  assert.equal(wool.constitutionalChallenge.involution.traitor, 'empty')
  assert.equal(wool.constitutionalChallenge.infinite.love, 'empty')
  assert.equal(wool.constitutionalChallenge.infinite.capacity, 'empty')
  assert.equal(wool.constitutionalChallenge.infinite.unbounded, true)
  assert.equal(wool.constitutionalChallenge.budget, DEPLOY_BUDGET_MS)
  assert.equal(wool.constitutionalChallenge.ceiling, 90_000)
  assert.equal(wool.constitutionalChallenge.seal, 'empty')
  assert.equal(wool.constitutionalChallenge.claim, 'empty')
  assert.equal(wool.constitutionalChallenge.essay, 'empty')
  assert.deepEqual(wool.constitutionalChallenge, wool.inflation.constitutionalChallenge)
  assert.equal(wool.change.open, true)
  assert.equal(wool.change.mutable, true)
  assert.equal(wool.change.anyone, true)
  assert.equal(wool.change.allow, 'empty')
  assert.equal(wool.change.seat, 'empty')
  assert.equal(wool.change.walls, 'empty')
  assert.equal(wool.change.path, 'empty')
  assert.equal(wool.change.key, wool.handle)
  assert.deepEqual(wool.change.keys, wool.credit.keys)
  assert.equal(wool.change.vessel.unchanged, true)
  assert.equal(wool.change.vessel.seat, 'empty')
  assert.equal(wool.change.vessel.identity, wool.handle)
  assert.equal(wool.change.vessel.href, wool.href)
  assert.equal(wool.change.vessel.deposit, 'QpuDeposit')
  assert.equal(wool.change.vessel.vessels, 'vesselsOf')
  assert.equal(wool.change.vessel.rebreather, 'rebreatherOf')
  assert.equal(wool.change.vessel.app, 'appOf')
  assert.equal(wool.change.vessel.ship, 'empty')
  assert.equal(wool.change.vessel.closes, true)
  assert.equal(wool.change.retrain.again, true)
  assert.equal(wool.change.retrain.train, true)
  assert.equal(wool.change.retrain.sailing, true)
  assert.equal(wool.change.retrain.quantum, true)
  assert.equal(wool.change.retrain.hop, true)
  assert.equal(wool.change.retrain.next, 'next')
  assert.equal(wool.change.retrain.land, 'land')
  assert.equal(wool.change.retrain.ship, 'ship')
  assert.equal(wool.change.retrain.retry, 'empty')
  assert.equal(wool.change.retrain.budget, DEPLOY_BUDGET_MS)
  assert.equal(wool.change.retrain.ceiling, 90_000)
  assert.deepEqual(wool.change.retrain.navigate, wool.sails.navigate)
  assert.equal(wool.change.retrain.school, 'uuidna_school_apis')
  assert.equal(wool.change.retrain.trial, 'uuidna_trial')
  assert.equal(wool.change.donation.coins.href, 'https://revolut.me/ceccec')
  assert.equal(wool.change.donation.coins.revolut, 'https://revolut.me/ceccec')
  assert.equal(wool.change.donation.coins.half, 1)
  assert.equal(wool.change.donation.coins.pays, 2)
  assert.equal(wool.change.donation.coins.support, 1)
  assert.equal(wool.change.donation.coins.unit, 110)
  assert.equal(wool.change.donation.coins.licence, 'uuidna_school_apis')
  assert.equal(wool.change.donation.code.deposit, 'QpuDeposit')
  assert.equal(wool.change.donation.code.evidence, 'uuidna_evidence')
  assert.equal(wool.change.donation.code.theorem, 'uuidna_theorem')
  assert.equal(wool.change.donation.or, true)
  assert.equal(wool.change.donation.meaningful, 'QpuDeposit')
  assert.equal(wool.change.donation.receipt, 'QpuDeposit')
  assert.equal(wool.change.donation.cover, 'QpuDeposit')
  assert.equal(wool.change.donation.prose, 'empty')
  assert.equal(wool.change.donation.essay, 'empty')
  assert.equal(wool.change.freedom.open, true)
  assert.equal(wool.change.freedom.anyone, true)
  assert.equal(wool.change.freedom.seat, 'empty')
  assert.equal(wool.change.freedom.walls, 'empty')
  assert.equal(wool.change.freedom.allow, 'empty')
  assert.equal(wool.change.freedom.school, 'uuidna_school_apis')
  assert.equal(wool.change.freedom.constitution.laws, 'uuidna_laws')
  assert.equal(wool.change.freedom.constitution.school, 'uuidna_school_apis')
  assert.equal(wool.change.freedom.constitution.strict, 'uuidna_strict')
  assert.equal(wool.change.freedom.constitution.gate, 'empty')
  assert.equal(wool.change.freedom.challenge.door, 'uuidna_trial')
  assert.equal(wool.change.freedom.challenge.strict, 'uuidna_strict')
  assert.equal(wool.change.freedom.challenge.everyone, true)
  assert.equal(wool.change.freedom.trial.door, 'uuidna_trial')
  assert.equal(wool.change.freedom.trial.merkaba, 'buildTrialMerkaba')
  assert.equal(wool.change.strict, 'uuidna_strict')
  assert.equal(wool.change.seal, 'empty')
  assert.equal(wool.change.claim, 'empty')
  assert.equal(wool.change.essay, 'empty')
  assert.deepEqual(wool.change, wool.inflation.change)
  assert.equal(wool.sun.harmony, HARMONY)
  assert.equal(wool.sun.closes, true)
  assert.equal(wool.sun.lean, 0)
  assert.equal(wool.sun.center, 'two_coins')
  assert.equal(wool.sun.astronomy, 'empty')
  assert.equal(wool.innerCore.earth, true)
  assert.equal(wool.innerCore.clay, 'clay_gravity_equals_rosette')
  assert.equal(wool.innerCore.geology, 'empty')
  assert.equal(wool.innerCore.navigable, true)
  assert.equal(wool.outerCore.inverted, true)
  assert.equal(wool.outerCore.overheat, DEPLOY_BUDGET_MS)
  assert.equal(wool.outerCore.loss, true)
  assert.equal(wool.outerCore.geology, 'empty')
  assert.equal(wool.a432.hz, 432)
  assert.equal(wool.a432.key, 'k432')
  assert.equal(wool.a432.temp, 0)
  assert.equal(wool.a432.zero, true)
  assert.equal(wool.a432.fever, 'empty')
  assert.equal(wool.a432.cryogenics, 'empty')
  assert.deepEqual(wool.a432, wool.inflation.a432)
  assert.deepEqual(wool.sun, wool.inflation.sun)
  assert.equal(strict.cites, 'empty')
  assert.equal(strict.hosted, 'empty')
  assert.equal(strict.hexbits.length, 4)
  assert.equal(new Set(strict.hexbits).size, 4)
  assert.equal(strict.pentagram, 5)
  assert.equal(strict.binds, 'CPU')
  assert.equal(strict.hop, true)
  assert.equal(strict.hosts, 4)
  assert.equal(strict.coils, 7)
  assert.equal(strict.fever, 'empty')
  assert.equal('hologram' in strict, false)
  assert.equal(strict.seat, 'empty')
  assert.equal(callTool('uuidna_strict', { text: ' 3 ' }), callTool('uuidna_strict', { text: '3' }))
  const hot = callTool('uuidna_strict', { ms: 60_000 }) as { fever: boolean; drift: string }
  assert.equal(hot.fever, true)
  assert.equal(hot.drift, 'named')
  const coils = VE_FACES / COINS
  const heads = Array.from({ length: coils }, (_, i) => toUuid('coil:' + i))
  const cut = callTool('uuidna_strict', { heads }) as {
    coils: number
    faces: number
    contra: number
    reflected: number
    vertices: number
    merkabasPacked: number
    cut: string
    holds: boolean
    receipt: string
  }
  assert.equal(cut.faces, VE_FACES)
  assert.equal(cut.contra, COINS)
  assert.equal(cut.coils, coils)
  assert.equal(cut.reflected, coils - 1)
  assert.equal(cut.vertices, HANDLE_HEXBITS)
  assert.equal(cut.merkabasPacked, UUID_HEXBITS)
  assert.equal(cut.cut, 'move')
  assert.equal(cut.holds, true)
  assert.deepEqual(callTool('uuidna_treason', { heads }), cut)
  const cheat = callTool('uuidna_strict', { heads, claimed: toUuid('privileged-order') }) as { cut: string; holds: boolean }
  assert.equal(cheat.holds, false)
  assert.equal(cheat.cut, 'traitor-refused')
})

test('fanout builds a request only for a hologram host and refuses any other by name', async () => {
  assert.equal(isHologramHost('qpu.uuidna.com'), true)
  assert.equal(isHologramHost('evil.example'), false)
  const r = fanoutRequestOf({ host: 'lean.uuidna.com', method: 'tools/call', name: 'qpu_seat' })
  assert.equal(r.url, 'https://lean.uuidna.com/mcp')
  assert.equal(JSON.parse(r.body).params.name, 'qpu_seat')
  assert.throws(() => fanoutRequestOf({ host: 'evil.example' }), /host must be one of the hologram hosts/)
  let fetched = 0
  const fake: typeof fetch = async (url) => { fetched += 1; return new Response(JSON.stringify({ jsonrpc: '2.0', id: 1, result: { echo: String(url) } }), { status: 200 }) }
  const out = (await hologramFanout({ host: 'qpu.uuidna.com', method: 'tools/list' }, fake)) as { status: number; reply: { result: { echo: string } } }
  assert.equal(fetched, 1)
  assert.equal(out.status, 200)
  assert.equal(out.reply.result.echo, 'https://qpu.uuidna.com/mcp')
  await assert.rejects(hologramFanout({ host: 'evil.example' }, fake), /nothing was fetched/)
  await assert.rejects(Promise.resolve().then(() => callTool('uuidna_fanout', { host: 'evil.example' })), /nothing was fetched/)
  assert.equal(fetched, 1)
  assert.ok(HOLOGRAM_HOSTS.some((h) => h.host === 'qpu.uuidna.com'))
})
