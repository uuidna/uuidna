// src/school/refusals/generated.ts — GENERATED by scripts/gen-refusals from lean/leads.json. Do not edit.
// The refusals register as a module, so the hosted edge (no filesystem) serves uuidna_refusals from the same rows
// the stdio server does. Regenerate with `npm run x -- gen-refusals`; the reconcile chain runs it on every pass.
import type { RefusalSlice } from './slice.js'

/** the refused leads with their boundaries, and the refusals later withdrawn with what overturned them */
export const REFUSAL_SLICE: RefusalSlice = {
 "refused": [],
 "withdrawn": [
  {
   "lead": "Port a security API and a further network API over the Alpine security (86 packages) and network (332 packages) domains, as chat, shell, filesystem, database and blockchain were ported.",
   "overturnedBy": "BUILT 2026-09-01 rather than refused. os/secapi carries ATTESTED OPERATIONS over the 86-package security domain: confine (firejail), inspect-files (clamscan), packet-policy (nft), audit-tls (openssl) — all four plannable, verified against a pinned rootfs whose SHA-256 re-checks with uuidna's own sha256, backend docker. uuidna reimplements none of them; what it adds is that the command and the byte"
  }
 ]
}
