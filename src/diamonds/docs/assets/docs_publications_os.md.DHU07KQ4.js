import{H as e,at as t,it as n,vt as r}from"./chunks/framework.dmHZ7NHG.js";var i=JSON.parse(`{"title":"The OS-integrity algebra","description":"","frontmatter":{},"headers":[],"params":{"slug":"os","address":"85ebdc92-44aa-823c-8673-7cd4c398ddd5","receipt":"3be4c4fb-0059-8343-9dbd-8aa0b80e11a9"},"relativePath":"docs/publications/os.md","filePath":"docs/publications/[slug].md"}`),a={name:`docs/publications/os.md`};function o(e,i,a,o,s,c){return r(),n(`div`,null,[...i[0]||=[t(`<h1 id="the-os-integrity-algebra" tabindex="-1">The OS-integrity algebra <a class="header-anchor" href="#the-os-integrity-algebra" aria-label="Permalink to “The OS-integrity algebra”">​</a></h1><blockquote><p>the decidable facts a DEPLOYMENT is verified against, completing hardware → software → os, each an axiom-free <code>by decide</code> particle: exact-copy verification IS byte-equality, so a single-byte tamper, a truncation, or a REORDERING breaks the match (a provenance is a SEQUENCE, not a set); the SHA-256 digest is a fixed 256 bits, the content-address a fixed 128; and the non-determinism boundary is EXACTLY the two named modules (src/os, src/drivers). The runtime side (Alpine + driver provenance, portAllAlpine over the whole arch matrix) enforces it against real bytes with uuidna&#39;s own pure-TS SHA-256. HONEST SCOPE: integrity, not truth, and NOT execution — uuidna seals what an exact-copy verification decides; it does not boot, port the runtime, link, or run an operating system. A sealed integrity spec, not a booted OS</p></blockquote><p>These 7 facts hold by decision. Each was proven in Lean 4 — no Mathlib, checked sorry-free — and every sentence below links the proof that seals it. This note claims nothing beyond what its theorems settle: not that the domain is complete, only that the prose says nothing the proofs do not. It was written by reading the sealed ledger; read the proofs, not the prose.</p><h2 id="the-facts-each-backed-by-its-proof" tabindex="-1">The facts, each backed by its proof <a class="header-anchor" href="#the-facts-each-backed-by-its-proof" aria-label="Permalink to “The facts, each backed by its proof”">​</a></h2><ul><li>PROVENANCE verification IS byte-equality: the bytes you hold match the pinned release exactly — [1,2,3] equals [1,2,3]. The exact-copy proof is nothing more, and nothing less, than the held bytes equalling the named ones. — <a href="/theorem/exact_copy_is_byte_equality">proof</a>.</li><li>A SINGLE-BYTE TAMPER is DETECTED: change one byte of the image and it no longer equals the pinned bytes — [1,2,3,4] ≠ [1,2,0,4]. One flipped bit fails the check; a modified base cannot masquerade as the named upstream. — <a href="/theorem/single_byte_tamper_is_detected">proof</a>.</li><li>A TRUNCATION is DETECTED: a short image does not equal the pinned bytes — [1,2,3] ≠ [1,2]. Dropping data breaks the exact-copy proof; you cannot pass off a partial base as the whole named release. — <a href="/theorem/truncation_is_detected">proof</a>.</li><li>A PROVENANCE is a SEQUENCE, not a set: reordering the bytes breaks the match — [1,2,3] ≠ [3,2,1]. The same bytes in a different order are a different image; exact-copy pins the order, not just the multiset. — <a href="/theorem/byte_order_is_significant">proof</a>.</li><li>The provenance DIGEST is a fixed width: SHA-256 is 256 bits = 32 bytes = 64 hex characters (32·8 = 256 and 64 = 32·2). The exact-copy fingerprint every release is pinned by has one fixed size. — <a href="/theorem/sha256_digest_is_256_bits">proof</a>.</li><li>The provenance CONTENT-ADDRESS is a 128-bit particle: 16 bytes, 16·8 = 128 — one uuid, the same particle width the whole ledger folds to. A pinned release addresses to exactly 128 bits. — <a href="/theorem/provenance_address_is_128_bits">proof</a>.</li><li>The NON-DETERMINISM boundary is EXACTLY TWO named modules — os and drivers — and nowhere else: [&quot;os&quot;,&quot;drivers&quot;].length = 2. Wall-clock-dependent &quot;latest&quot; reads are honest ONLY here; the rest of uuidna is deterministic, and the count is fixed at two. — <a href="/theorem/boundary_is_exactly_two_named_modules">proof</a>.</li></ul><h2 id="provenance" tabindex="-1">Provenance <a class="header-anchor" href="#provenance" aria-label="Permalink to “Provenance”">​</a></h2><p>These 7 proofs fold, order-invariant, to the receipt <code>3be4c4fb-0059-8343-9dbd-8aa0b80e11a9</code> — recompute it from the same ledger and it returns. The note itself content-addresses to a uuid, so any edit is visible. Writing descends from reading: this note holds only what the proofs it links already sealed, and it was audited by uuidna&#39;s own honesty gate before it was published — a sentence that overreached a proof would have been refused, not shipped.</p><p>Source · <a href="https://github.com/uuidna/uuidna/blob/main/lean/Os.lean" target="_blank" rel="noreferrer">lean/Os.lean</a> · <a href="/theorems">all theorems</a></p><hr><p><strong>Audited before published</strong> · this note passed uuidna&#39;s honesty gate at build time — every claim above links the proof that seals it. Content-address <code>85ebdc92</code> · proofs fold to receipt <code>3be4c4fb</code> (the handles; the full uuids are in this page&#39;s meta, recomputable) · 7 sealed theorems.</p><p><a href="/publications">All publications</a> · <a href="/theorems">All theorems</a> · <a href="/trials">How auditing works</a></p>`,11)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};