import{H as e,at as t,it as n,vt as r}from"./chunks/framework.dmHZ7NHG.js";var i=JSON.parse(`{"title":"Contributing to uuidna","description":"","frontmatter":{},"headers":[],"relativePath":"CONTRIBUTING.md","filePath":"CONTRIBUTING.md"}`),a={name:`CONTRIBUTING.md`};function o(e,i,a,o,s,c){return r(),n(`div`,null,[...i[0]||=[t(`<h1 id="contributing-to-uuidna" tabindex="-1">Contributing to uuidna <a class="header-anchor" href="#contributing-to-uuidna" aria-label="Permalink to “Contributing to uuidna”">​</a></h1><p><strong>Lean is the single source of theorems.</strong> A theorem computes in Lean, or it is not a theorem. Everything the package, the MCP tools and the site consume — <a href="src/theorems/generated.ts"><code>src/theorems/generated.ts</code></a>, <a href="./lean/PRINCIPLE"><code>lean/PRINCIPLE.md</code></a>, <code>CHANGELOG.md</code>, <code>docs/mcp.md</code>, the decide-step costs <code>lean/heartbeats.json</code>, and <code>audit-citations.json</code> — is <strong>derived</strong> from <code>lean/*.lean</code>. Nothing is authored twice.</p><h2 id="setup" tabindex="-1">Setup <a class="header-anchor" href="#setup" aria-label="Permalink to “Setup”">​</a></h2><div class="language-bash"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> hooks:install</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">   # installs the pre-push readiness gate (core.hooksPath=hooks)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> lean</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">            # verify every proof sorry-free and regenerate the ledger</span></span></code></pre></div><p><code>npm run lean</code> needs the Lean toolchain (pinned by <code>lean-toolchain</code>); the generators shell out to <code>lean</code>.</p><h2 id="adding-a-theorem-or-a-domain" tabindex="-1">Adding a theorem or a domain <a class="header-anchor" href="#adding-a-theorem-or-a-domain" aria-label="Permalink to “Adding a theorem or a domain”">​</a></h2><ol><li><p>Add a generator <code>src/scripts/lean-&lt;domain&gt;.ts</code> (a set of decidable facts — each a JS predicate paired with its <code>by decide</code> Lean proposition; see any existing <code>lean-*.ts</code>), <strong>or</strong> a hand-written <code>lean/&lt;Domain&gt;.lean</code> with an inline <code>-- @skill: &lt;capability&gt;</code> above each theorem. <code>lean-all.ts</code> auto-discovers generators — no wiring needed.</p></li><li><p>Author the <strong>skill</strong> at the source (the manifest <code>skill</code> a generator emits, or the inline <code>-- @skill:</code>), never a TS heuristic — every theorem carries an authored skill (a test enforces it).</p></li><li><p>Run <strong>one command</strong> to bring the whole derived layer back in lockstep, commit, and push:</p><div class="language-bash"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> reconcile</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">                          # default commit message</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> reconcile</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;Add the nim domain&quot;</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">  # your own message</span></span></code></pre></div><p>It runs <code>npm run lean</code> (regenerate + verify every proof), rebuilds the MCP catalog, <code>--sync</code>s the heartbeats to 100% coverage, refreshes the citation audit, and <strong>aborts before committing if the ledger does not reconcile</strong> (<code>account.js</code>). Only then does it commit (skipped if nothing changed) and <code>git push origin HEAD</code>.</p></li></ol><h2 id="the-readiness-gate" tabindex="-1">The readiness gate <a class="header-anchor" href="#the-readiness-gate" aria-label="Permalink to “The readiness gate”">​</a></h2><p>The pre-push hook runs the full gate (<code>npm run next</code>) and <strong>blocks the push</strong> on any denial — an overclaim the honesty gate drains, an unsealed proof, an unreconciled account, a next-gap, or a <strong>stale derived layer</strong>. If the denial is a stale derived layer (a <code>git diff</code> of the generated files after adding theorems), the fix is <code>npm run reconcile</code>. A genuine overclaim or unsealed proof must be fixed at the source. The gate can fail — that is what makes a pass mean something.</p><h2 id="honesty" tabindex="-1">Honesty <a class="header-anchor" href="#honesty" aria-label="Permalink to “Honesty”">​</a></h2><p>Every prose claim on the site, in the MCP descriptions, and in the code comments must <strong>link a sealed theorem or be demarcated</strong> — the provenance audit checks it, and the publish gate refuses a note that cites a proof not in the ledger. Say what computes; hold open what does not (<strong>UNVERIFIED</strong>, never &quot;false&quot;); never label a classical thing quantum-accelerated or a non-cryptographic address secret. Integrity, not truth.</p>`,11)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};