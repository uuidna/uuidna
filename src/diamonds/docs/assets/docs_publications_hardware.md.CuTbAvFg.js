import{H as e,at as t,it as n,vt as r}from"./chunks/framework.dmHZ7NHG.js";var i=JSON.parse(`{"title":"The hardware-verifiable binary algebra","description":"","frontmatter":{},"headers":[],"params":{"slug":"hardware","address":"01986c30-09a3-888e-b4ce-ed9715b9af51","receipt":"dc1f95e6-3e49-8c94-9c7b-23a0157bfe9e"},"relativePath":"docs/publications/hardware.md","filePath":"docs/publications/[slug].md"}`),a={name:`docs/publications/hardware.md`};function o(e,i,a,o,s,c){return r(),n(`div`,null,[...i[0]||=[t(`<h1 id="the-hardware-verifiable-binary-algebra" tabindex="-1">The hardware-verifiable binary algebra <a class="header-anchor" href="#the-hardware-verifiable-binary-algebra" aria-label="Permalink to “The hardware-verifiable binary algebra”">​</a></h1><blockquote><p>the low-level combinational-logic identities every digital circuit is built from, each a decidable axiom-free <code>by decide</code> particle: the four gate truth tables as bit arithmetic (NOT a = 1−a, AND = a·b, OR = a+b−a·b, XOR = the axiom-free lxor), XOR = ℤ/2 parity, Boolean closure on the bit, NAND functional completeness (NAND rebuilds NOT/AND/OR — why chips are one repeated gate), De Morgan, the half- and full-adder (sum + 2·carry = the input sum), and the 2:1 multiplexer. All match the live published digital-logic spec. HONEST SCOPE: integrity, not truth — uuidna SEALS the spec a netlist is verified AGAINST; it does not fabricate a device, synthesise a netlist, or develop silicon. A sealed spec, not a chip</p></blockquote><p>These 14 facts hold by decision. Each was proven in Lean 4 — no Mathlib, checked sorry-free — and every sentence below links the proof that seals it. This note claims nothing beyond what its theorems settle: not that the domain is complete, only that the prose says nothing the proofs do not. It was written by reading the sealed ledger; read the proofs, not the prose.</p><h2 id="the-facts-each-backed-by-its-proof" tabindex="-1">The facts, each backed by its proof <a class="header-anchor" href="#the-facts-each-backed-by-its-proof" aria-label="Permalink to “The facts, each backed by its proof”">​</a></h2><ul><li>The NOT gate as arithmetic: NOT a = 1 − a over a bit. Its truth table is [0,1] ↦ [1,0] — the one-input inverter, sealed exactly. — <a href="/theorem/not_gate_truth_table">proof</a>.</li><li>The AND gate as arithmetic: AND a b = a · b over bits. Its truth table over (0,0),(0,1),(1,0),(1,1) is [0,0,0,1] — one only when both inputs are one. — <a href="/theorem/and_gate_truth_table">proof</a>.</li><li>The OR gate as arithmetic: OR a b = a + b − a·b over bits. Its truth table is [0,1,1,1] — zero only when both inputs are zero. — <a href="/theorem/or_gate_truth_table">proof</a>.</li><li>The XOR gate as the axiom-free bitwise <code>lxor</code>: its truth table over the four rows is [0,1,1,0] — one exactly when the inputs differ. The difference detector, kernel-only. — <a href="/theorem/xor_gate_truth_table">proof</a>.</li><li>XOR IS addition in ℤ/2: lxor a b = (a + b) mod 2 for bits. The difference gate and the parity sum are one arithmetic — the binary algebra folds back to the field of two elements. — <a href="/theorem/xor_is_addition_mod_two">proof</a>.</li><li>The algebra is CLOSED on the bit: every primitive gate returns a value ≤ 1 for bit inputs — NOT, AND, OR, XOR all land back in {0,1}. Combinational logic never leaves 𝔹. — <a href="/theorem/gate_output_is_one_bit">proof</a>.</li><li>NAND rebuilds NOT: NAND a a = 1 − a·a = 1 − a for a bit — tie a NAND&#39;s inputs together and it inverts. The first leg of NAND&#39;s universality. — <a href="/theorem/nand_reconstructs_not">proof</a>.</li><li>NAND rebuilds AND: AND a b = NOT (NAND a b) = 1 − (1 − a·b) = a·b — a NAND followed by a NAND-inverter is an AND. The second leg. — <a href="/theorem/nand_reconstructs_and">proof</a>.</li><li>NAND rebuilds OR: OR a b = NAND (NOT a) (NOT b) = 1 − (1−a)(1−b) = a + b − a·b — invert both inputs into a NAND. The third leg. — <a href="/theorem/nand_reconstructs_or">proof</a>.</li><li>NAND is FUNCTIONALLY COMPLETE for {NOT, AND, OR}: across every bit assignment, the three NAND reconstructions all hold at once — so a single gate type generates the whole basis. This is why digital chips are one repeated NAND. — <a href="/theorem/nand_functionally_complete">proof</a>.</li><li>De Morgan in gates: NOT (a AND b) = (NOT a) OR (NOT b), as 1 − a·b = (1−a) + (1−b) − (1−a)(1−b) over bits. The identity that lets a synthesiser push bubbles through gates. — <a href="/theorem/de_morgan_gate_law">proof</a>.</li><li>The HALF-ADDER is correct: sum = XOR a b, carry = AND a b, and sum + 2·carry = a + b over every bit pair. The one-bit addition circuit, proven against its arithmetic meaning. — <a href="/theorem/half_adder_correct">proof</a>.</li><li>The FULL-ADDER is correct: sum = XOR (XOR a b) cin, carry = (a+b+cin)/2, and sum + 2·carry = a + b + cin across all eight input rows. The cell every ripple-carry adder chains, proven exact. — <a href="/theorem/full_adder_correct">proof</a>.</li><li>The 2:1 MULTIPLEXER selects: mux s a b = (1−s)·a + s·b equals a when the select is 0 and b when it is 1, across all eight rows. Routing as arithmetic — the primitive every datapath is woven from. — <a href="/theorem/mux_selects_input">proof</a>.</li></ul><h2 id="provenance" tabindex="-1">Provenance <a class="header-anchor" href="#provenance" aria-label="Permalink to “Provenance”">​</a></h2><p>These 14 proofs fold, order-invariant, to the receipt <code>dc1f95e6-3e49-8c94-9c7b-23a0157bfe9e</code> — recompute it from the same ledger and it returns. The note itself content-addresses to a uuid, so any edit is visible. Writing descends from reading: this note holds only what the proofs it links already sealed, and it was audited by uuidna&#39;s own honesty gate before it was published — a sentence that overreached a proof would have been refused, not shipped.</p><p>Source · <a href="https://github.com/uuidna/uuidna/blob/main/lean/Hardware.lean" target="_blank" rel="noreferrer">lean/Hardware.lean</a> · <a href="/theorems">all theorems</a></p><hr><p><strong>Audited before published</strong> · this note passed uuidna&#39;s honesty gate at build time — every claim above links the proof that seals it. Content-address <code>01986c30</code> · proofs fold to receipt <code>dc1f95e6</code> (the handles; the full uuids are in this page&#39;s meta, recomputable) · 14 sealed theorems.</p><p><a href="/publications">All publications</a> · <a href="/theorems">All theorems</a> · <a href="/trials">How auditing works</a></p>`,11)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};