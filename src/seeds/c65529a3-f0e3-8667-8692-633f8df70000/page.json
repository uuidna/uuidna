{
  "file": "Isometry.lean",
  "slug": "isometry",
  "status": "usable",
  "address": "326e923a-0756-89e9-a142-624356892dde",
  "page": {
    "root": {
      "type": "root",
      "children": [
        {
          "type": "heading",
          "tag": "h1",
          "children": [
            {
              "type": "text",
              "text": "Isometry.lean — the sealed source"
            }
          ]
        },
        {
          "type": "code",
          "language": "lean",
          "children": [
            {
              "type": "text",
              "text": "-- lean/Isometry.lean — GENERATED. THE XOR ISOMETRY — the one identity the cipher, the strand and the code each hold a corner of. Xoring both sides by the same key leaves the Hamming distance unchanged, over every pair and every key: an isometry of the bit cube. Decided here, three separate seals stop being a coincidence of domains and become one algebra read three times. KEY REUSE LEAKS BECAUSE OF IT — the distance between two ciphertexts equals the distance between their plaintexts, so an attacker holding neither key nor message still reads a true fact about the messages; Cipher.lean seals that reuse leaks the plaintext XOR, and this seals why it must. THE STRAND IS THE SAME STEP AT WIDTH TWO — complementing a base is lxor with 3, whose weight is two, so every base differs from its complement in exactly two bits, and a codon (three bases, 4^3 = 64 = 2^6) flips all six. AND A CODE CORRECTS BECAUSE CORRECTION DEPENDS ONLY ON DISTANCE, which the isometry preserves: at distance three a decoder corrects one and detects two, and cannot correct two. The distance is a metric — symmetric, and zero exactly when the words are equal. PURE ARITHMETIC, no ledger count, nothing measured from the world; popcount is axiom-free structural recursion over an 8-bit fuel, the same shape lxor uses. HONEST SCOPE: integrity, not truth — this decides the algebra shared by the three domains, never that any cipher, organism or channel behaves as the model does. Every proof `by decide`, sorry-free, no Mathlib, and axiom-free — depends on NO axiom beyond the leanprover/lean4 kernel (verified by scripts/lean-axioms; not even propext).\n\n-- lxor — bitwise XOR as decidable, AXIOM-FREE arithmetic. Lean's native `^^^` (Nat.xor) is defined by well-founded\n-- recursion over Nat.bitwise, whose `by decide` proof term borrows the `propext` axiom — so a theorem stated with it\n-- is NOT kernel-only. This structural recursion over an 8-bit fuel (covers 0..255, wider than any xor the ledger\n-- takes) folds the SAME value with NO axiom; scripts/lean-axioms proves it. `lxor a b` = a XOR b.\ndef lxorAux : Nat → Nat → Nat → Nat\n  | 0, _, _ => 0\n  | Nat.succ w, a, b => (if a % 2 == b % 2 then 0 else 1) + 2 * lxorAux w (a / 2) (b / 2)\ndef lxor (a b : Nat) : Nat := lxorAux 8 a b\n\n-- popcount as decidable, AXIOM-FREE structural recursion over an 8-bit fuel, the same shape lxor uses\ndef popAux : Nat -> Nat -> Nat\n  | 0, _ => 0\n  | Nat.succ w, n => n % 2 + popAux w (n / 2)\ndef pop (n : Nat) : Nat := popAux 8 n\n\n-- the Hamming distance IS the weight of the difference\ndef dist (a b : Nat) : Nat := pop (lxor a b)\n\n/-- THE ISOMETRY: xoring both sides by the same key leaves the Hamming distance unchanged, for every pair and\n    every key over the four-bit cube. This is the single fact the cipher, the strand and the code each hold a\n    corner of. -/\ntheorem xor_preserves_distance : (List.range 16).all (fun a => (List.range 16).all (fun b => (List.range 16).all (fun k => dist (lxor a k) (lxor b k) == dist a b))) := by decide\n\n/-- WHY KEY REUSE LEAKS, stated as the cause rather than the symptom: because the pad is an isometry, the\n    distance between two ciphertexts EQUALS the distance between their plaintexts. An attacker with neither key\n    nor message still reads a true fact about the messages. Cipher.lean seals that reuse leaks the plaintext\n    XOR; this seals why it must. -/\ntheorem reuse_leaks_by_isometry : (List.range 8).all (fun m1 => (List.range 8).all (fun m2 => (List.range 8).all (fun k => dist (lxor m1 k) (lxor m2 k) == dist m1 m2))) := by decide\n\n/-- EVERY DNA BASE DIFFERS FROM ITS COMPLEMENT IN EXACTLY TWO BITS. A base is two bits, complementing is lxor\n    with 3, and 3 has weight two — so the distance is two for all four bases, never one and never zero. The\n    strand's pairing is the pad's step, at width two. -/\ntheorem complement_flips_two : ((List.range 4).all (fun x => dist x (lxor x 3) == 2)) ∧ (pop 3 = 2) := by decide\n\n/-- AND A CODON IS THREE BASES, SO SIX BITS: 4^3 = 64 = 2^6, and complementing a whole codon flips every one of\n    the six — three bases at two bits each. The width scales with the word; the isometry does not change. -/\ntheorem codon_flips_six : ((4:Nat)^3 = 64) ∧ ((2:Nat)^6 = 64) ∧ (3 * 2 = 6) ∧ (pop 63 = 6) := by decide\n\n/-- THE DISTANCE IS A METRIC, not merely a number: it is symmetric, and it is zero exactly when the two words\n    are equal. Both halves on the line, so the second is discharged where it is claimed rather than assumed from\n    the first. -/\ntheorem distance_is_symmetric : (List.range 16).all (fun a => (List.range 16).all (fun b => (dist a b == dist b a) && ((dist a b == 0) == (a == b)))) := by decide\n\n/-- AND WHY A CODE CORRECTS AT ALL: correction depends only on distance, which the isometry preserves, so the\n    decoder's geometry survives encoding. At distance three a decoder corrects one error and detects two —\n    (3−1)/2 = 1 and 3−1 = 2 — and it cannot correct two, which the line proves rather than leaves implied. -/\ntheorem isometry_bounds_correction : ((3 - 1) / 2 = 1) ∧ (3 - 1 = 2) ∧ ((3 - 1) / 2 ≠ 2) := by decide\n"
            }
          ]
        },
        {
          "type": "page",
          "slug": "theorem-xor_preserves_distance",
          "children": [
            {
              "type": "heading",
              "tag": "h2",
              "children": [
                {
                  "type": "text",
                  "text": "THE ISOMETRY: xoring both sides by the same key leaves the Hamming distance unchanged, for every pair and every key over the four-bit cube. This is the single fact the cipher, the strand and the code each hold a corner of."
                }
              ]
            },
            {
              "type": "code",
              "language": "lean",
              "children": [
                {
                  "type": "text",
                  "text": "(List.range 16).all (fun a => (List.range 16).all (fun b => (List.range 16).all (fun k => dist (lxor a k) (lxor b k) == dist a b)))"
                }
              ]
            },
            {
              "type": "code",
              "language": "lean",
              "children": [
                {
                  "type": "text",
                  "text": "theorem xor_preserves_distance : (List.range 16).all (fun a => (List.range 16).all (fun b => (List.range 16).all (fun k => dist (lxor a k) (lxor b k) == dist a b))) := by decide"
                }
              ]
            }
          ]
        },
        {
          "type": "page",
          "slug": "theorem-reuse_leaks_by_isometry",
          "children": [
            {
              "type": "heading",
              "tag": "h2",
              "children": [
                {
                  "type": "text",
                  "text": "WHY KEY REUSE LEAKS, stated as the cause rather than the symptom: because the pad is an isometry, the distance between two ciphertexts EQUALS the distance between their plaintexts. An attacker with neither key nor message still reads a true fact about the messages. Cipher.lean seals that reuse leaks the plaintext XOR; this seals why it must."
                }
              ]
            },
            {
              "type": "code",
              "language": "lean",
              "children": [
                {
                  "type": "text",
                  "text": "(List.range 8).all (fun m1 => (List.range 8).all (fun m2 => (List.range 8).all (fun k => dist (lxor m1 k) (lxor m2 k) == dist m1 m2)))"
                }
              ]
            },
            {
              "type": "code",
              "language": "lean",
              "children": [
                {
                  "type": "text",
                  "text": "theorem reuse_leaks_by_isometry : (List.range 8).all (fun m1 => (List.range 8).all (fun m2 => (List.range 8).all (fun k => dist (lxor m1 k) (lxor m2 k) == dist m1 m2))) := by decide"
                }
              ]
            }
          ]
        },
        {
          "type": "page",
          "slug": "theorem-complement_flips_two",
          "children": [
            {
              "type": "heading",
              "tag": "h2",
              "children": [
                {
                  "type": "text",
                  "text": "EVERY DNA BASE DIFFERS FROM ITS COMPLEMENT IN EXACTLY TWO BITS. A base is two bits, complementing is lxor with 3, and 3 has weight two — so the distance is two for all four bases, never one and never zero. The strand's pairing is the pad's step, at width two."
                }
              ]
            },
            {
              "type": "code",
              "language": "lean",
              "children": [
                {
                  "type": "text",
                  "text": "((List.range 4).all (fun x => dist x (lxor x 3) == 2)) ∧ (pop 3 = 2)"
                }
              ]
            },
            {
              "type": "code",
              "language": "lean",
              "children": [
                {
                  "type": "text",
                  "text": "theorem complement_flips_two : ((List.range 4).all (fun x => dist x (lxor x 3) == 2)) ∧ (pop 3 = 2) := by decide"
                }
              ]
            }
          ]
        },
        {
          "type": "page",
          "slug": "theorem-codon_flips_six",
          "children": [
            {
              "type": "heading",
              "tag": "h2",
              "children": [
                {
                  "type": "text",
                  "text": "AND A CODON IS THREE BASES, SO SIX BITS: 4^3 = 64 = 2^6, and complementing a whole codon flips every one of the six — three bases at two bits each. The width scales with the word; the isometry does not change."
                }
              ]
            },
            {
              "type": "code",
              "language": "lean",
              "children": [
                {
                  "type": "text",
                  "text": "((4:Nat)^3 = 64) ∧ ((2:Nat)^6 = 64) ∧ (3 * 2 = 6) ∧ (pop 63 = 6)"
                }
              ]
            },
            {
              "type": "code",
              "language": "lean",
              "children": [
                {
                  "type": "text",
                  "text": "theorem codon_flips_six : ((4:Nat)^3 = 64) ∧ ((2:Nat)^6 = 64) ∧ (3 * 2 = 6) ∧ (pop 63 = 6) := by decide"
                }
              ]
            }
          ]
        },
        {
          "type": "page",
          "slug": "theorem-distance_is_symmetric",
          "children": [
            {
              "type": "heading",
              "tag": "h2",
              "children": [
                {
                  "type": "text",
                  "text": "THE DISTANCE IS A METRIC, not merely a number: it is symmetric, and it is zero exactly when the two words are equal. Both halves on the line, so the second is discharged where it is claimed rather than assumed from the first."
                }
              ]
            },
            {
              "type": "code",
              "language": "lean",
              "children": [
                {
                  "type": "text",
                  "text": "(List.range 16).all (fun a => (List.range 16).all (fun b => (dist a b == dist b a) && ((dist a b == 0) == (a == b))))"
                }
              ]
            },
            {
              "type": "code",
              "language": "lean",
              "children": [
                {
                  "type": "text",
                  "text": "theorem distance_is_symmetric : (List.range 16).all (fun a => (List.range 16).all (fun b => (dist a b == dist b a) && ((dist a b == 0) == (a == b)))) := by decide"
                }
              ]
            }
          ]
        },
        {
          "type": "page",
          "slug": "theorem-isometry_bounds_correction",
          "children": [
            {
              "type": "heading",
              "tag": "h2",
              "children": [
                {
                  "type": "text",
                  "text": "AND WHY A CODE CORRECTS AT ALL: correction depends only on distance, which the isometry preserves, so the decoder's geometry survives encoding. At distance three a decoder corrects one error and detects two — (3−1)/2 = 1 and 3−1 = 2 — and it cannot correct two, which the line proves rather than leaves implied."
                }
              ]
            },
            {
              "type": "code",
              "language": "lean",
              "children": [
                {
                  "type": "text",
                  "text": "((3 - 1) / 2 = 1) ∧ (3 - 1 = 2) ∧ ((3 - 1) / 2 ≠ 2)"
                }
              ]
            },
            {
              "type": "code",
              "language": "lean",
              "children": [
                {
                  "type": "text",
                  "text": "theorem isometry_bounds_correction : ((3 - 1) / 2 = 1) ∧ (3 - 1 = 2) ∧ ((3 - 1) / 2 ≠ 2) := by decide"
                }
              ]
            }
          ]
        }
      ]
    }
  }
}
