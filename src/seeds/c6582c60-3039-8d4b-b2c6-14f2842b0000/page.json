{
  "file": "Phase.lean",
  "slug": "phase",
  "status": "usable",
  "address": "de741c82-678b-8bfe-a672-7435022203d5",
  "page": {
    "root": {
      "type": "root",
      "children": [
        {
          "type": "heading",
          "tag": "h1",
          "children": [
            {
              "type": "text",
              "text": "Phase.lean — the sealed source"
            }
          ]
        },
        {
          "type": "code",
          "language": "lean",
          "children": [
            {
              "type": "text",
              "text": "-- lean/Phase.lean — GENERATED. PHASE — why the alternating walk does not close, and which half is responsible. The walk alternates the reflection dz(x) = 10 − x with doubling mod 9, so a PERIOD requires returning to the seed IN PHASE, after a completed pair. The cause of the failure is decided here: dz is a BIJECTION, sending ten digits onto ten and erasing nothing, while doubling sends 0 and 9 to the same image and so maps ten digits onto NINE — one digit lost per pass, and the image never widens again. A walk that has left the image cannot return to a seed outside it. Only zero closes, at two steps, because both maps fix it; five sits on its seed after the first step but that is out of phase, with doubling still owed, and doubling then moves it to 1. PURE ARITHMETIC, no ledger count, nothing measured from the world. HONEST SCOPE: integrity, not truth — `by decide` settles the two maps and their images. That a given walk therefore never closes is the reading these facts support, and is not itself sealed. Every proof `by decide`, sorry-free, no Mathlib, and axiom-free — depends on NO axiom beyond the leanprover/lean4 kernel (verified by scripts/lean-axioms; not even propext).\n\ndef dz (d : Nat) : Nat := if d = 0 then 0 else 10 - d\ndef dbl (d : Nat) : Nat := (2 * d) % 9\n\n/-- THE REFLECTION IS A BIJECTION: dz sends the ten digits onto ten distinct digits, so nothing is lost and\n    every step can be undone. Reversible, and therefore barren on its own. -/\ntheorem dz_loses_nothing : ((List.range 10).map dz).eraseDups.length = 10 := by decide\n\n/-- DOUBLING LOSES A DIGIT, and the loss is named: 2·0 mod 9 = 0 and 2·9 mod 9 = 0, so nine and zero share an\n    image. The map sends ten digits onto nine, is therefore not injective, and the step cannot be undone. -/\ntheorem doubling_collapses_nine : (((List.range 10).map dbl).eraseDups.length = 9) ∧ (dbl 0 = dbl 9) ∧ (dbl 0 = 0) := by decide\n\n/-- THE TWO HALVES ARE NOT THE SAME KIND OF MAP, and the line says so: the reflection's image has ten members\n    and doubling's has nine, and ten is not nine. One erases nothing, the other erases exactly one digit per\n    pass. -/\ntheorem maps_differ_in_reach : (((List.range 10).map dz).eraseDups.length ≠ ((List.range 10).map dbl).eraseDups.length) ∧ (10 ≠ 9) := by decide\n\n/-- ONLY ZERO CLOSES. Both maps fix it — dz 0 = 0 and dbl 0 = 0 — so the walk returns to its seed after one\n    completed pair, in phase, at two steps. It is the sole seed with a period, and the reason is that neither\n    map moves it. -/\ntheorem zero_closes_in_phase : (dz 0 = 0) ∧ (dbl 0 = 0) := by decide\n\n/-- AND A RETURN IS NOT A PERIOD. Five is fixed by the reflection (dz 5 = 5) so the walk sits on its seed after\n    the FIRST step — but that is an odd number of operations, with doubling still owed, so the walk is out of\n    phase and has not closed. Doubling then moves it: dbl 5 = 1, and 1 is not 5. -/\ntheorem five_returns_out_of_phase : (dz 5 = 5) ∧ (dbl 5 = 1) ∧ (dbl 5 ≠ 5) := by decide\n\n/-- THE DOMAIN NARROWS AS THE WALK RUNS: applying doubling to the ten digits leaves nine, and applying it again\n    leaves nine of those — the image cannot grow. SCOPE: what decides here is that the image never widens, which\n    is what makes a return to an outside seed impossible. That the walk therefore NEVER closes for such a seed\n    is the reading, not the seal — `by decide` settles the maps, not every walk. -/\ntheorem reach_shrinks_each_pass : ((((List.range 10).map dbl).eraseDups.map dbl).eraseDups.length ≤ ((List.range 10).map dbl).eraseDups.length) ∧ (((List.range 10).map dbl).eraseDups.length = 9) := by decide\n"
            }
          ]
        },
        {
          "type": "page",
          "slug": "theorem-dz_loses_nothing",
          "children": [
            {
              "type": "heading",
              "tag": "h2",
              "children": [
                {
                  "type": "text",
                  "text": "THE REFLECTION IS A BIJECTION: dz sends the ten digits onto ten distinct digits, so nothing is lost and every step can be undone. Reversible, and therefore barren on its own."
                }
              ]
            },
            {
              "type": "code",
              "language": "lean",
              "children": [
                {
                  "type": "text",
                  "text": "((List.range 10).map dz).eraseDups.length = 10"
                }
              ]
            },
            {
              "type": "code",
              "language": "lean",
              "children": [
                {
                  "type": "text",
                  "text": "theorem dz_loses_nothing : ((List.range 10).map dz).eraseDups.length = 10 := by decide"
                }
              ]
            }
          ]
        },
        {
          "type": "page",
          "slug": "theorem-doubling_collapses_nine",
          "children": [
            {
              "type": "heading",
              "tag": "h2",
              "children": [
                {
                  "type": "text",
                  "text": "DOUBLING LOSES A DIGIT, and the loss is named: 2·0 mod 9 = 0 and 2·9 mod 9 = 0, so nine and zero share an image. The map sends ten digits onto nine, is therefore not injective, and the step cannot be undone."
                }
              ]
            },
            {
              "type": "code",
              "language": "lean",
              "children": [
                {
                  "type": "text",
                  "text": "(((List.range 10).map dbl).eraseDups.length = 9) ∧ (dbl 0 = dbl 9) ∧ (dbl 0 = 0)"
                }
              ]
            },
            {
              "type": "code",
              "language": "lean",
              "children": [
                {
                  "type": "text",
                  "text": "theorem doubling_collapses_nine : (((List.range 10).map dbl).eraseDups.length = 9) ∧ (dbl 0 = dbl 9) ∧ (dbl 0 = 0) := by decide"
                }
              ]
            }
          ]
        },
        {
          "type": "page",
          "slug": "theorem-maps_differ_in_reach",
          "children": [
            {
              "type": "heading",
              "tag": "h2",
              "children": [
                {
                  "type": "text",
                  "text": "THE TWO HALVES ARE NOT THE SAME KIND OF MAP, and the line says so: the reflection's image has ten members and doubling's has nine, and ten is not nine. One erases nothing, the other erases exactly one digit per pass."
                }
              ]
            },
            {
              "type": "code",
              "language": "lean",
              "children": [
                {
                  "type": "text",
                  "text": "(((List.range 10).map dz).eraseDups.length ≠ ((List.range 10).map dbl).eraseDups.length) ∧ (10 ≠ 9)"
                }
              ]
            },
            {
              "type": "code",
              "language": "lean",
              "children": [
                {
                  "type": "text",
                  "text": "theorem maps_differ_in_reach : (((List.range 10).map dz).eraseDups.length ≠ ((List.range 10).map dbl).eraseDups.length) ∧ (10 ≠ 9) := by decide"
                }
              ]
            }
          ]
        },
        {
          "type": "page",
          "slug": "theorem-zero_closes_in_phase",
          "children": [
            {
              "type": "heading",
              "tag": "h2",
              "children": [
                {
                  "type": "text",
                  "text": "ONLY ZERO CLOSES. Both maps fix it — dz 0 = 0 and dbl 0 = 0 — so the walk returns to its seed after one completed pair, in phase, at two steps. It is the sole seed with a period, and the reason is that neither map moves it."
                }
              ]
            },
            {
              "type": "code",
              "language": "lean",
              "children": [
                {
                  "type": "text",
                  "text": "(dz 0 = 0) ∧ (dbl 0 = 0)"
                }
              ]
            },
            {
              "type": "code",
              "language": "lean",
              "children": [
                {
                  "type": "text",
                  "text": "theorem zero_closes_in_phase : (dz 0 = 0) ∧ (dbl 0 = 0) := by decide"
                }
              ]
            }
          ]
        },
        {
          "type": "page",
          "slug": "theorem-five_returns_out_of_phase",
          "children": [
            {
              "type": "heading",
              "tag": "h2",
              "children": [
                {
                  "type": "text",
                  "text": "AND A RETURN IS NOT A PERIOD. Five is fixed by the reflection (dz 5 = 5) so the walk sits on its seed after the FIRST step — but that is an odd number of operations, with doubling still owed, so the walk is out of phase and has not closed. Doubling then moves it: dbl 5 = 1, and 1 is not 5."
                }
              ]
            },
            {
              "type": "code",
              "language": "lean",
              "children": [
                {
                  "type": "text",
                  "text": "(dz 5 = 5) ∧ (dbl 5 = 1) ∧ (dbl 5 ≠ 5)"
                }
              ]
            },
            {
              "type": "code",
              "language": "lean",
              "children": [
                {
                  "type": "text",
                  "text": "theorem five_returns_out_of_phase : (dz 5 = 5) ∧ (dbl 5 = 1) ∧ (dbl 5 ≠ 5) := by decide"
                }
              ]
            }
          ]
        },
        {
          "type": "page",
          "slug": "theorem-reach_shrinks_each_pass",
          "children": [
            {
              "type": "heading",
              "tag": "h2",
              "children": [
                {
                  "type": "text",
                  "text": "THE DOMAIN NARROWS AS THE WALK RUNS: applying doubling to the ten digits leaves nine, and applying it again leaves nine of those — the image cannot grow. SCOPE: what decides here is that the image never widens, which is what makes a return to an outside seed impossible. That the walk therefore NEVER closes for such a seed is the reading, not the seal — `by decide` settles the maps, not every walk."
                }
              ]
            },
            {
              "type": "code",
              "language": "lean",
              "children": [
                {
                  "type": "text",
                  "text": "((((List.range 10).map dbl).eraseDups.map dbl).eraseDups.length ≤ ((List.range 10).map dbl).eraseDups.length) ∧ (((List.range 10).map dbl).eraseDups.length = 9)"
                }
              ]
            },
            {
              "type": "code",
              "language": "lean",
              "children": [
                {
                  "type": "text",
                  "text": "theorem reach_shrinks_each_pass : ((((List.range 10).map dbl).eraseDups.map dbl).eraseDups.length ≤ ((List.range 10).map dbl).eraseDups.length) ∧ (((List.range 10).map dbl).eraseDups.length = 9) := by decide"
                }
              ]
            }
          ]
        }
      ]
    }
  }
}
