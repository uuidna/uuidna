// categories/books — THE READING ROOM SHELF (lead 81b, the captain's "next 4/4 come from books"): the corpus
// pipeline as four hand instruments — the reader (a text's recomputable identity and its ledger linkage), the
// fact-finder (the word-arithmetic ear, controls-first), the librarian (the shelf ranked by counted weight),
// and the quote-trial (a quoted sentence stands the citation trial and leaves addressed). The books the first
// thread surfaced — music, dance, singing — become the school's own reading room: everything computes where the
// visitor stands, nothing fetched, nothing believed on anyone's word.
export { readPassage, type Reading } from './reader.js'
export { findFacts, type FactFinding } from './fact-finder.js'
export { rankShelf, type BookLead, type ShelfEntry } from './librarian.js'
export { tryQuote, type QuoteVerdict } from './quote-trial.js'
