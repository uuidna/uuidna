// categories/practice — THE PRACTICE SHELF (lead 81c, the captain's "next 4/4 come from practice"): the school's
// own loop as four hand instruments — the drill (a sealed theorem as a recompute challenge, recorded the loop's
// way), the feedback fold (trials to one order-invariant receipt — same work, same address, no answers shared),
// the loop-closure meter (the trinity closes the loop: three consecutive recomputations, reopenable — a ratchet
// that cannot reopen is a certificate), and the prerequisite walker (a failed theorem names its own prerequisite;
// the walker makes the road from the ledger's own citations). Everything computes where the student stands.
export { drillOf, attemptDrill, type Drill, type TheoremLike } from './drill.js'
export { foldFeedback, type FeedbackFold } from './feedback-fold.js'
export { meterLoop, CLOSES_AT, type LoopMeter } from './loop-meter.js'
export { prerequisitesOf, walkTo, type Walk } from './prereq-walker.js'
