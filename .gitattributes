# GENERATED by gen-gitattributes from DRAIN_PATHS — do not hand-edit (one-receipt drain holds it).
# THE NEWLINE IS PART OF THE ADDRESS. Seals fold file BYTES, so a CRLF checkout moves every coin while git still
# reports a clean tree (it normalises the ending away when comparing, and never when a generator reads). LF on
# every host, declared by the repository rather than by each clone remembering a local setting.
* text=auto eol=lf

# The derived layer never merges: both sides of a conflict here are correct outputs of different sources, and the
# true value is the recomputation from the merged source. Git keeps one side; reconcile/spin --seal then collapses
# it to the computed value. Enable the driver once per clone: npm run setup:git
src/theorems/generated.ts merge=derived
lean/PRINCIPLE.md merge=derived
CHANGELOG.md merge=derived
lean/axioms.json merge=derived
docs/mcp.md merge=derived
docs/apis.md merge=derived
docs/leads.md merge=derived
audit-citations.json merge=derived
support-audit.json merge=derived
research-leads.json merge=derived
quantum-fold.json merge=derived
spin-manifest.json merge=derived
lean/heartbeats.json merge=derived
lean/proof-cache.json merge=derived
prose-trials.json merge=derived
docs/captain-claims.json merge=derived
src/seeds/** merge=derived
lean/quantum-capacity.json merge=derived
lean/quantum-capacity.md merge=derived
docs/quantum.md merge=derived
lean/quantum-advantage.json merge=derived
docs/public/quantum-advantage.jsonld merge=derived
lean/seo-url-map.json merge=derived
docs/expose.md merge=derived
docs/waves.md merge=derived
docs/unlocks.md merge=derived
docs/index.md merge=derived
lean/unlocks.json merge=derived
README.md merge=derived
llm.txt merge=derived
docs/analytics.md merge=derived
gate-receipt.json merge=derived
docs/school.md merge=derived
docs/sequence-field.md merge=derived
src/falsifiers.test.ts merge=derived
src/rosetta-mirror.ts merge=derived
docs/articles/** merge=derived
docs/public/feed.json merge=derived
docs/public/terminology.json merge=derived
docs/prose-evidence.md merge=derived
.gitattributes merge=derived
packages/** merge=derived
lean/** merge=derived
.zenodo.json merge=derived
zenodo/** merge=derived
reports.json merge=derived
lean/statement-index.json merge=derived
lean/*-manifest.json merge=derived
docs/captain-claims.md merge=derived
docs/captain-claims-complete.json merge=derived
book-leads.json merge=derived
docs/song.md merge=derived
docs/public/song.wav merge=derived
docs/anthem.md merge=derived
lean/sessions.json merge=derived
docs/referrer-song.md merge=derived
docs/open-questions.md merge=derived
docs/search-feed.md merge=derived
docs/apps.md merge=derived
docs/evasion.md merge=derived
docs/symphony.md merge=derived
docs/utterances.md merge=derived
docs/os.md merge=derived
lean/alpine-hexbit-monitor.json merge=derived
docs/public/alpine-hexbit-monitor.jsonld merge=derived
lean/mcp-alpine-man.json merge=derived
lean/browser-apps-usable.json merge=derived
lean/alpine-discovery.json merge=derived
mirror/alpine-overlay.tsv merge=derived
mirror/alpine-testing-leads.tsv merge=derived
docs/store.md merge=derived
docs/models.md merge=derived
src/quantum/models/mirror.ts merge=derived
docs/doctrine.md merge=derived
src/chunks/** merge=derived
src/handles/** merge=derived

# ── THE RECORDS UNDER A DIRECTORY ENTRY THAT ARE NOT DERIVED (2026-08-25) ────────────────────────────────────
# A DRAIN_PATHS entry with no dot becomes a DIRECTORY glob above — 'lean' becomes lean/**. That is right for the
# WINGS: every .lean file there is written by its generator, so both sides of a conflict are correct outputs and
# the recomputation settles it. It is far too wide for the hand-maintained JSON living beside them, and the
# driver is not a merge at all: merge.derived.driver is `true`, which keeps one side and DISCARDS the other in
# silence, on the promise that a reconcile recomputes the truth. NOTHING recomputes these.
#
# Measured before this list existed: lean/wave-queue.json is a DEPOSIT record — mcp.ts writes it through
# depositCandidates, gen-waves only READS it, and it appears nowhere in reconcile's derive block or generate's
# manifest. One checkout held 462 accepted deposits where origin held 53, and the wings regenerate from a queue
# that no longer names them, so src/firewall, src/knowledge and src/resonance are zero files on origin and
# thirteen sealed theorems went with them. Partial, which is why it went unnoticed: whichever side won lived.
#
# UNSET, NOT RE-CLASSIFIED, and NOT removed from DRAIN_PATHS — the staging behaviour reconcile depends on is a
# separate question from whether a merge may discard a side. `!merge` restores the ordinary three-way merge, so
# a real conflict here produces CONFLICT MARKERS an author resolves. A visible conflict is recoverable; a silent
# discard is not.
lean/wave-queue.json !merge
lean/leads.json !merge
lean/research-ledger.json !merge
lean/dormant-scripts.json !merge
lean/mcp-context-budget.json !merge
lean/tool-exercise-baseline.json !merge
.gitattributes !merge
