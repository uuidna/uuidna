---
title: The terminal
description: Layer 1 uuidnaOS in the browser — ls, apk, man over the virtual install and full catalogue. Local exec, receipted, no Alpine binaries.
---

# The terminal <Badge type="tip" text="Layer 1 · production shell" />

> This path's **sealed meaning** is busybox — *"Size optimized toolbox of many common UNIX utilities"*
> ([`the_terminal_is_the_toolbox`](/theorem/the_terminal_is_the_toolbox)). On this computer the toolbox runs as
> **Layer 1 uuidnaOS**: the shell below boots the verified install port, primes the Alpine catalogue, and runs
> `uuidna_exec` **locally in your browser** — `ls`, `apk`, `man`, busybox applets over the virtual filesystem and
> full census. Nothing executes Alpine binaries ([`the_os_is_bootable_quantum`](/theorem/the_os_is_bootable_quantum)).

<ClientOnly>
  <ExecShell />
</ClientOnly>

**Production use:** type a published Alpine package (`nginx`, `openssl`, `busybox`) or a `cmd:` (`dotnet`, `omp`);
each answer folds to a content-address receipt and 32 hexbit states
([`a_spec_compiles_to_hexbits`](/theorem/a_spec_compiles_to_hexbits)). Familiar busybox/apk lines still work. Session
`apk add/del` persists for this tab only — simulated install, host rootfs unchanged.

**Living field:** `sequence field`, `sequence run 9`, `sequence dash`, and `sequence invariants` walk the ℤ/9 vortex
constructors from [Sequence.lean](/lean/Sequence.lean) — same surface as MCP **Living field** tools on [/mcp](/mcp).
Full reference: [The living field](/sequence-field).

| Layer | Where | What |
|-------|-------|------|
| **1** | **This page** | Local `uuidna_exec` — virtual install + 28k catalogue |
| **1** | [/catalogue](/catalogue) | Search/inspect packages (same mint as `apk info`) |
| **2** | Host stdio / MCP `uuidna_run` | Verify-then-run pinned rootfs via docker chroot (not in browser) |
| MCP | [/chat](/chat) | Full toolbox — every tool on `/mcp`, gate + two coins |

Browse the [default install](/os) for port status and the hexbit boot image. Integrity and meaning — not execution
on a CPU unless you explicitly invoke Layer 2 on a host with Docker.
