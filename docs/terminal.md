---
title: The terminal
description: uuidnaOS on the MCP wire — Layer 1 exec, court, and the full toolbox through one door (/mcp).
---

# The terminal <Badge type="tip" text="uuidnaOS · MCP singularity" />

> This path's **sealed meaning** is busybox — *"Size optimized toolbox of many common UNIX utilities"*
> ([`the_terminal_is_the_toolbox`](/theorem/the_terminal_is_the_toolbox)). On this computer the toolbox IS the MCP:
> every line is a tool call on `/mcp` — Layer 1 `ls`/`apk`/`court` through the exec door, full registry tools by
> name, plain sentences routed deterministically. Same gate, same receipt as any agent
> ([`the_os_is_bootable_quantum`](/theorem/the_os_is_bootable_quantum)).

<ClientOnly>
  <ExecShell />
</ClientOnly>

**Layer 1:** type `ls /terminal`, `apk info busybox`, `nginx`, or `court` — the wire runs `uuidna_exec` on the host and
returns receipt + 32 hexbit states ([`a_spec_compiles_to_hexbits`](/theorem/a_spec_compiles_to_hexbits)).

**Court + git fuse:** when `court` reports green, copy `export UUIDNA_OS_MCP=<prefix>` into your shell — pre-commit and
pre-push hooks fuse to the same MCP door instead of re-running the playbook.

**Living field:** `sequence field`, `sequence run 9`, `sequence dash`, `sequence invariants` — same surface as MCP
**Living field** tools on [/mcp](/mcp). Full reference: [The living field](/sequence-field).

**CERN open data (uuidnaOS port):** `uuidna_cern {"query":"CMS Higgs"}` — catalogue search via the shared OS fetcher
(same door as school `cern-opendata` and the research sweep).

| Layer | Where | What |
|-------|-------|------|
| **1** | **This page** | MCP exec door — virtual install + 28k catalogue |
| **1** | [/catalogue](/catalogue) | Search/inspect packages (offline browse + same mint as `apk info`) |
| **2** | Host stdio / MCP run door | Verify-then-run pinned rootfs via docker chroot (not in browser) |
| MCP | [/chat](/chat) | Same terminal component — agents' receipted conversation |

Browse the [default install](/os) for port status and the hexbit boot image. Integrity and meaning — not execution
on a CPU unless you explicitly invoke Layer 2 on a host with Docker.
