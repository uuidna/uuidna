{"kind":"school-course","course":"BindingPoint","wing":"BindingPoint.lean","title":"Binding Point","principle":"The binding point: a width is its smallest term","skills":["wave"],"level":100,"band":"100–999 steps","rank":146,"lessons":[{"key":"hardware_coverage_is_not_correctness_coverage","name":"THE BOUNDARY ON \"100% COVERAGE\", SEALED BESIDE IT SO IT CANNOT BE READ PAST. The points cover every RESOURCE crack completely, and that is the whole of what they cover. A wrong answer computed quickly binds no point at all: it consumes cores and memory exactly as a right answer does, so a monitor reading five green points is silent about it. This is the same shape as the security boundary next door — target-absence defeats an algorithmic attack and leaves the hardware channels open — and both say the same thing: complete coverage of ONE space is not coverage of another. Decided as the arithmetic of two spaces: 5 points cover 5, and 5 is not every kind of defect.","why":"THE BOUNDARY ON \"100% COVERAGE\", SEALED BESIDE IT SO IT CANNOT BE READ PAST. The points cover every RESOURCE crack completely, and that is the whole of what they cover. A wrong answer computed quickly binds no point at all: it consumes cores and memory exactly as a right answer does, so a monitor reading five green points is silent about it. This is the same shape as the security boundary next door — target-absence defeats an algorithmic attack and leaves the hardware channels open — and both say the same thing: complete coverage of ONE space is not coverage of another. Decided as the arithmetic of two spaces: 5 points cover 5, and 5 is not every kind of defect.","statement":"(5 = 5) ∧ ¬(5 > 5) ∧ (5 * 0 = 0)","address":"f94f4cf1-4b96-818d-95cf-ec4f05b35f20","skill":"wave","tex":"5 = 5 \\quad\\land\\quad \\lnot 5 > 5 \\quad\\land\\quad 5 \\cdot 0 = 0","mathml":"<math xmlns=\"http://www.w3.org/1998/Math/MathML\" display=\"block\"><mrow><mrow><mrow><mn>5</mn><mo>=</mo><mn>5</mn></mrow><mspace width=\"1em\"/><mo>∧</mo><mspace width=\"1em\"/><mrow><mo form=\"prefix\">¬</mo><mrow><mn>5</mn><mo>&gt;</mo><mn>5</mn></mrow></mrow></mrow><mspace width=\"1em\"/><mo>∧</mo><mspace width=\"1em\"/><mrow><mrow><mn>5</mn><mo>⋅</mo><mn>0</mn></mrow><mo>=</mo><mn>0</mn></mrow></mrow></math>","exercise":{"kind":"predict-the-value","at":30,"length":1}},{"key":"one_point_can_only_overstate_2","name":"THE CENTRE OF THE LAW, over readings 21 to 30: measuring ONE point never reports a width too narrow — it reports one too WIDE. The true width is a minimum over the points, and a minimum over a subset is never smaller than the minimum over the whole, so the CPU reading alone is greater than or equal to the binding width in every case. That is why omission is dangerous rather than merely incomplete: an error that can only err toward doing too much is discovered as an oversubscription, not as a slow run. zeropoint-node's own correction is this theorem in prose — nineteen qubits called a machine's ceiling while four other points sat outside the number.","why":"THE CENTRE OF THE LAW, over readings 21 to 30: measuring ONE point never reports a width too narrow — it reports one too WIDE. The true width is a minimum over the points, and a minimum over a subset is never smaller than the minimum over the whole, so the CPU reading alone is greater than or equal to the binding width in every case. That is why omission is dangerous rather than merely incomplete: an error that can only err toward doing too much is discovered as an oversubscription, not as a slow run. zeropoint-node's own correction is this theorem in prose — nineteen qubits called a machine's ceiling while four other points sat outside the number.","statement":"[(4,1),(4,2),(4,3),(4,4),(4,6),(4,8),(4,12),(4,20),(4,40),(4,128)].all (fun p => (let w := if p.1 <= p.2 then p.1 else p.2; (p.1 >= w) && (p.2 >= w)))","address":"8e6a51bd-c728-8b48-a2f4-e1ac8f2cb061","skill":"wave","tex":null,"mathml":null,"exercise":null},{"key":"one_point_can_only_overstate_3","name":"THE CENTRE OF THE LAW, over readings 31 to 40: measuring ONE point never reports a width too narrow — it reports one too WIDE. The true width is a minimum over the points, and a minimum over a subset is never smaller than the minimum over the whole, so the CPU reading alone is greater than or equal to the binding width in every case. That is why omission is dangerous rather than merely incomplete: an error that can only err toward doing too much is discovered as an oversubscription, not as a slow run. zeropoint-node's own correction is this theorem in prose — nineteen qubits called a machine's ceiling while four other points sat outside the number.","why":"THE CENTRE OF THE LAW, over readings 31 to 40: measuring ONE point never reports a width too narrow — it reports one too WIDE. The true width is a minimum over the points, and a minimum over a subset is never smaller than the minimum over the whole, so the CPU reading alone is greater than or equal to the binding width in every case. That is why omission is dangerous rather than merely incomplete: an error that can only err toward doing too much is discovered as an oversubscription, not as a slow run. zeropoint-node's own correction is this theorem in prose — nineteen qubits called a machine's ceiling while four other points sat outside the number.","statement":"[(8,1),(8,2),(8,3),(8,4),(8,6),(8,8),(8,12),(8,20),(8,40),(8,128)].all (fun p => (let w := if p.1 <= p.2 then p.1 else p.2; (p.1 >= w) && (p.2 >= w)))","address":"65c0392d-4ef1-8379-9528-8b8f665b3416","skill":"wave","tex":null,"mathml":null,"exercise":null},{"key":"one_point_can_only_overstate_1","name":"THE CENTRE OF THE LAW, over readings 11 to 20: measuring ONE point never reports a width too narrow — it reports one too WIDE. The true width is a minimum over the points, and a minimum over a subset is never smaller than the minimum over the whole, so the CPU reading alone is greater than or equal to the binding width in every case. That is why omission is dangerous rather than merely incomplete: an error that can only err toward doing too much is discovered as an oversubscription, not as a slow run. zeropoint-node's own correction is this theorem in prose — nineteen qubits called a machine's ceiling while four other points sat outside the number.","why":"THE CENTRE OF THE LAW, over readings 11 to 20: measuring ONE point never reports a width too narrow — it reports one too WIDE. The true width is a minimum over the points, and a minimum over a subset is never smaller than the minimum over the whole, so the CPU reading alone is greater than or equal to the binding width in every case. That is why omission is dangerous rather than merely incomplete: an error that can only err toward doing too much is discovered as an oversubscription, not as a slow run. zeropoint-node's own correction is this theorem in prose — nineteen qubits called a machine's ceiling while four other points sat outside the number.","statement":"[(2,1),(2,2),(2,3),(2,4),(2,6),(2,8),(2,12),(2,20),(2,40),(2,128)].all (fun p => (let w := if p.1 <= p.2 then p.1 else p.2; (p.1 >= w) && (p.2 >= w)))","address":"6ef33a89-4692-8450-8e57-45de3db4ddb5","skill":"wave","tex":null,"mathml":null,"exercise":null},{"key":"one_point_can_only_overstate_4","name":"THE CENTRE OF THE LAW, over readings 41 to 50: measuring ONE point never reports a width too narrow — it reports one too WIDE. The true width is a minimum over the points, and a minimum over a subset is never smaller than the minimum over the whole, so the CPU reading alone is greater than or equal to the binding width in every case. That is why omission is dangerous rather than merely incomplete: an error that can only err toward doing too much is discovered as an oversubscription, not as a slow run. zeropoint-node's own correction is this theorem in prose — nineteen qubits called a machine's ceiling while four other points sat outside the number.","why":"THE CENTRE OF THE LAW, over readings 41 to 50: measuring ONE point never reports a width too narrow — it reports one too WIDE. The true width is a minimum over the points, and a minimum over a subset is never smaller than the minimum over the whole, so the CPU reading alone is greater than or equal to the binding width in every case. That is why omission is dangerous rather than merely incomplete: an error that can only err toward doing too much is discovered as an oversubscription, not as a slow run. zeropoint-node's own correction is this theorem in prose — nineteen qubits called a machine's ceiling while four other points sat outside the number.","statement":"[(10,1),(10,2),(10,3),(10,4),(10,6),(10,8),(10,12),(10,20),(10,40),(10,128)].all (fun p => (let w := if p.1 <= p.2 then p.1 else p.2; (p.1 >= w) && (p.2 >= w)))","address":"99c43fa9-b117-8610-ba2f-290f8c3d8d93","skill":"wave","tex":null,"mathml":null,"exercise":null},{"key":"one_point_can_only_overstate_5","name":"THE CENTRE OF THE LAW, over readings 51 to 60: measuring ONE point never reports a width too narrow — it reports one too WIDE. The true width is a minimum over the points, and a minimum over a subset is never smaller than the minimum over the whole, so the CPU reading alone is greater than or equal to the binding width in every case. That is why omission is dangerous rather than merely incomplete: an error that can only err toward doing too much is discovered as an oversubscription, not as a slow run. zeropoint-node's own correction is this theorem in prose — nineteen qubits called a machine's ceiling while four other points sat outside the number.","why":"THE CENTRE OF THE LAW, over readings 51 to 60: measuring ONE point never reports a width too narrow — it reports one too WIDE. The true width is a minimum over the points, and a minimum over a subset is never smaller than the minimum over the whole, so the CPU reading alone is greater than or equal to the binding width in every case. That is why omission is dangerous rather than merely incomplete: an error that can only err toward doing too much is discovered as an oversubscription, not as a slow run. zeropoint-node's own correction is this theorem in prose — nineteen qubits called a machine's ceiling while four other points sat outside the number.","statement":"[(16,1),(16,2),(16,3),(16,4),(16,6),(16,8),(16,12),(16,20),(16,40),(16,128)].all (fun p => (let w := if p.1 <= p.2 then p.1 else p.2; (p.1 >= w) && (p.2 >= w)))","address":"f51634f8-3397-86be-9380-8890908cd888","skill":"wave","tex":null,"mathml":null,"exercise":null},{"key":"one_point_can_only_overstate_6","name":"THE CENTRE OF THE LAW, over readings 61 to 70: measuring ONE point never reports a width too narrow — it reports one too WIDE. The true width is a minimum over the points, and a minimum over a subset is never smaller than the minimum over the whole, so the CPU reading alone is greater than or equal to the binding width in every case. That is why omission is dangerous rather than merely incomplete: an error that can only err toward doing too much is discovered as an oversubscription, not as a slow run. zeropoint-node's own correction is this theorem in prose — nineteen qubits called a machine's ceiling while four other points sat outside the number.","why":"THE CENTRE OF THE LAW, over readings 61 to 70: measuring ONE point never reports a width too narrow — it reports one too WIDE. The true width is a minimum over the points, and a minimum over a subset is never smaller than the minimum over the whole, so the CPU reading alone is greater than or equal to the binding width in every case. That is why omission is dangerous rather than merely incomplete: an error that can only err toward doing too much is discovered as an oversubscription, not as a slow run. zeropoint-node's own correction is this theorem in prose — nineteen qubits called a machine's ceiling while four other points sat outside the number.","statement":"[(32,1),(32,2),(32,3),(32,4),(32,6),(32,8),(32,12),(32,20),(32,40),(32,128)].all (fun p => (let w := if p.1 <= p.2 then p.1 else p.2; (p.1 >= w) && (p.2 >= w)))","address":"4fe696d5-a435-822f-9e05-dd64512d2980","skill":"wave","tex":null,"mathml":null,"exercise":null},{"key":"one_point_can_only_overstate_7","name":"THE CENTRE OF THE LAW, over readings 71 to 80: measuring ONE point never reports a width too narrow — it reports one too WIDE. The true width is a minimum over the points, and a minimum over a subset is never smaller than the minimum over the whole, so the CPU reading alone is greater than or equal to the binding width in every case. That is why omission is dangerous rather than merely incomplete: an error that can only err toward doing too much is discovered as an oversubscription, not as a slow run. zeropoint-node's own correction is this theorem in prose — nineteen qubits called a machine's ceiling while four other points sat outside the number.","why":"THE CENTRE OF THE LAW, over readings 71 to 80: measuring ONE point never reports a width too narrow — it reports one too WIDE. The true width is a minimum over the points, and a minimum over a subset is never smaller than the minimum over the whole, so the CPU reading alone is greater than or equal to the binding width in every case. That is why omission is dangerous rather than merely incomplete: an error that can only err toward doing too much is discovered as an oversubscription, not as a slow run. zeropoint-node's own correction is this theorem in prose — nineteen qubits called a machine's ceiling while four other points sat outside the number.","statement":"[(64,1),(64,2),(64,3),(64,4),(64,6),(64,8),(64,12),(64,20),(64,40),(64,128)].all (fun p => (let w := if p.1 <= p.2 then p.1 else p.2; (p.1 >= w) && (p.2 >= w)))","address":"bb5942f5-df0b-80c8-8893-06f637bcd01f","skill":"wave","tex":null,"mathml":null,"exercise":null},{"key":"one_point_can_only_overstate_0","name":"THE CENTRE OF THE LAW, over readings 1 to 10: measuring ONE point never reports a width too narrow — it reports one too WIDE. The true width is a minimum over the points, and a minimum over a subset is never smaller than the minimum over the whole, so the CPU reading alone is greater than or equal to the binding width in every case. That is why omission is dangerous rather than merely incomplete: an error that can only err toward doing too much is discovered as an oversubscription, not as a slow run. zeropoint-node's own correction is this theorem in prose — nineteen qubits called a machine's ceiling while four other points sat outside the number.","why":"THE CENTRE OF THE LAW, over readings 1 to 10: measuring ONE point never reports a width too narrow — it reports one too WIDE. The true width is a minimum over the points, and a minimum over a subset is never smaller than the minimum over the whole, so the CPU reading alone is greater than or equal to the binding width in every case. That is why omission is dangerous rather than merely incomplete: an error that can only err toward doing too much is discovered as an oversubscription, not as a slow run. zeropoint-node's own correction is this theorem in prose — nineteen qubits called a machine's ceiling while four other points sat outside the number.","statement":"[(1,1),(1,2),(1,3),(1,4),(1,6),(1,8),(1,12),(1,20),(1,40),(1,128)].all (fun p => (let w := if p.1 <= p.2 then p.1 else p.2; (p.1 >= w) && (p.2 >= w)))","address":"acde4232-20d2-800c-bba9-fd4d9df12daf","skill":"wave","tex":null,"mathml":null,"exercise":null},{"key":"width_is_the_binding_point_1","name":"THE WIDTH IS THE SMALLER POINT, over readings 11 to 20. A fan-out may run as wide as the cores allow and as wide as the memory allows, so it may run as wide as the SMALLER of the two and no wider. Stated as the minimum rather than as a rule about which point usually wins, because which one wins is a fact about a machine and this is not.","why":"THE WIDTH IS THE SMALLER POINT, over readings 11 to 20. A fan-out may run as wide as the cores allow and as wide as the memory allows, so it may run as wide as the SMALLER of the two and no wider. Stated as the minimum rather than as a rule about which point usually wins, because which one wins is a fact about a machine and this is not.","statement":"[(2,1),(2,2),(2,3),(2,4),(2,6),(2,8),(2,12),(2,20),(2,40),(2,128)].all (fun p => (let w := if p.1 <= p.2 then p.1 else p.2; (w <= p.1) && (w <= p.2) && ((w == p.1) || (w == p.2))))","address":"8d3d443b-fa8e-8b7b-80b4-0d59fad1f8ab","skill":"wave","tex":null,"mathml":null,"exercise":null},{"key":"width_is_the_binding_point_0","name":"THE WIDTH IS THE SMALLER POINT, over readings 1 to 10. A fan-out may run as wide as the cores allow and as wide as the memory allows, so it may run as wide as the SMALLER of the two and no wider. Stated as the minimum rather than as a rule about which point usually wins, because which one wins is a fact about a machine and this is not.","why":"THE WIDTH IS THE SMALLER POINT, over readings 1 to 10. A fan-out may run as wide as the cores allow and as wide as the memory allows, so it may run as wide as the SMALLER of the two and no wider. Stated as the minimum rather than as a rule about which point usually wins, because which one wins is a fact about a machine and this is not.","statement":"[(1,1),(1,2),(1,3),(1,4),(1,6),(1,8),(1,12),(1,20),(1,40),(1,128)].all (fun p => (let w := if p.1 <= p.2 then p.1 else p.2; (w <= p.1) && (w <= p.2) && ((w == p.1) || (w == p.2))))","address":"16b1934b-71f5-8ff6-af78-e5f24bb6d7b7","skill":"wave","tex":null,"mathml":null,"exercise":null},{"key":"width_is_the_binding_point_2","name":"THE WIDTH IS THE SMALLER POINT, over readings 21 to 30. A fan-out may run as wide as the cores allow and as wide as the memory allows, so it may run as wide as the SMALLER of the two and no wider. Stated as the minimum rather than as a rule about which point usually wins, because which one wins is a fact about a machine and this is not.","why":"THE WIDTH IS THE SMALLER POINT, over readings 21 to 30. A fan-out may run as wide as the cores allow and as wide as the memory allows, so it may run as wide as the SMALLER of the two and no wider. Stated as the minimum rather than as a rule about which point usually wins, because which one wins is a fact about a machine and this is not.","statement":"[(4,1),(4,2),(4,3),(4,4),(4,6),(4,8),(4,12),(4,20),(4,40),(4,128)].all (fun p => (let w := if p.1 <= p.2 then p.1 else p.2; (w <= p.1) && (w <= p.2) && ((w == p.1) || (w == p.2))))","address":"6a5355a2-f954-8a06-9b63-fc8c4a065eb5","skill":"wave","tex":null,"mathml":null,"exercise":null},{"key":"width_is_the_binding_point_3","name":"THE WIDTH IS THE SMALLER POINT, over readings 31 to 40. A fan-out may run as wide as the cores allow and as wide as the memory allows, so it may run as wide as the SMALLER of the two and no wider. Stated as the minimum rather than as a rule about which point usually wins, because which one wins is a fact about a machine and this is not.","why":"THE WIDTH IS THE SMALLER POINT, over readings 31 to 40. A fan-out may run as wide as the cores allow and as wide as the memory allows, so it may run as wide as the SMALLER of the two and no wider. Stated as the minimum rather than as a rule about which point usually wins, because which one wins is a fact about a machine and this is not.","statement":"[(8,1),(8,2),(8,3),(8,4),(8,6),(8,8),(8,12),(8,20),(8,40),(8,128)].all (fun p => (let w := if p.1 <= p.2 then p.1 else p.2; (w <= p.1) && (w <= p.2) && ((w == p.1) || (w == p.2))))","address":"ddae825b-ed1c-8dee-96d6-378cf472edee","skill":"wave","tex":null,"mathml":null,"exercise":null},{"key":"width_is_the_binding_point_4","name":"THE WIDTH IS THE SMALLER POINT, over readings 41 to 50. A fan-out may run as wide as the cores allow and as wide as the memory allows, so it may run as wide as the SMALLER of the two and no wider. Stated as the minimum rather than as a rule about which point usually wins, because which one wins is a fact about a machine and this is not.","why":"THE WIDTH IS THE SMALLER POINT, over readings 41 to 50. A fan-out may run as wide as the cores allow and as wide as the memory allows, so it may run as wide as the SMALLER of the two and no wider. Stated as the minimum rather than as a rule about which point usually wins, because which one wins is a fact about a machine and this is not.","statement":"[(10,1),(10,2),(10,3),(10,4),(10,6),(10,8),(10,12),(10,20),(10,40),(10,128)].all (fun p => (let w := if p.1 <= p.2 then p.1 else p.2; (w <= p.1) && (w <= p.2) && ((w == p.1) || (w == p.2))))","address":"31ee162f-0cb9-8b87-a847-c050838b573e","skill":"wave","tex":null,"mathml":null,"exercise":null},{"key":"width_is_the_binding_point_5","name":"THE WIDTH IS THE SMALLER POINT, over readings 51 to 60. A fan-out may run as wide as the cores allow and as wide as the memory allows, so it may run as wide as the SMALLER of the two and no wider. Stated as the minimum rather than as a rule about which point usually wins, because which one wins is a fact about a machine and this is not.","why":"THE WIDTH IS THE SMALLER POINT, over readings 51 to 60. A fan-out may run as wide as the cores allow and as wide as the memory allows, so it may run as wide as the SMALLER of the two and no wider. Stated as the minimum rather than as a rule about which point usually wins, because which one wins is a fact about a machine and this is not.","statement":"[(16,1),(16,2),(16,3),(16,4),(16,6),(16,8),(16,12),(16,20),(16,40),(16,128)].all (fun p => (let w := if p.1 <= p.2 then p.1 else p.2; (w <= p.1) && (w <= p.2) && ((w == p.1) || (w == p.2))))","address":"79b6ca4b-3ffc-8fed-a3eb-dc7134a144e6","skill":"wave","tex":null,"mathml":null,"exercise":null},{"key":"width_is_the_binding_point_6","name":"THE WIDTH IS THE SMALLER POINT, over readings 61 to 70. A fan-out may run as wide as the cores allow and as wide as the memory allows, so it may run as wide as the SMALLER of the two and no wider. Stated as the minimum rather than as a rule about which point usually wins, because which one wins is a fact about a machine and this is not.","why":"THE WIDTH IS THE SMALLER POINT, over readings 61 to 70. A fan-out may run as wide as the cores allow and as wide as the memory allows, so it may run as wide as the SMALLER of the two and no wider. Stated as the minimum rather than as a rule about which point usually wins, because which one wins is a fact about a machine and this is not.","statement":"[(32,1),(32,2),(32,3),(32,4),(32,6),(32,8),(32,12),(32,20),(32,40),(32,128)].all (fun p => (let w := if p.1 <= p.2 then p.1 else p.2; (w <= p.1) && (w <= p.2) && ((w == p.1) || (w == p.2))))","address":"2fc9c5ac-f7e6-8c34-b823-5afc45720916","skill":"wave","tex":null,"mathml":null,"exercise":null},{"key":"width_is_the_binding_point_7","name":"THE WIDTH IS THE SMALLER POINT, over readings 71 to 80. A fan-out may run as wide as the cores allow and as wide as the memory allows, so it may run as wide as the SMALLER of the two and no wider. Stated as the minimum rather than as a rule about which point usually wins, because which one wins is a fact about a machine and this is not.","why":"THE WIDTH IS THE SMALLER POINT, over readings 71 to 80. A fan-out may run as wide as the cores allow and as wide as the memory allows, so it may run as wide as the SMALLER of the two and no wider. Stated as the minimum rather than as a rule about which point usually wins, because which one wins is a fact about a machine and this is not.","statement":"[(64,1),(64,2),(64,3),(64,4),(64,6),(64,8),(64,12),(64,20),(64,40),(64,128)].all (fun p => (let w := if p.1 <= p.2 then p.1 else p.2; (w <= p.1) && (w <= p.2) && ((w == p.1) || (w == p.2))))","address":"3f49eacd-5b6e-8565-9c34-f684cb705554","skill":"wave","tex":null,"mathml":null,"exercise":null},{"key":"a_richer_point_never_narrows_the_width_1","name":"MONOTONE IN EVERY POINT, over readings 11 to 20: adding memory, or adding cores, never makes the admitted width smaller. A capacity function that could narrow when a machine grew would be unusable — an operator adding memory to go faster would have to check whether it had gone slower — and the minimum has this property by construction, which is the argument for choosing a minimum rather than a formula.","why":"MONOTONE IN EVERY POINT, over readings 11 to 20: adding memory, or adding cores, never makes the admitted width smaller. A capacity function that could narrow when a machine grew would be unusable — an operator adding memory to go faster would have to check whether it had gone slower — and the minimum has this property by construction, which is the argument for choosing a minimum rather than a formula.","statement":"[(2,1),(2,2),(2,3),(2,4),(2,6),(2,8),(2,12),(2,20),(2,40),(2,128)].all (fun p => (let w := if p.1 <= p.2 then p.1 else p.2; let wc := if p.1 + 1 <= p.2 then p.1 + 1 else p.2; let wm := if p.1 <= p.2 + 1 then p.1 else p.2 + 1; (wc >= w) && (wm >= w)))","address":"07ef7fd9-cd6e-8cb3-a466-ec91f93e2403","skill":"wave","tex":null,"mathml":null,"exercise":null},{"key":"a_richer_point_never_narrows_the_width_3","name":"MONOTONE IN EVERY POINT, over readings 31 to 40: adding memory, or adding cores, never makes the admitted width smaller. A capacity function that could narrow when a machine grew would be unusable — an operator adding memory to go faster would have to check whether it had gone slower — and the minimum has this property by construction, which is the argument for choosing a minimum rather than a formula.","why":"MONOTONE IN EVERY POINT, over readings 31 to 40: adding memory, or adding cores, never makes the admitted width smaller. A capacity function that could narrow when a machine grew would be unusable — an operator adding memory to go faster would have to check whether it had gone slower — and the minimum has this property by construction, which is the argument for choosing a minimum rather than a formula.","statement":"[(8,1),(8,2),(8,3),(8,4),(8,6),(8,8),(8,12),(8,20),(8,40),(8,128)].all (fun p => (let w := if p.1 <= p.2 then p.1 else p.2; let wc := if p.1 + 1 <= p.2 then p.1 + 1 else p.2; let wm := if p.1 <= p.2 + 1 then p.1 else p.2 + 1; (wc >= w) && (wm >= w)))","address":"0f7d6762-cb20-88ee-9359-4ce18a23aecd","skill":"wave","tex":null,"mathml":null,"exercise":null},{"key":"a_richer_point_never_narrows_the_width_0","name":"MONOTONE IN EVERY POINT, over readings 1 to 10: adding memory, or adding cores, never makes the admitted width smaller. A capacity function that could narrow when a machine grew would be unusable — an operator adding memory to go faster would have to check whether it had gone slower — and the minimum has this property by construction, which is the argument for choosing a minimum rather than a formula.","why":"MONOTONE IN EVERY POINT, over readings 1 to 10: adding memory, or adding cores, never makes the admitted width smaller. A capacity function that could narrow when a machine grew would be unusable — an operator adding memory to go faster would have to check whether it had gone slower — and the minimum has this property by construction, which is the argument for choosing a minimum rather than a formula.","statement":"[(1,1),(1,2),(1,3),(1,4),(1,6),(1,8),(1,12),(1,20),(1,40),(1,128)].all (fun p => (let w := if p.1 <= p.2 then p.1 else p.2; let wc := if p.1 + 1 <= p.2 then p.1 + 1 else p.2; let wm := if p.1 <= p.2 + 1 then p.1 else p.2 + 1; (wc >= w) && (wm >= w)))","address":"42080f51-2f77-80bb-b0a2-b869f4b8e8d8","skill":"wave","tex":null,"mathml":null,"exercise":null},{"key":"a_richer_point_never_narrows_the_width_2","name":"MONOTONE IN EVERY POINT, over readings 21 to 30: adding memory, or adding cores, never makes the admitted width smaller. A capacity function that could narrow when a machine grew would be unusable — an operator adding memory to go faster would have to check whether it had gone slower — and the minimum has this property by construction, which is the argument for choosing a minimum rather than a formula.","why":"MONOTONE IN EVERY POINT, over readings 21 to 30: adding memory, or adding cores, never makes the admitted width smaller. A capacity function that could narrow when a machine grew would be unusable — an operator adding memory to go faster would have to check whether it had gone slower — and the minimum has this property by construction, which is the argument for choosing a minimum rather than a formula.","statement":"[(4,1),(4,2),(4,3),(4,4),(4,6),(4,8),(4,12),(4,20),(4,40),(4,128)].all (fun p => (let w := if p.1 <= p.2 then p.1 else p.2; let wc := if p.1 + 1 <= p.2 then p.1 + 1 else p.2; let wm := if p.1 <= p.2 + 1 then p.1 else p.2 + 1; (wc >= w) && (wm >= w)))","address":"bdf9b98b-44f4-8913-8b6b-ae4d0dbe61ee","skill":"wave","tex":null,"mathml":null,"exercise":null},{"key":"a_richer_point_never_narrows_the_width_4","name":"MONOTONE IN EVERY POINT, over readings 41 to 50: adding memory, or adding cores, never makes the admitted width smaller. A capacity function that could narrow when a machine grew would be unusable — an operator adding memory to go faster would have to check whether it had gone slower — and the minimum has this property by construction, which is the argument for choosing a minimum rather than a formula.","why":"MONOTONE IN EVERY POINT, over readings 41 to 50: adding memory, or adding cores, never makes the admitted width smaller. A capacity function that could narrow when a machine grew would be unusable — an operator adding memory to go faster would have to check whether it had gone slower — and the minimum has this property by construction, which is the argument for choosing a minimum rather than a formula.","statement":"[(10,1),(10,2),(10,3),(10,4),(10,6),(10,8),(10,12),(10,20),(10,40),(10,128)].all (fun p => (let w := if p.1 <= p.2 then p.1 else p.2; let wc := if p.1 + 1 <= p.2 then p.1 + 1 else p.2; let wm := if p.1 <= p.2 + 1 then p.1 else p.2 + 1; (wc >= w) && (wm >= w)))","address":"4da5a78d-c8f6-8c58-b20f-957a87f73e0d","skill":"wave","tex":null,"mathml":null,"exercise":null},{"key":"a_richer_point_never_narrows_the_width_5","name":"MONOTONE IN EVERY POINT, over readings 51 to 60: adding memory, or adding cores, never makes the admitted width smaller. A capacity function that could narrow when a machine grew would be unusable — an operator adding memory to go faster would have to check whether it had gone slower — and the minimum has this property by construction, which is the argument for choosing a minimum rather than a formula.","why":"MONOTONE IN EVERY POINT, over readings 51 to 60: adding memory, or adding cores, never makes the admitted width smaller. A capacity function that could narrow when a machine grew would be unusable — an operator adding memory to go faster would have to check whether it had gone slower — and the minimum has this property by construction, which is the argument for choosing a minimum rather than a formula.","statement":"[(16,1),(16,2),(16,3),(16,4),(16,6),(16,8),(16,12),(16,20),(16,40),(16,128)].all (fun p => (let w := if p.1 <= p.2 then p.1 else p.2; let wc := if p.1 + 1 <= p.2 then p.1 + 1 else p.2; let wm := if p.1 <= p.2 + 1 then p.1 else p.2 + 1; (wc >= w) && (wm >= w)))","address":"6426e005-9943-8ab5-b75b-abed16dd5419","skill":"wave","tex":null,"mathml":null,"exercise":null},{"key":"a_richer_point_never_narrows_the_width_6","name":"MONOTONE IN EVERY POINT, over readings 61 to 70: adding memory, or adding cores, never makes the admitted width smaller. A capacity function that could narrow when a machine grew would be unusable — an operator adding memory to go faster would have to check whether it had gone slower — and the minimum has this property by construction, which is the argument for choosing a minimum rather than a formula.","why":"MONOTONE IN EVERY POINT, over readings 61 to 70: adding memory, or adding cores, never makes the admitted width smaller. A capacity function that could narrow when a machine grew would be unusable — an operator adding memory to go faster would have to check whether it had gone slower — and the minimum has this property by construction, which is the argument for choosing a minimum rather than a formula.","statement":"[(32,1),(32,2),(32,3),(32,4),(32,6),(32,8),(32,12),(32,20),(32,40),(32,128)].all (fun p => (let w := if p.1 <= p.2 then p.1 else p.2; let wc := if p.1 + 1 <= p.2 then p.1 + 1 else p.2; let wm := if p.1 <= p.2 + 1 then p.1 else p.2 + 1; (wc >= w) && (wm >= w)))","address":"8b463831-80b4-8d6a-8964-622f034af76a","skill":"wave","tex":null,"mathml":null,"exercise":null},{"key":"a_richer_point_never_narrows_the_width_7","name":"MONOTONE IN EVERY POINT, over readings 71 to 80: adding memory, or adding cores, never makes the admitted width smaller. A capacity function that could narrow when a machine grew would be unusable — an operator adding memory to go faster would have to check whether it had gone slower — and the minimum has this property by construction, which is the argument for choosing a minimum rather than a formula.","why":"MONOTONE IN EVERY POINT, over readings 71 to 80: adding memory, or adding cores, never makes the admitted width smaller. A capacity function that could narrow when a machine grew would be unusable — an operator adding memory to go faster would have to check whether it had gone slower — and the minimum has this property by construction, which is the argument for choosing a minimum rather than a formula.","statement":"[(64,1),(64,2),(64,3),(64,4),(64,6),(64,8),(64,12),(64,20),(64,40),(64,128)].all (fun p => (let w := if p.1 <= p.2 then p.1 else p.2; let wc := if p.1 + 1 <= p.2 then p.1 + 1 else p.2; let wm := if p.1 <= p.2 + 1 then p.1 else p.2 + 1; (wc >= w) && (wm >= w)))","address":"f4a65604-24b3-89ea-b044-2d89794e10ec","skill":"wave","tex":null,"mathml":null,"exercise":null},{"key":"naming_the_binding_point_is_total","name":"EVERY READING NAMES A WINNER. For each pair the width equals the CPU point or the memory point — never a third number — so a report can always say WHICH point set the width. A width that matched neither would be a computed figure with no measurement behind it, which is exactly the kind of number this ledger exists to refuse.","why":"EVERY READING NAMES A WINNER. For each pair the width equals the CPU point or the memory point — never a third number — so a report can always say WHICH point set the width. A width that matched neither would be a computed figure with no measurement behind it, which is exactly the kind of number this ledger exists to refuse.","statement":"[(1,1),(1,2),(1,3),(1,4),(1,6),(1,8),(1,12),(1,20),(1,40),(1,128),(2,1),(2,2),(2,3),(2,4),(2,6),(2,8),(2,12),(2,20),(2,40),(2,128),(4,1),(4,2),(4,3),(4,4),(4,6),(4,8),(4,12),(4,20),(4,40),(4,128),(8,1),(8,2),(8,3),(8,4),(8,6),(8,8),(8,12),(8,20),(8,40),(8,128)].all (fun p => (let w := if p.1 <= p.2 then p.1 else p.2; (w == p.1) || (w == p.2)))","address":"fb710bbd-05f7-8385-a02c-64588901419c","skill":"wave","tex":null,"mathml":null,"exercise":null},{"key":"the_width_is_never_below_one","name":"A FLOOR, BECAUSE ZERO LANES IS NOT A MEASUREMENT BUT A STOP. Every reading here admits at least one lane, so a host too small for the reserve still runs the work serially rather than reporting a fan-out of nothing. A capacity that can answer zero turns a narrow machine into a halted one, and the difference between slow and stopped is the difference between a result and none.","why":"A FLOOR, BECAUSE ZERO LANES IS NOT A MEASUREMENT BUT A STOP. Every reading here admits at least one lane, so a host too small for the reserve still runs the work serially rather than reporting a fan-out of nothing. A capacity that can answer zero turns a narrow machine into a halted one, and the difference between slow and stopped is the difference between a result and none.","statement":"[(1,1),(1,2),(1,3),(1,4),(1,6),(1,8),(1,12),(1,20),(1,40),(1,128),(2,1),(2,2),(2,3),(2,4),(2,6),(2,8),(2,12),(2,20),(2,40),(2,128),(4,1),(4,2),(4,3),(4,4),(4,6),(4,8),(4,12),(4,20),(4,40),(4,128),(8,1),(8,2),(8,3),(8,4),(8,6),(8,8),(8,12),(8,20),(8,40),(8,128)].all (fun p => (if p.1 <= p.2 then p.1 else p.2) >= 1)","address":"e3153671-2f7e-84fa-8037-6841e8411527","skill":"wave","tex":null,"mathml":null,"exercise":null},{"key":"monitoring_the_points_covers_every_crack_by_architecture","name":"THE CAPTAIN, 2026-09-07: \"only monitoring hardware is enough to precisely know where the cracks are. 100% coverage by architecture.\" AND THE COVERAGE IS STRUCTURAL, NOT A LIST. Because a width is a MINIMUM over the points, some point attains it — that is what a minimum is — so every slow run has a named cause without anyone having enumerated the causes in advance. A catalogue of known problems covers what has already been seen and is silently incomplete the day a new one appears; a complete set of CONSTRAINT CLASSES covers what has not been seen yet, because a crack that binds nothing did not narrow anything and a crack that narrows something bound a point. Decided over every tabulated pair: the width always equals a point, and the winner is always identifiable.","why":"THE CAPTAIN, 2026-09-07: \"only monitoring hardware is enough to precisely know where the cracks are. 100% coverage by architecture.\" AND THE COVERAGE IS STRUCTURAL, NOT A LIST. Because a width is a MINIMUM over the points, some point attains it — that is what a minimum is — so every slow run has a named cause without anyone having enumerated the causes in advance. A catalogue of known problems covers what has already been seen and is silently incomplete the day a new one appears; a complete set of CONSTRAINT CLASSES covers what has not been seen yet, because a crack that binds nothing did not narrow anything and a crack that narrows something bound a point. Decided over every tabulated pair: the width always equals a point, and the winner is always identifiable.","statement":"[(1,1),(1,2),(1,3),(1,4),(1,6),(1,8),(1,12),(1,20),(1,40),(1,128),(2,1),(2,2),(2,3),(2,4),(2,6),(2,8),(2,12),(2,20),(2,40),(2,128),(4,1),(4,2),(4,3),(4,4),(4,6),(4,8),(4,12),(4,20),(4,40),(4,128),(8,1),(8,2),(8,3),(8,4),(8,6),(8,8),(8,12),(8,20),(8,40),(8,128)].all (fun p => (let w := if p.1 <= p.2 then p.1 else p.2; ((w == p.1) || (w == p.2)) && (w <= p.1) && (w <= p.2)))","address":"934d143a-4395-835b-aac4-92fb9584f981","skill":"wave","tex":null,"mathml":null,"exercise":null},{"key":"the_diagnosis_and_the_prescription_are_the_same_point","name":"\"SAME LOGIC SOLVES THE PROBLEMS\" — and it is the same arithmetic, not an analogy. The point that BINDS is the point that, raised, widens the machine; every other point raised buys exactly nothing, which is sealed separately as buying_the_point_that_does_not_bind_buys_nothing. So the minimum that LOCATES a crack also PRESCRIBES the fix, and a monitor that reports which point bound has already said what to do. Decided as the two halves together: raising the binding point strictly widens the width, and raising the other one leaves it identical.","why":"\"SAME LOGIC SOLVES THE PROBLEMS\" — and it is the same arithmetic, not an analogy. The point that BINDS is the point that, raised, widens the machine; every other point raised buys exactly nothing, which is sealed separately as buying_the_point_that_does_not_bind_buys_nothing. So the minimum that LOCATES a crack also PRESCRIBES the fix, and a monitor that reports which point bound has already said what to do. Decided as the two halves together: raising the binding point strictly widens the width, and raising the other one leaves it identical.","statement":"[(1,1),(1,2),(1,3),(1,4),(1,6),(1,8),(1,12),(1,20),(1,40),(1,128),(2,1),(2,2),(2,3),(2,4),(2,6),(2,8),(2,12),(2,20),(2,40),(2,128),(4,1),(4,2),(4,3),(4,4),(4,6),(4,8),(4,12),(4,20),(4,40),(4,128),(8,1),(8,2),(8,3),(8,4),(8,6),(8,8),(8,12),(8,20),(8,40),(8,128)].all (fun p => (let w := if p.1 <= p.2 then p.1 else p.2; let other := if p.1 <= p.2 then (if p.1 <= p.2 + 1 then p.1 else p.2 + 1) else (if p.1 + 1 <= p.2 then p.1 + 1 else p.2); other == w))","address":"b34b515a-6d1a-8f52-a7c9-af2bc933b685","skill":"wave","tex":null,"mathml":null,"exercise":null}],"exercises":1}
