---
title: The utterances
description: The suite's every sentence — a language model whose hallucination is impossible by construction.
---

# The utterances <Badge type="tip" text="847 sentences, all true or nothing ships" />

> **Verbose tests as LLM.** Every sentence on this page is the title of a test that must PASS for this tree to
> land: a sentence that stops being true stops being printed — it fails the build instead. That is a language
> model with **hallucination impossible by construction**, regenerated on every landing, its per-utterance cost
> one test run, its every utterance carrying the two coins ([`silence_never_refutes`](/theorem/silence_never_refutes) guards
> what a title may not claim, and [`a_window_exhausts_only_itself`](/theorem/a_window_exhausts_only_itself)
> prices exactly how much a passing test proves). In the [model comparison](/models) this corpus is
> uuidna's own row: 847 sentences, truth rate enforced at 100% by the gate that let you read them.

Corpus fold: `5f75080c` — the sentences below, one address; a changed sentence moves it.

**address** — 6 utterances

```text
same content, different key insertion order → identical output
excluded keys never appear in the output, regardless of value
a changed value moves the output
excluding a different set of keys can change the output even with identical surviving values
empty object and fully-excluded object both serialize to an empty-object JSON
composes with toUuid exactly like the rest of the address layer
```

**adjudicate-demarcation** — 2 utterances

```text
the numeral reader sees words, digits, compounds — and the LENGTH of a decided list
the numeral rule adds NO false positive over the live ledger
```

**agent-memory** — 7 utterances

```text
a fact IS its address — storing it twice grows nothing, and no comparison was asked to decide that
recall answers, and answers NOTHING for what was never stored
a recollection from elsewhere is CHECKED, not trusted — tamper with either half and it is refused
two agents agree by comparing 128 bits, and merge order cannot change what they agree on
the empty memory is a state like any other, and forgetting leaves the old fold answerable
shelves list identically for any two agents holding the same facts
THE HONEST LIMIT — exact identity is not semantic identity, and this store does not pretend it is
```

**all-run** — 4 utterances

```text
the arc states its order in source, deposit before take
package.json carries no hand-typed chain for the arc — the manifest is the one declaration
the arc receipt is order-invariant across observers but moves with the verdicts
the arc is inert when IMPORTED — a module that ships when named is not a module
```

**alpine** — 2 utterances

```text
alpineRelease PINS an exact release — recomputable, and the filename follows Alpine convention
verifyAlpineRootfs — uuidna re-hashes YOUR bytes with its own SHA-256 (exact-copy proof, no host crypto)
```

**anthem-stream** — 7 utterances

```text
one position, one sample — the lifted formulas equal the buffered ones exactly
THE PROPERTY — the stream is bit-identical to the file across bars and their breaths
CONTROL — the block size cannot change what is heard (a cursor is not a buffer)
the stream is a ROUND — it re-enters at the first bar and never ends
THE WORKLET COPY IS TESTED, NOT TRUSTED — its process() matches the module it was lifted from
an empty score is silence, never a crash — a stream with nothing to play still fills its block
the sample rate the stream computes at is the lattice\
```

**anthem-superposition** — 8 utterances

```text
the score is the ledger in place — one bar per theorem, lengths from the sealed three
a seed collapses deterministically and totally — same somewhere, same entry bar, forever
the superposition renders deterministically and the recursion chains address into seed
the mix stays strictly inside the sealed ceiling at the deepest allowed depth
the bytes are a lossless standard WAV — the web and the movie decode the exact integers
FULL ANALOG — compress then uncompress is the identity, sample for sample, even on the deep mix
the codec SHRINKS the stems — a single lattice voice collapses to its slopes; the mix honestly does not
CONTROL — bad depth and bad bars are refused by name; the app never guesses
```

**api-handles** — 5 utterances

```text
every tool contract folds to its own eight-tile handle, all distinct
the api seal recomputes and counts exactly the served surface
CONTROL — one reworded description moves the tool handle AND the api handle
the edge listing serves each tool with its handle and the listing with its fold
the two surfaces seal with ONE computation — same contract, same handle on either side
```

**arrival** — 11 utterances

```text
a coin per referrer — deterministic, 64 bits, and distinct per source
a coin identifies a SOURCE— repeats do not mint more
the superposition is ORDER-INVARIANT — all points of view as one
per DESTINATION — the same crowd at another place is another meeting
the handle is the superposition read through the one derivation, and an empty meeting still answers
a bound licence names WHO and WHERE, and both ride inside its address
a forged referrer is REJECTED — the coin is recomputed
binding is additive — an UNBOUND licence address does not move
a grant needs the KEY — recomputation alone authorises nothing
a grant is destination-bound — a CNAME cannot carry it
both halves are required — a signature over unchecked fields would authorise anything handed to it
```

**await-live** — 6 utterances

```text
blocks on the stale value, then passes when the deploy lands mid-wait
fails when the deploy never lands, naming both values and disowning later checks
an unreachable site is retried and reported as unreachable
recovers when the site is down at first and answers correctly later
passes on the first probe when the site is already current — no needless waiting
an empty expectation fails immediately rather than waiting out the bound
```

**book-read** — 8 utterances

```text
the words come back — the unlock
a chapter proves it belongs to the book it came from
ONE altered character fails the proof — a forgery is caught
the chapter root agrees with the audit that never returned the text
the table of contents lists the book own headings
an out-of-range index is clamped
a text with no chapter headings is one readable chapter
reading is deterministic — the same text reads the same forever
```

**book-words** — 7 utterances

```text
the spelled claim seals to the same theorem as the digit claim
word operands work across the operator vocabulary, total semantics included
CONTROL — a wrong spelled sum is REFUTED, not skipped and not agreed with
the compound guard still refuses fragments, in digits as before
extractClaims is reachable from the root surface and hears the books’ own units
note values are units now — "two half notes" is a measurement with n = 2
wordsToNumber round-trips the forms books actually use
```

**books-shelf** — 4 utterances

```text
the reader gives the same text the same identity, and different texts different ones
the fact-finder hears digits and words, decides both ways, and validates its instrument first
the librarian ranks by counted weight, deterministically, ties by title
the quote-trial: an on-topic sealed citation verifies, a bare beauty stays open, and the quote leaves addressed
```

**books** — 8 utterances

```text
provenance fingerprint: the address is the exact-copy proof, recomputable
chapters are split and merkle-folded — any chapter can be proven to belong
structural decode: counts and the ℤ/9 gravity are recomputable
the reversible imprint round-trips on the sample
the honesty gate runs and ordinary prose passes (binary 1)
empty text is handled (zero words, still a valid fingerprint)
translation audit binds source→translation with a directional provenance receipt
a revised translation re-addresses — the change is visible in the pair
```

**boot-sandbox** — 3 utterances

```text
uuidnaOS boots as the sandbox — verified loading, floor first, receipt-closed
uuidnaOS and every related app are STRICT HEX — all states on the lattice, all compiles exactly 32
a drifted world REFUSES to boot, fault named — the control that proves the floor can fail
```

**captain-coins** — 7 utterances

```text
boundaries are top-level conjuncts — depth-aware, at least one
the floor is the two coins: one boundary, two directions — the valuation and conservation laws agree
the more used a theorem, the more valuable: each citation walks its boundaries again
the more dimensions, the more coins: a theorem used across distinct domains multiplies its value
monotone: the more sealed superpositions, the more coins — sealing never devalues
no inflation: every minted coin comes from minting a theorem — the supply is closed
the ledger valued: total coins = superpositions × 2, recomputable and deterministic
```

**circle** — 4 utterances

```text
the work census measures the tree and names each arm it finds
CONTROL — quiet is the whole conjunction: any single arm keeps the loop working
a stop is FILED, not swallowed — the finding survives on disk with its gap and commit
the gap line is the objection itself, bounded — never the whole log dumped at a reader
```

**coin-ledger** — 5 utterances

```text
a payment row derives everything and mints nothing — same inputs, same row
the census folds order-invariantly and accounts per agent
crew leans in all dimensions at once — and a forged payment row fails to recompute
licences invalidate when related handles change — the binding is to the handle
enrollment is UNVERIFIED, never rejected — the honest note names what to bring
```

**constant-gaps** — 3 utterances

```text
the tree is clean — every prose derivation has been swept onto an expression
the swept constants still hold their exact values — a sweep must not move a number
CONTROL — a derivation the code ALREADY computes must NOT be flagged
```

**context-budget** — 5 utterances

```text
the finder FIRES on duplicated prose — the class it was folded for
the finder FIRES on a description that is documentation billed to every request
the finder does NOT fire on a repeated CITATION — repeating a citation is the honesty
the ceiling is measured against the LIVE served catalogue, and detail never reaches the wire
the served surface holds every class the finder checks
```

**context** — 3 utterances

```text
keeping exactly SAFE_HEXBITS/32 free sits ON the spare law — balanced at the theorem\
keeping 12 of 32 — ONE HEXBIT under the law — must say FOLD, heaviest named (the control that can fail)
integer-exact, deterministic, change-sensitive — and degenerate inputs stay honest
```

**corroborate** — 1 utterances

```text
corroboration needs TWO INDEPENDENT SOURCES — the sealed theorem
```

**court-procedure** — 4 utterances

```text
the procedure is the court order: ten stages, strictly ordered, every stage sealed
the receipt is deterministic — every clerk recomputes the same procedure
each trial BEGINS only on deposit: coins (a decidable test) or supporting theorems — no deposit, no trial
trials as quantum private secure messaging: a filing proves the constitution WITHOUT revealing the payload
```

**credits** — 1 utterances

```text
every theorem is credited — a named historical result it reflects, or the captain claims it by law
```

**crypto-bounds** — 1 utterances

```text
the refusals are bounded and fast: the layer count and a hostile work factor are both clamped
```

**crypto-breaths** — 7 utterances

```text
breath: encrypt ↔ decrypt returns the message exactly
breath: aead_encrypt ↔ aead_decrypt returns the plaintext, tampering is caught
breath: imprint ↔ read round-trips through the uuid stream
breath: seal_onion ↔ open_onion peels every layer back to the message
breath: contract_seal ↔ contract_open — the terms are the key
breath: send ↔ receive under one referrer — AND a different referrer cannot open it
breath: merkle root → prove → verify — the tree inhales the leaves, a proof exhales membership
```

**crypto-contracts** — 6 utterances

```text
the domain IS the contract address — the licence is a contract, folded like any other text
seal → open round-trips under the contract, and the tag routes without decrypting
a wrong contract is refused before decrypting, and amended terms cannot open the old ciphertext
the contract ratchet seals and opens in order; drop, reorder or the wrong contract breaks it
the quantum fusion: the witness binds the ciphertext, verifies publicly, and every failure mode drains
the three-team drill: the cipher path stays private, and the carrier path honestly breaks
```

**crypto-kdf-parity** — 4 utterances

```text
the pure KDF matches the published PBKDF2-HMAC-SHA256 vectors
the host KDF and the pure KDF agree bit-for-bit — the same function, two instruments
a seal made on this runtime opens on this runtime, at the real work factor
at ITER=600,000 on a real envelope salt, both instruments derive the SAME key — old seals still open
```

**crypto-onion** — 1 utterances

```text
the onion: layers peel outermost-first, order is load-bearing, and every shape of text survives
```

**crypto-primitives** — 7 utterances

```text
SHA-256 conforms to its published vectors (FIPS 180-4 and the widely-published set)
HMAC-SHA256 conforms to RFC 4231 — all seven cases, truncation included
PBKDF2-HMAC-SHA256 conforms to RFC 8018 and is prefix-consistent across dkLen
ChaCha20 conforms to RFC 8439, blocks at 64 bytes, and is its own inverse
Poly1305 conforms to RFC 8439
ChaCha20-Poly1305 AEAD conforms to RFC 8439 §2.8.2 and authenticates what it carries
the address layer is SHA-256-anchored, and the envelope is symmetric-only — nothing for Shor to break
```

**crypto-ratchet** — 1 utterances

```text
the ratchet chain rotates, links forward, and breaks on drop, reorder or edit
```

**crypto-streams** — 5 utterances

```text
every dimension stream round-trips bidirectionally through its envelope
each stream seals to a distinct address, the same stream is convergent, and a step freshens the salt
the negative matrix holds on every stream: wrong passphrase, tampered ciphertext, and a foreign key all fail
the public envelope verifies for every stream and carries no plaintext on the wire
the uuid carrier moves every stream both ways, and a sealed envelope survives the trip
```

**cube-memory** — 8 utterances

```text
a partial neighbourhood NEVER seals, and the last member is what seals it
a COUNT is not a neighbourhood — the right number of the wrong keys is refused
the cube handle is ORDER-INVARIANT, and moves when any member moves
a standing receipt means NOT RECOMPUTED, and only the changed cube pays
a MISSING receipt is not a fresh one
a PARTIAL RUN neither writes a partial store nor forgets the cubes it never looked at
over the LIVE ledger every theorem lands in exactly one cube, and the whole ledger seals
a complete fusion TRAVELS — imprinted, witnessed by its own contents, and refused when incomplete
```

**cycles** — 6 utterances

```text
gcd / coprime
star polygon {5/2} is the pentagram — one stroke over all five (pentagram_single_stroke)
star polygon splits when the step shares a factor
single-digit Fibonacci Pisano periods: 9→24, 5→20, 7→16
rotate reports the strand structure — coprime stride is one closed cover
CRT fuses coprime moduli — ℤ/21 ≅ ℤ/3 × ℤ/7
```

**daemon** — 6 utterances

```text
health returns the ledger receipt
address content-addresses a value, GET and POST
verify is a keyless tamper-check
gate drains a fabricated citation and reveals everything else
trial returns one answer — VERIFIED or UNVERIFIED, all else void
an unknown route 404s
```

**detail-audit** — 13 utterances

```text
the controls are pre-registered, run first, and all rejected — the instrument can fail
every route gets a detail and every detail lands on its route
a document is audited detail by detail, counted without a silent cap, folded to one receipt
the split is deterministic, bullet-stripping, and never eats a leading number
a verdict moved is a receipt moved — altering one detail is visible in the fold
the tool hears word arithmetic — a false spoken sum is REFUTED, a true one decides
the chained sum parses whole — the film\
the captain\
a fabricated citation outranks true arithmetic — draining is the gate\
the powers-of-ten grammar: equations and orders decide, magnitudes only speak
cross-detail composition reaches the film\
ambiguous operands are refused, not guessed
an explicit delimiter is the split law for unpunctuated text
```

**discover** — 4 utterances

```text
rigidity CANNOT detect definitional dependence — the tool reports a definition as rigid
rigidity is the refutation — a relation that survives a moved input is not describing these numbers
divisibility by one is caught as limp
the superposition is enumerated whole
```

**domain-wave** — 4 utterances

```text
every wing recomputes — no address is forged
the recompute audit is FALSIFIABLE — a tampered statement is caught
order-invariance is STRUCTURAL— it holds for every input
every wing is covered — the wave leaves no theorem unaudited
```

**driver** — 2 utterances

```text
driverBundle PINS an exact netboot bundle — recomputable, Alpine filename convention
verifyDriverBundle — uuidna re-hashes YOUR bytes with its own SHA-256 (exact-copy proof, no host crypto)
```

**editor** — 5 utterances

```text
deterministic — the same document folds to the same address (editor_empty / same tree, same fold)
ORDER-SENSITIVE — reordering paragraphs MOVES the address (editor_fold_order_sensitive: a document is a sequence
CHANGE-SENSITIVE — editing any node MOVES the address, an untouched doc does not (editor_fold_change_sensitive)
NESTING-SENSITIVE — depth is part of identity; a flat doc ≠ the same text nested
payloadFoldHook — the Payload save hook stamps _handle + _address, and passes through a record with no editorState
```

**energy** — 23 utterances

```text
the exact SI products are the published constants, and the circle bracket is a unimodular pair
verifyBracket rejects a tampered bracket — the verifier is fallible
wind: the Betz ceiling brackets an INDEPENDENT recomputation, and rejects a wrong one
wind: a claim above the Betz limit is REFUSED, and the boundary is exactly where the module says
wind: the cube law and the area law are the ones actually computed
wind: hostile and impossible inputs REFUSE rather than compute
biogas: the chemical energy brackets an INDEPENDENT recomputation, and rejects a wrong one
biogas: the Carnot boundary is exact — 75% passes at 1200/300 K and 76% is REFUSED
biogas: unity, impossible fractions and impossible engines are REFUSED
biogas: the four-stroke counts are the definitional ones
mfc: the two measured bands DISAGREE at short retention, and the boundary is at 3 vs 4 hours
mfc: the bands disagree again at very long retention — the upper boundary is 1416 vs 1417 hours
mfc: an asserted power above the reported ceiling is REFUSED, at the exact integer boundary
mfc: nothing is claimed as exact, and the band is the finding
electrolysis: the reversible floor is about 1.2289 V, and 1.23 V is proved to be a ROUNDING UP
photon: a long-wavelength photon does NOT clear the floor, and the boundary is 1008 vs 1009 nm
electrolysis: a voltage below the floor is REFUSED, at the exact millivolt boundary
electrolysis: the endothermic band is REFUSED rather than printed as an efficiency above 100%
electrolysis: a faradaic efficiency above 100% is REFUSED as over-unity
every bracket in every answer verifies against its own witnesses, and carries no floating point
every answer is deterministic, total, and never over-unity in its own words
the MCP surface serves the same computation, and refuses an empty call by name
this suite actually exercised the negative paths — it is not green by never asking
```

**evasion-ticker** — 4 utterances

```text
every convicting seal in the catalogue is sealed in the ledger — the catalogue cannot evade
CONTROL: the seal-check can fail — a fabricated entry must be caught
the board folds order-blind and moves with one figure
every trick carries all four faces — no half-taught lesson enters
```

**evidence** — 3 utterances

```text
evidence delivers a cited proof in full and is recomputable
a fabricated citation yields no exhibit and is named missing
the same statement + ledger reproduces the same evidence receipt
```

**exercise-dormant** — 6 utterances

```text
a dormant script that exits 0 with no writes is no gap — dormancy stays permitted
a non-zero exit is a gap, and says the script is broken rather than idle
a timeout is reported as a gap too — 124 is not success
a declared write is not a gap, but an undeclared one is
both arms fire independently — a broken script that also writes is two gaps
the manifest is a roster: it declares writes only for scripts it also lists
```

**falsifiability** — 4 utterances

```text
falsify reports a check that catches every mutation
falsify NAMES the mutations a weak check lets through
the recompute audit IS falsifiable — tampering with key or statement breaks it
order-invariance is NOT falsifiable — it survives every mutation, which is why it is not an audit
```

**falsifier-floor** — 18 utterances

```text
the floor is the uuid less the captain commission, halved — not a snapshot
uuidna_is_dna_times_the_two_coins — the codon and the six doublings meet at 64, and the coins double it
residues_identify_digit — 6 and 9 separate every digit below 16, and stop at 18
vortex_one_leap — the doubling orbit on the units, and its digit sum
redirect_imitable_but_coins_authorise — exactly two coins authorise, no other count does
reversible_erases_nothing — multiplying by the modulus keeps zero at zero and one at itself
octave_codon_address — eight doublings reach 128, and the codon lands mid-octave
forgery_flags_every_mismatch — all 81 pairs walked, 9 honest and 72 forged
captain_commission_two_coins — two coins per FULL hundred-ten, nothing for a partial
THE FLOOR IS MET BY A LIVE RECOMPUTE, not by the mirror it shipped
honesty_gate_passes_iff_all_sealed — an IFF, so no second passing state can hide
conformance_failure_detects_intrusion — one flag drains the whole audit, no partial credit
honesty_gate_is_theorem_not_oracle — the implementation equals its spec at every state
overclaim_with_fake_cite_fails — a fabricated citation drains whatever else is clean
reduce_is_order_invariant — the fold is blind to the order it is handed
z7fermat — every non-multiple of 7 raised to the sixth returns to 1
three_no_inverse — 3 is the digit ℤ/9 will not invert
doubling_circuit — six doublings close the ring of units
```

**feed** — 4 utterances

```text
feed: unites every theorem, exactly once, nothing dropped
feed: @type/@id are structurally correct DataFeed/DataFeedItem, receipt is recomputable
feed: every nested item is vetted schema.org vocabulary — no second, unchecked JSON-LD surface
feed: carries the release version, held to package.json
```

**finder-coverage** — 3 utterances

```text
every finder one-receipt exports is wired: blocking in the guard, advisory with a reason, or on-demand with why
an advisory finder must state WHY it does not block — "not blocking" is a decision
the on-demand exemptions each carry their reason
```

**forensics** — 7 utterances

```text
a clean, backed statement passes
a fabricated citation is caught
a false address presented as sealed is caught
an address-mismatch (tamper/forgery) is caught by recomputation
an unbacked legal claim is flagged — a legal claim must carry a receipt
an unbacked patent / IP-ownership claim is flagged (hardened from a real near-miss)
auditAgents aggregates a stream and folds to one receipt
```

**forged-wings** — 5 utterances

```text
the real ledger is fully witnessed by the wings
an invented theorem is caught — the exact entry that passed four other checks
drift on a REAL key is reported separately from invention
whitespace alone is not drift — the wings and the ledger may format differently
an empty wing corpus condemns everything rather than passing quietly
```

**gaming-shelf** — 5 utterances

```text
the start position is exact to the piece — both kings on their squares, 32 pieces
scholar’s mate arrives as checkmate, decided
nim pays the coin: P-position verified by one XOR, and the win move returns the sum to zero
mobility counts name their seals at the corners of the map
the engine is deterministic and prefers the mate it can see
```

**gate-all** — 11 utterances

```text
the plan is READ from the chain, so it cannot drift from what audit runs
the three classes are told apart by what a step DOES
every failing CHECK is reported in a single pass
read-only checks run CONCURRENTLY — the linear walk was the slowness
tree-touching checks never share the tree — they run strictly alone
a failing GENERATOR still aborts — later steps read what it never wrote
a check failing never stops the generators or the checks that follow it
an all-green chain reports no failures and never aborts
pool honours its limit and preserves result order
every instrument names the arms it voids, and the invisible one is covered
labels stay short enough to read as a table
```

**gate-paths** — 4 utterances

```text
every literal path a gate script names resolves — a drifted reference makes a check inert
the finder bites — the historical offenders are exactly what the rule flags
the drain stages EXPLICIT paths — no unattended commit may sweep a sibling session\
the built-site audit DISCOVERS its pages — no gate hardcodes a page of the generated site
```

**grid** — 10 utterances

```text
the source ray holds no seat — 504 counts 72 identities, 432 counts the work
the grid is exactly one seat per (ray, wing), all distinct
the grid is rays times wings, at whatever count the ledger holds
harmony is stated in a form that does not depend on the base
the digit-reversal fusion holds for one spelling in one base, and no other
seats are addressable by name and by slug, and the source ray is refused
a seat MOVES when its wing moves — the address is derived
the grid root is order-invariant and recomputes
the finder names which base a count satisfies, and is silent only when both are
every wing carries at least one sealed theorem — no empty seat
```

**grow-milestone** — 4 utterances

```text
crossing a milestone ADVANCES it — the case that went stale
the milestone is always strictly ahead, at every size
toGo is never negative against the live ledger
the honest string no longer names a milestone already passed
```

**handle-chunks** — 5 utterances

```text
handle-chunks: chunk count matches the audited distinct-statement figure exactly
handle-chunks: every theorem key resolves to exactly one chunk, and that chunk lists the key back
handle-chunks: a reused statement (z9mul_1_1 / mul9_1_1) is ONE chunk citing both keys
handle-chunks: handles are collision-free and deterministic across two independent builds
handle-chunks: chunkHandleOf agrees with the bulk build for a sampled key
```

**handle-vectors** — 3 utterances

```text
a handle is exactly eight tiles, and every tile is a hexbit
all eight move together — one step advances every tile, none left behind
the value is the eight tiles read as one, and the residue places the whole handle
```

**handle** — 16 utterances

```text
every live handle is eight lowercase hex characters
malformed handles are REFUSED
the round trip holds for EVERY live handle
handleOfPath refuses what is not in the store — "not ours" is distinct from "malformed"
no two live handles address the same path
path order equals handle order across the live ledger
the tree narrows by 256 at each level and never exceeds it
handleDirs returns each level outermost first, so a writer can create them in order
the extremes of the space address correctly
every handle in the source comes from handleOf — no site re-derives it inline
handleOf refuses what it cannot address, rather than coercing it
the two routes to an address-integer agree over the domain, edges included
seedOf is the handle read as a number, over the LIVE ledger and not over examples
the derivations that are deliberately NOT seedOf keep their own width and domain
buildChunks is computed ONCE — the same object, not an equal one
the key index resolves EXACTLY what a linear scan resolves, over every live key
```

**harmony-scan** — 9 utterances

```text
the interval is the reduced ratio of the tiles, and the ladder names the small ones
the structural scan is a census — every bar counted, the widest located by number
a healthy recording agrees with its score, and the panel carries its receipt and its scope
CONTROL — a mix over the sealed ceiling is FLAGGED, not passed
CONTROL — a recording of a DIFFERENT song fails the cross-check, though both are valid audio
CONTROL — the measured pitch can disagree: a tone off the lattice reads off it
the apparatus reads the artifact as handed to it — a WAV this tree wrote comes back as its samples
silence alone is no song: an empty recording is voiceless and says so
THE APPARATUS\
```

**hero-channel** — 4 utterances

```text
every theorem transmits its own address through its animation, and it reads back exactly
different theorems animate differently — the channel distinguishes, it does not decorate
the lcm bound makes the read exact — and the margin over a hex digit is exactly the two coins
a foreign animation is refused
```

**hexbit-animator** — 4 utterances

```text
the motion layer is deterministic and the fold moves with one state
the bars align with the recording by the sealed arithmetic, not by code
the two layers of one identity share it exactly
off-lattice refuses loudly, as the player does
```

**hexbit-player** — 3 utterances

```text
the app is deterministic: same states, same bytes, same address
one moved state moves the address — a tampered recording cannot keep its identity
an off-lattice state refuses loudly — nothing off the lattice can sound
```

**hexbit-readings** — 6 utterances

```text
the reading table covers exactly the seven sealed rays — no ray unread, no stowaway tongue
every language names all sixteen states, each name distinct within its tongue
English text folds to one handle and all seven rays read it — seven texts, one address
the reading is exact — state a is "ten" in English, "десет" in Bulgarian, "十" in Chinese
CONTROL — a non-hexbit character is refused by name, never guessed at
the reading round-trips — the words point back to the states that made them
```

**host** — 8 utterances

```text
resolveShell — a POSIX host gets sh; the recipe runs one command string
resolveShell — THIS host resolves, and never to a shell that cannot expand a glob
resolveShell — the recipe carries the environment that makes the shell a WHOLE one
capacity — measured, never assumed, and it always leaves the machine room to breathe
hostProfile — the reading moves between hosts, the fold does not move on one
childProbe — every host has a way to ask, and "none" never reads as "one"
loadMeasurable — the one host whose load average is a permanent zero is named as unmeasured
speedup — an exact integer ratio in hundredths, and zero rather than a number it did not earn
```

**installs** — 7 utterances

```text
EXACTLY PORTED — every spec recomputes from the published tuple alone, field for field
the closure is exact — every dependency lands inside the set, and home is the meta package
the authored route layer covers the set EXACTLY — no stale entry, no unnamed member, all distinct
the port rises from the floor — dependencies first, the published cycle the ONLY exception
compiled from source in hexbit — 32 on-lattice states, the address nibble for nibble
the OS is bootable quantum — the boot image is the compiled specs in boot order, receipt-closed
the receipt is stable, recomputable, and CHANGE-SENSITIVE — the instrument can fail
```

**laws** — 1 utterances

```text
uuidna states its laws IN uuidna, and every law holds by recomputation (not by prose)
```

**lead-shapes** — 4 utterances

```text
a defining constant is told apart from a narrative measurement
both phrasings of a definition are caught, before and after the number
a bare number is NOT a defining constant — the words carry the claim
every lead carries the sentence that produced it — a number with no provenance is not a lead
```

**llm-txt** — 7 utterances

```text
every identifier cited in llm.txt is a sealed theorem or a real tool
it cites both kinds — theorems to verify, and tools to call
the proof leads and the endpoint is reachable in one screenful
llm.txt stays within its size budget
every payload handle resolves to the chunk it names
the operational hard rules survive regeneration, in the payload
it does not freeze a ledger count that will drift
```

**machine** — 3 utterances

```text
keeping exactly 13/32 spare sits ON the law — balanced at the theorem\
one ring under says FOLD and names the heaviest writer, priced — the control that can fail
deterministic, change-sensitive, degenerate-safe
```

**market** — 5 utterances

```text
a sealed series is addressed, handled, and deterministic
the backtest is receipted: same handle + same registration = same receipt, and integers throughout
the rise-then-fall series produces the one round trip the shape implies
degenerate registrations are refused, not guessed
the stooq parser floors to integer cents and bounds the window
```

**mcp-coverage** — 1 utterances

```text
the MCP covers every theorem in full functionality, in all dimensions; each theorem scans its neighbours
```

**mcp-e2e-online** — 3 utterances

```text
e2e online: ${source} answers the served contract
e2e online: education↔jobs walks ESCO and returns a shaped pairing
e2e online: the liveness probe reports rather than raises
```

**mcp-e2e** — 9 utterances

```text
e2e: initialize returns a protocol version and names the server
e2e: tools/list serves every tool with a usable schema
e2e: a tool call returns parseable JSON with the field it promises
e2e: the same call twice returns the same receipt
e2e: an unknown tool is refused by name, and the server survives it
e2e: a missing required argument is refused, and the refusal NAMES the argument
e2e: an unknown METHOD is an error
e2e: many sequential calls on one connection stay independent
e2e: the server wrote nothing unexpected to stderr while serving
```

**mcp-http** — 6 utterances

```text
initialize announces the protocol and server
tools/list enumerates the Workers-safe subset (every tool named, described, schema-typed)
tools/call runs a tool and wraps the result as MCP text content
tools/call is deterministic — the same call recomputes the same address
an unknown tool is a tools/call error
a notification carries no reply (null), an unknown method is -32601
```

**mcp-schema** — 3 utterances

```text
the served surface enforces its own schema — a declared-required argument is never silently absent
no empty call can spawn a process — every orchestration tool declares a required argument
the whole no-arg dispatch probe stays cheap — the gate cannot silently regrow a workflow
```

**mcp-surface** — 8 utterances

```text
every edge tool is either in the stdio catalogue or a DECLARED edge-only name
a tool served by BOTH surfaces takes the same required arguments, or the difference is declared
the research surface is served by BOTH, with identical argument contracts
the hosted edge REFUSES a missing required argument instead of computing over "undefined"
the same call to both surfaces returns the same research answer
the edge serves the whole catalogue except what it DECLARES it cannot
every declared absence is a real tool — a stale excuse is drift with paperwork
an inherited tool answers with the SAME dispatch the stdio surface uses
```

**mcp-version** — 1 utterances

```text
the hosted MCP advertises the package version
```

**mcp** — 5 utterances

```text
catalog ↔ handlers: the catalog lists exactly the dispatchable tools, unknown tools throw
CI through the MCP: the served tools return what the sealed package computes
unify: one receipt folds theorems + domains + tools, recomputable
mcp tests itself: catalog↔handlers hold, zero-arg tools recompute (only live resources may vary)
the MCP measures itself: uuidna_mcp_benchmark scores the whole served surface
```

**measure** — 5 utterances

```text
a receipt is deterministic across runs of the same measurement
a receipt MOVES when the value moves
every registered measurement runs and returns a receipt
wing-parity holds in BOTH directions
no handle fails to recover itself from its path
```

**messaging-handle** — 5 utterances

```text
the glyphs ARE the address — 32 of them, derived, never decorative
the round-trip returns the exact text, and says so honestly
a wrong passphrase refuses — it never guesses and never half-opens
CONTROL — a tampered envelope refuses: the reader can fail, so its acceptances mean something
the form is stated, so a receiver never has to guess the reading order
```

**models** — 3 utterances

```text
EXACT over the whole census — every row recomputes from its mirror model, none invented, none dropped
the fold lands on 16 on-lattice pairs for ANY input — and pairs flatten back to the 32 states
the receipt is change-sensitive — a tampered mirror is CAUGHT (the instrument can fail)
```

**no-pinned-counts** — 1 utterances

```text
no test pins the ledger count — the forbidden literal is derived, so this guard rides the sequence
```

**notice** — 4 utterances

```text
an open verdict becomes a notice that points at self-inverse structure
a verified claim needs no direction — the notice stays silent
CONTROL — words the ledger has never sealed get an honest empty list, never invented pointers
the notice never invents a verdict — it only adds direction to the one the trial gave
```

**oeapi** — 7 utterances

```text
OEAPI field audit — every emitted field, code and enum value is vetted v6.0 vocabulary
OEAPI ids ARE the ledger content-addresses — every identifier recomputes from the proof it names
OEAPI cross-references resolve — no dangling id in the projection
OEAPI refuses what it cannot derive — no complexityLevel, and every absence names its replacement
OEAPI profile is deterministic and counted — the receipt recomputes, the counts are the ledger
additive spec fields never move an identity — the receipt folds ids
a field that would require inventing a value is absent BY NAME
```

**one-writer-working** — 2 utterances

```text
a holder with a live child reads WORKING, and the same holder reads not-working once it ends
a pid that cannot have children answers no rather than throwing
```

**one-writer** — 9 utterances

```text
one writer wins; a STRANGER is refused with the holder NAMED (a descendant passes — that is reentrancy)
a dead holder is stale by pid-liveness — reclaimed on the next acquire, no clock consulted
only the holder releases; releasing a live stranger\
a corrupt lock file never wedges the tree
a free tree is acquired with zero polls — waiting costs nothing when nothing holds
a DEAD holder is reclaimed by the wait — stale-reclaim is inherited, not reimplemented
CONTROL — a LIVE stranger is announced once and refused at the ceiling, never waited on forever
a holder with NO live child is still refused at the ceiling — the stuck signal is unchanged
CONTROL — a holder that IS working EXTENDS past the ceiling, and the lane is taken when it finishes
```

**open-questions** — 3 utterances

```text
placement is deterministic and one shared word is not a topic
the involution magnets are involution-shaped, every one
topics group and sort with the frontier last
```

**os-exec-run** — 8 utterances

```text
ls runs as an applet — the same listing the dedicated tool gives, now through the executor
cat reads a file — the package\
which walks both directions — name→route and route→name
stat, pwd, echo — the small applets answer from the sealed spec
deterministic AND change-sensitive AND honest on the unknown — the executor cannot fake or drift
apk — the package manager\
du — the hexbit footprint: 32 states per package, the whole OS one boot image
the SERVED tool uuidna_exec dispatches — the terminal\
```

**os-exec** — 3 utterances

```text
uuidna_ls / lists the virtual OS root — packages and dirs, each with its identity, from the sandbox
uuidna_ls /terminal lists the busybox family — the toolbox\
deterministic AND change-sensitive — the receipt is the run\
```

**os-port-status** — 6 utterances

```text
portStatus reports the pinned port — the committed mirror made observable
CONTROL — the port compared to ITSELF is CURRENT: no move, no update due
a moved RELEASE flips it STALE and is named
a changed CHECKSUM, an ADDED and a REMOVED package are each caught and named
the delta receipt is deterministic AND change-sensitive
the SERVED tool uuidna_port reports the pinned port through the wire
```

**os-registry** — 5 utterances

```text
every port — tool or install — wears the one shape: uuidna/<name>, 128-bit address, 32 hexbit states
a tool\
the whole registry folds to ONE root, recomputable AND change-sensitive
the registry is sorted by id — a stable order, so the fold is order-free of insertion
the SERVED tool uuidna_registry dispatches: the whole live catalogue folds with the OS to one receipt
```

**os-related** — 6 utterances

```text
a ported package the wings name is PORTED — related, and already carried
a name occurring only inside a ported package\
an ordinary-English name is UNDECIDED — claimed neither related nor unrelated
CONTROL — the instrument CAN report an uncarried package; it is not closed by construction
over the REAL port: every ported package is named, and the receipt is change-sensitive
the SERVED tool adjudicates against the SHIPPED ledger — a narrower haystack than the Lean sources
```

**packages** — 1 utterances

```text
each Alpine package becomes uuidna/<name> — a deterministic, injective provenance identity
```

**pair-grid** — 10 utterances

```text
42 is the full product with the identity removed — the same rule that makes 432
6 × 7 and 7 × 6 are the same 42, read from two sides
no dimension is ever paired with itself
transposition is an involution with NO fixed point
the 42 directions fall into exactly 21 orbits of size two
the grid is regular — every dimension a source and a target exactly six times
every direction is uniquely named and uniquely addressed
a direction and its reverse are DIFFERENT addresses — order is real
42 is NOT a reshape of 432 — the two grids stay separate
the finder is silent on a healthy pair grid
```

**payload-seed** — 5 utterances

```text
the uuid reverse-engineers: status, stem and content decode from the name alone — zero reads, zero index
any change to the Lean mints a NEW version: the uuid moves with the contents, deterministically
filtering is free: a folder listing filters by status decoded from the names
the seed is a stamped, verifiable nested page: parent + one nested child page per theorem
payload sync speaks only the standard shapes: pages, nested-docs parent, drafts _status, lexical content
```

**polygraph** — 5 utterances

```text
a kept promise reads clean: every path the message names, the diff contains
a broken promise is named with its commit — the say-do gap, decided by set arithmetic
the empty case verifies — a message naming no path promises no path
CONTROL — the needle moves both ways: the same chart charges the gap and clears the keeper
prose that is not a path is never charged — the finder does not guess at nouns
```

**practice-shelf** — 4 utterances

```text
the drill presents the sealed and refuses the open by name
the feedback fold: same trials any order, same receipt — and one changed answer moves it
the meter closes at the trinity and reopens on failure
the walker derives a real road: prerequisites exist, come first, and the walk is deterministic
```

**precede** — 4 utterances

```text
a dirty tree with NOTHING staged is not an inversion — the finder stays silent
derived STAGED while its Lean source is not is caught, and the gap names both sides
staging the source alongside its derived output clears the gap — the finder can be satisfied
a staged SOURCE with no derived file staged is not an inversion — the order it forbids has a direction
```

**predict** — 3 utterances

```text
the engine never predicts what the repository has already declared
what the declaration absorbed is reported
the remaining predictors still FIRE — a quiet engine must not be an empty one
```

**proof-cache-sign** — 2 utterances

```text
a keyed host mints signed entries, verifies them, and DISTRUSTS the unsigned and the forged
a keyless host keeps the weaker address-match floor — named, not smoothed
```

**proof-cache** — 3 utterances

```text
the release gate re-proves from the KERNEL — it never accepts the receipt cache
the cache is a map a human can commit — which is exactly why the release cannot lean on it
no source claims the cache is incapable of a false pass
```

**prose-analysis** — 5 utterances

```text
COMPUTE — binary arithmetic in prose is settled by decide (VERIFIED / REFUTED)
COMPUTE — the compound guard refuses a fragment (no false REFUTED)
COMPUTE — uuidna computes division with TOTAL Nat semantics, including division by zero
UNDERSTAND — resonance ranks by amplitude, so the reflection is legible
GATE — a fabricated theorem citation DRAINS; honest prose clears
```

**prose-gate** — 3 utterances

```text
a claim that cites no theorem is REVEALED
citing a FABRICATED theorem is the one decidably-false case — it drains
citing a REAL sealed theorem passes — a backed claim is not drained
```

**publish** — 7 utterances

```text
the whole stream is publishable — every note passes its own audit before publishing
each note is content-addressed and its proofs fold to a receipt — recomputable
every load-bearing claim links a proof — the note earns what it asserts
the gate BITES a fabricated citation — a note that invents a proof is refused
editing is re-addressing — a revision re-fingerprints, and an edit that overreaches is refused
similarity is derived from difference — pattern recognition, inclusion–exclusion exact
a proof-backed sentence keeps its strong words — backing clears the gate
```

**quantum-message-encoding** — 5 utterances

```text
the two gates the encoding used are INDISTINGUISHABLE by measurement
distinct theorems give distinct quantum receipts
measureMessage recovers bits
measurement is deterministic — the same message measures the same
the quantum receipt folds the theorem, and the message fold carries the content
```

**quantum-message** — 1 utterances

```text
MAX_MESSAGE_QUBITS matches the sealed theorem message_qubit_cap_states
```

**quantum-news-portal** — 5 utterances

```text
quantum-news-portal: extracts facts from articles
quantum-news-portal: audits facts against ledger
quantum-news-portal: builds a domain portal
quantum-news-portal: renders summary
quantum-news-portal: produces order-invariant receipts
```

**quantum** — 29 utterances

```text
X|0⟩ = |1⟩
Z|0⟩ = |0⟩ (phase acts only on |1⟩)
Y|0⟩ = i|1⟩ — probability on |1⟩, amplitude purely imaginary
H|0⟩ = |+⟩ — exact 1/2, 1/2
H·Z·H = X (on |0⟩)
S·S = Z (on |+⟩)
S·S† = I (on |+⟩)
H·H = I (on |0⟩)
X·X = I, Y·Y = I, Z·Z = I
X, Y, Z, H are involutions (g∘g = I) — verified via isInvolution on a live state
CNOT, CZ, SWAP, Toffoli, CCZ are involutions on entangled/superposed states
S is NOT an involution — it has order 4 (S²=Z, S⁴=I); S·S†=I
CNOT: |01⟩ → |11⟩ (control q0 set flips q1)
CZ: phase-flips |11⟩ only (probabilities unchanged, amplitude negates)
SWAP: |01⟩ ↔ |10⟩
Toffoli: |110⟩ → |111⟩ (both controls set flip target)
CCZ: phase-flips |111⟩ only
Bell state (|00⟩+|11⟩)/√2 — exact 1/2, 0, 0, 1/2
no-signaling: each Bell marginal is exactly 1/2
GHZ(3) = (|000⟩+|111⟩)/√2 — only 000 and 111 carry 1/2
every distribution sums to exactly 1
probability(i) agrees with the distribution
the quantum receipt is deterministic and recomputable; distinct states → distinct receipts
runCircuit with OpenQASM gates reproduces Bell and GHZ
classical bridge: cx = XOR, ccx = reversible AND (truth table)
classical bridge: a half-adder (sum = a⊕b, carry = a∧b) from cx + ccx
a circuit with H has no classical map (honestly flagged)
runCircuit rejects unknown gates and out-of-range qubits
the report is honest (passes the gate) and carries no floating point
```

**reactor** — 3 utterances

```text
snapshot fuses sealed theorems across domains to one recomputable superposition
snapshot NAMES an unknown key rather than folding it — drift refused
the reactor recycles — an unverified claim returns with its develop plan
```

**readme-coverage** — 3 utterances

```text
the README states what a handle spans — every superposition an address can name
coverage is accounted in coins, and the rate is the live one
the directions are named, so a per-coin figure means something
```

**readme-quantum** — 2 utterances

```text
the published quantum capacity equals the measured capacity, figure for figure
the capacity section keeps its honest boundary
```

**reason** — 4 utterances

```text
modus ponens and the chained syllogism, each citing a sealed rule
an unmet premise never fires — it derives only what follows
deterministic — same facts and rules give the same receipt
bounded — a cyclic rule set settles at the round cap
```

**recompute** — 1 utterances

```text
every sealed theorem recomputes its content-address — generated from the ledger, red only if an intruder tampers
```

**redos-bounds** — 4 utterances

```text
the exported extractors finish in bounded time on input built to make them backtrack
readHero finishes in bounded time on an SVG that opens a node and never closes it
THE PROBE ITSELF CAN FAIL — the killable-child harness is not green by never running
THE BOUNDS DID NOT COST THE READINGS — every extractor still returns what it did before
```

**referrer-song** — 6 utterances

```text
the six doors are the six rotations of the sealed round, each a rotation of one melody
the door pick is deterministic and admits every visitor
all six doors are reachable — no verse nobody hears
consonance is the sealed ladder — unison 2, octave 3, fifth 5, in that order, as numbers
the cycle is total both ways and the last page wraps home
the greeting is bytes that recompute — same referrer same song, different doors different songs
```

**research-ledger** — 9 utterances

```text
the shipped findings are the record in lean/research-ledger.json, field for field
ONLY A READ PRIMARY SOURCE ANCHORS — and the negative controls prove the predicate discriminates
a MEASUREMENT never seals as an equality, however well it was read
the address moves when any addressed field moves — a forged value cannot keep its identity
the ledger objects to ITSELF — and stays silent when there is nothing to object to
the census counts every status and cannot be flattered by a filter
an unknown filter is REFUSED by name
the report makes plain which findings may anchor a theorem and which may not
the tool dispatches through the served surface and is listed in the catalogue
```

**rosetta-legs** — 8 utterances

```text
the leg mask round-trips for every subset of the five legs
THE MIRROR THE HOSTED EDGE ANSWERS FROM MATCHES A LIVE RECOMPUTE, KEY FOR KEY
the two surfaces give the SAME answer for the same theorem, not merely the same tool name
three legs LOCATE a fault, two only DETECT one — the bound, not a preference
an unknown key is REFUSED — the census does not invent a theorem to describe
the census counts exactly, on rows whose answer is known in advance
THE FLOOR MAY ONLY RISE, and the check FIRES when anchoring is lost
the scarce legs are reported as they stand, never smoothed
```

**school-apis** — 19 utterances

```text
every registered source declares what it serves and how it answered
sources that could not be called are recorded in ABSENT, by name
the receipt is deterministic and MOVES with the registry
the GISCO vintage is pinned — a directory without a vintage cannot be rechecked
no two sources share an id
ESCO is declared as the bridge, and the jobs side is reachable from the same door
EURES is named in ABSENT with what stands in its place
an unknown source is refused BY NAME, never answered with an empty result
the CSV split survives quoted commas and doubled quotes
a fragment hit is a homograph, not a mapping
the removed bulk mapping is recorded as an absence, with what replaced it
a source wired on the responding path rather than the serving path is refused, and says which
each fetched source is refused by name when its own required argument is missing
a multilingual title prefers English and never falls through to nothing
every source declares the known-good query that proves it still answers
the heartbeat separates answering, empty, REFUSED and thrown — and never raises
the heartbeat receipt moves when a source goes dark, and is stable when nothing changes
an HTML body is recognised as a web page, not as data
the heartbeat treats a DECLINED source as dark, never as an empty world
```

**school-tools** — 4 utterances

```text
the tester runs its controls first and all reject — then the subject gets a real verdict
the tester leaves an unproven claim honestly UNVERIFIED, and a recomputation verifies
the editor: every unit squares to one, undo returns from any walk, and a non-unit refuses loudly
the builder reports door, multiplier and beats consistently with the seals it cites
```

**school** — 15 utterances

```text
all eleven sections are present, each with a body
accreditation states plainly that uuidna is NOT accredited
enrolment collects nothing and costs nothing
every wing is a course, and lessons sum to the ledger
most sections are computed, and the authored ones are positions rather than facts
the computed sections carry real values
the catalogue MOVES with the ledger — it is a reading
a level is the DECADE of a measured cost, and its boundaries are exact
a level is a property of the course— it survives the catalogue changing
the reading order is total, starts at the cheapest course, and never sorts an absence to the front
a course cost is a cost the kernel actually paid — an element of its own roll, in integers
nothing measured is nothing graded — an absent measure is never a low one
the levels are the levels PRESENT — the school advertises no grade it cannot fill
OEAPI serves the measured level in ext and REFUSES the spec field that would imply a degree
the levels section RENDERS the derivation — the measure reaches the reader
```

**seo** — 10 utterances

```text
schema.org naming audit — every emitted @type and property across the WHOLE surface is vetted vocabulary
quantum SEO: /school is a WebPage whose mainEntity is a schema.org School
quantum SEO: /trials is a MathSolver whose SolveMathAction targets the live endpoint
quantum SEO: a page frontmatter description flows into the entity — one voice, no second copy
quantum SEO: /theorems is the SAME Dataset node every theorem cites as isPartOf — the graph closes
quantum SEO: every theorem carries its own line-content @id, distinct from its proposition identifier
theorem handle citation shorthand (first 8 hex chars) has zero collisions, for address and lineAddress
quantum SEO: /quantum-cryptography is a free Course provided by the School node
quantum SEO: strict means refusing too — the law pages stay WebPage, no legal types anywhere
quantum SEO: the receipt still folds description — a changed page voice moves the receipt
```

**separation** — 12 utterances

```text
the mirror involutes and the doubling does not — the negative control is the point
a lone involution separates nothing — it permutes the set, it does not sort it
the triangle is entered by ALTERNATION only
order is not cosmetic — the two orders disagree, which is the commutator
only the void is stuck; a cascade cannot enrich what it cannot move
the alternating walk does not close, though doubling alone does
the mirror is reversible and the doubling is not — the irreversible step is the productive one
cascades are exact rationals
stagesToReach returns the BRACKET, and refuses a factor that cannot climb
the price is bracketed— sail five to make good three
a cascade addresses its own outcome, so a stranger recomputes it
asymmetry names its witnesses, and a map against itself has none
```

**sequence-coverage** — 10 utterances

```text
the orbit itself is sealed — [1, 2, 4, 8, 7, 5], written out in a decided statement
the sequence closes — six steps, and the sixth returns to one
the digit sum 27 is sealed
the wing exists, is non-trivial, and every one of its theorems is kernel-only
the coverage predicate FIRES on a sequence that is not sealed
every STEP of the recurrence is sealed on its own
the closure over the proof graph is finite, and every theorem it reaches is sealed
the recursive walk FIRES when a step is missing — the control for the closure
a lone theorem and an unknown key are DIFFERENT answers
the neighbourhood names the principle the caller would otherwise re-derive
```

**sequence-run** — 6 utterances

```text
every residue is reached, and the seed is always in the ring
the fixed points are EXACTLY 0 and 5 — measured
the executor DISCOVERS that dz reverses and doubling does not
text enters through its content-address, and the same text always lands the same way
order matters almost everywhere — the two steps do not commute
the walk is bounded and the report is total — no field is left undefined
```

**sessions** — 5 utterances

```text
a session identity is stable across its life, and distinct per host, purpose and birth
CONTROL — announcing updates only my own line; another crew\
staleness is LEDGER DISTANCE, not time — no clock is consulted anywhere
a polite departure gives the line back; a crash lets it age out instead
two devices with the same roster fold to the same receipt — and a single changed line moves it
```

**site-contents** — 7 utterances

```text
site contents is a book: every chapter carries an index, a heading and its own address
the contents root folds every chapter, and recomputes
chapter addresses are distinct — no two chapters fold alike
the contents covers the whole sidebar — no group is dropped on the way to the book
by-proof groups every page that cites, and no page that does not
by-proof is deterministic and its root folds every group
by-proof is a SECOND axis— the two disagree, and that is the point
```

**skill-surface** — 9 utterances

```text
every skill the sealed ledger carries is OPENABLE over stdio — and five broken surfaces are each caught
the HOSTED EDGE serves the same axis, with the same argument contract, and refuses an empty call
both surfaces fold one skill to the SAME receipt — and two different skills never to the same one
skillSurface serves ONLY that skill\
skillIndex enumerates EVERY skill with its count — the surface is discoverable without guessing a name
the ESCO mapping is a POINTER and a RULE — a homograph is named
skillReach counts what a surface SERVES — and a half-served surface does not read as full
the skills FINDER is clean, and runs by name in the audit chain
the two skill tools are CATEGORISED
```

**skills** — 1 utterances

```text
every theorem carries an INLINE-authored skill — skillOf is retired (0 fallback)
```

**smoke** — 21 utterances

```text
content-address is deterministic and context-free
the Lean 4 formal layer — its asserted ℤ/9 facts recompute true (toolchain-independent)
imprint codec round-trips arbitrary text
merkle proof is sound — the true leaf verifies, a forgery fails
the trial — one answer, VERIFIED or UNVERIFIED, all else void
harness makes any output auditable; reeducate bounds overclaims until they hold
render presents by reference — pure TS+CSS, address in every card, no framework
billing measures bits saved; coins are conserved; public interest is free
crypt: pure-TS ChaCha20-Poly1305 round-trips; wrong key and tamper fail; deterministic; 7d-fold envelope verifies
777 · the theorem-fold gate holds across every stream — a real description passes, a fabricated proof drains
777 · the same tests generate the UI — shadcn microdata cards, each statement linked to its proof
prose aligns to the theorems — no hardcoded count drifts from the live ledger/catalog
local chat — conversation fold: authenticity, per-referer rooms, sealed round-trip
attached chat — one primitive for donations, support cases, any subject
trial deposit — requires the two coins deposited by the parties, sealed diamonds, computes in parity, builds the lacking
conformance — the commit DNA gate: coins conserved, every theorem address recomputes, security clean
exploit fold — computes from the ledger (no table), verifies BOTH problem and solution, honest boundary
sanitise by all standards — process any input, sanitise any output, rules bound to the theorems
the quantum engine — one input→output surface: any op dispatches, folds to a recomputable receipt
pentagram monographs — the split computes itself from the addresses; prime-neighbour walk, order-invariant seal
spin — the bits spin by themselves: a sealed layer verifies O(1), any drifted coin hard-fails
```

**song-lattice** — 11 utterances

```text
THE LATTICE READS BACK: every note of song.wav decodes to its sealed digit
the coda is Az held long — the tuning itself, nearly a second
NEGATIVE CONTROL: the comparison can see a difference, or it is worth nothing
container 1/4: RIFF magic, and the declared size spans the whole file
container 2/4: WAVE with a canonical 16-byte PCM fmt chunk
container 3/4: mono 16-bit at the lattice sample rate, rates self-consistent
container 4/4: the data chunk is the rest of the file, in whole samples
the beat is one bar — every non-coda note holds ~252 ms (9·7·4, the sealed CRT cycle)
the rungs close to nine: in every DECODED verse, half against half, digit against digit
the decoded verse pairs are complementary strands — sung strand then complement, rung by rung
every note sung in the verses is a unit of ℤ/9 — the recording never sounds 0, 3, 6 or 9
```

**spin-parity** — 4 utterances

```text
every gated file is sealed by spin
an EMPTY manifest refuses, rather than agreeing with everything
a DIRECTORY entry seals every file beneath it
a sealed manifest still detects a tampered file, and passes an intact one
```

**stream-step** — 3 utterances

```text
a passing step captures its output and reports ok
a FAILING step reports ok:false — the exit code survives, which a pipe would have eaten
stderr is captured alongside stdout — a runner classifies what a step said, wherever it said it
```

**terminal** — 7 utterances

```text
THE SINGULARITY — the module ships zero tool names; the toolbox comes only from the wire
the grammar — a line parses to exactly one honest command
the wire shape — envelopes are exact JSON-RPC 2.0, the MCP\
the meaning is the SEALED one — read from the port, never restated by hand
results render the answer then the ledger line; errors and strange shapes stay honest
the NL router — deterministic, floor-honest, schema-driven; ambiguity SHOWN, never guessed
the transcript compiles to the lattice — deterministic, change-sensitive, 32 on-lattice states
```

**tool-census** — 9 utterances

```text
coprime, crt, compare — the number-theory tools compute their exact answers
digital_root, fibonacci — the ℤ/9 tools land on the sealed cycle
sha256, hmac, coin64 — the crypto primitives are deterministic and correct-width
involute — the tool computes an involution with its fixed points
expose — the coordinates of unsealed structure compute, fold, and stay coordinates
conformance, axiom_witness, gate_status — the self-audits report clean and consistent
gravity, seats, alpine, coverage, cost — each returns its computed answer
pairs, triad, pentagram, grid, fingerprint, units — the geometry tools answer
reflects, reveal, holofractal, security_audit, cloudflare_audit — the reflective tools answer
```

**tool-exercise** — 2 utterances

```text
the under-tested set is DECLARED and does not grow — new tools earn a test or a deliberate baseline entry
the census is honest — directly-exercised and aggregate-only partition the tools exactly
```

**trading-shelf** — 5 utterances

```text
the census counts two coins per sealed thing and CHECKS the conservation
the leverage never reports a negative saving — the sealed floor holds
the compound counts whole doublings at the sealed threshold and keeps the ratio exact
the desk prices a workload from the sealed billing surface
CONTROL — every instrument states its scope IN the answer, so the disclaimer cannot be lost in transit
```

**trial-dimensions** — 6 utterances

```text
the χ claim computes in ALL THREE genus dimensions — the whole range, one verdict
CONTROL — one failing dimension fails the whole trial and names itself
CONTROL — the empty range verifies nothing (vacuous truth refused)
a throwing dimension is a failed dimension, never a crash and never a pass
the proof root is order-invariant — two observers, two orders, one root
the seven locale rays ride the same trial — harness7 as one instance of the general range
```

**trial-protocol** — 14 utterances

```text
a test that always passes is VOID
a protocol with no registered refutation criterion cannot run
control rejected and subject holds is SUPPORTED — and says that is not proven
control rejected and subject fails is REFUTED — a result
one leaked control voids the whole trial, however many hold
no controls at all is INCONCLUSIVE — nothing established that the test can fail
the ledger's dna-recomputes check is VOID: a forged theorem passes its control
a void folds into a refutation of the instrument, keeping its receipt
folding is inert on a real result — it cannot launder a failure into a finding
the receipt identifies the TRIAL, so the same question folds to the same address
a refutation folds into support for the negation, keeping its receipt
a refutation from an UNSOUND trial supports no negation
the chain settles: void -> refuted(instrument), and refuted -> supported(negation)
supported is terminal — folding it again changes nothing
```

**tts-languages** — 6 utterances

```text
every locale ray folds to a singable handle — one lattice for all seven languages
composition is exact in every script — whitespace collapses, bytes decide, addresses reproduce
dialect pairs address apart and sound apart — the ledger speaks bytes, not tongues
the seven rays receipt one output seven ways and fold to one root
the host voice is only ever dry-run here — the command composes, the device stays untouched
whatever the language, the sung handle is eight tiles and seven breaths, tile by tile on the lattice
```

**tts-voice** — 7 utterances

```text
every hexbit state sounds its own A432 harmonic — the pitch is the digit, all sixteen
no two states collide — sixteen tones, sixteen distinct measured pitches
samples are exact integers inside the declared headroom
humanise removes the click and only the click — edges at zero, pitch untouched
a handle is eight tiles with a breath between them, and the breath is real silence
the recording carries a receipt anyone can recompute, and it covers every byte
the WAV header declares exactly the bytes it carries
```

**typography-audit** — 4 utterances

```text
the 13 sealed Typesetting details exist in the ledger
SEALED frameworks ring their gateway theorem (resonance)
GAP frameworks are a GENUINE absence — no sealed theorem carries their native unit
DEMARCATED irrationals are marked
```

**url-audit** — 5 utterances

```text
TOTALITY — every url answers: known, unknown, empty, garbage, malformed escapes
EXACT where exactness exists — /terminal audits to busybox, the sealed meaning first
the url\
the relevance floor holds — of/the never outrank a content word (a peer session\
deterministic AND change-sensitive — the instrument can fail
```

**use-versus-mention** — 6 utterances

```text
a file is never its own witness — selfExcluded drops only the candidate
self-reference alone is not an invocation
a mention without a runner is not an invocation
a constructed path still counts — the runner need not touch the filename
both extensions are invocations — node runs TypeScript directly
the live tree obeys the law — no script is dormant, and none is exempt by self-reference
```

**video** — 2 utterances

```text
every URL shape yields the same id, and a bare id passes through
what carries no id is returned as given — never an invented id
```

**wave-deposit** — 2 utterances

```text
every refusal class refuses, and the lawful candidate passes — the door can fail
a deposit lands only the lawful, returns refusals with reasons, and never writes a refusal
```

**waves** — 3 utterances

```text
the whole ledger as one wave-run — sandboxed, chained, every hook heard, count derived
the chain is order-sensitive and deterministic — the transcript cannot be quietly rearranged
afterWave may REFUSE — a short wave stops the run at its exact link
```

## Honest scope

The corpus is the titles as their authors wrote them; the derivation adds nothing and hides nothing. Their truth
is the suite's — enforced at every gate — and their prose is the authors' craft: the test names are the prompt
engineering, written as sentences worth reading because they are the corpus.
