---
title: "The torus dimensions"
description: "Computed from lean/Torus.lean — 8 sealed theorems, every claim citing its proof."
---

# The torus dimensions

> TORUS — the torus dimensions sealed: the genus ladder pricing in coins (2g − 2 over g = 1..6), the hextorus at 2·3 = 6 handles minting a trinity of addresses and paying the ten, and the 7-torus's Pascal row summing to the uuid's 128 with equal 64-boards for halves. The kernel seals the integer table; the topology reading stays in the prose. — held by [hextorus_is_coins_times_trinity](/theorem/hextorus_is_coins_times_trinity) and its 7 siblings below.

**8 theorems**, from [hextorus_is_coins_times_trinity](/theorem/hextorus_is_coins_times_trinity) onward, each proven `by decide` in <a href="/lean/Torus.lean">lean/Torus.lean</a>, axiom-free against the bare Lean kernel. This article is computed from the ledger — nothing here is authored, and every claim carries its citation. This wing states what HOLDS and seals no boundary of its own — read its honest scope in the wing header, which is not a theorem.

**[Re-prove this wing in your browser ↗](https://live.lean-lang.org/#project=mathlib-stable&url=https%3A%2F%2Fraw.githubusercontent.com%2Fuuidna%2Fuuidna%2Frefs%2Fheads%2Fmain%2Flean%2FTorus.lean)** — nothing to install. The editor fetches `lean/Torus.lean` from the repository and re-decides all 8 proofs on Lean v4.33.0, the toolchain this ledger is sealed against. The wing imports nothing, so what the reader runs is the whole input: a green run there is the reader's own verdict, not ours.

### THE HEXTORUS IS THE DOUBLE TORUS TIMES THE TRINITY: 2 · 3 = 6 handles. The handle count is a number the ledger already turns on — the vortex orbit's length is the same 6 (2 has order 6 in ℤ/9*, sealed as the coins times the trinity in sequence_and_coins_are_one) — so the six-handled surface walks one handle per toss and closes with the orbit.
The ledger holds this as [hextorus_is_coins_times_trinity](/theorem/hextorus_is_coins_times_trinity) — proven `by decide`, sorry-free:

```lean
2 * 3 = 6
```

### THE HEXTORUS PAYS THE TEN WHERE THE DOUBLE TORUS PAYS THE COINS. The Euler deficit 2g − 2 (the negation of χ = 2 − 2g, stated Nat-total) prices genus 2 at exactly the two coins and genus 6 at 10 — the reflection's constant sum (dz_sum_ten: x + x/0 = 10) and the size of the complete digit set (seal_ten). The shape that carries three double tori owes the whole decimal.
The ledger holds this as [hextorus_deficit_is_the_ten](/theorem/hextorus_deficit_is_the_ten) — proven `by decide`, sorry-free:

```lean
(2 * 6 - 2 = 10) ∧ (2 * 2 - 2 = 2)
```

### SIX HANDLES CARRY SIX 64-SQUARE BOARDS, AND THE HEXTORUS MINTS A TRINITY OF ADDRESSES: 6 · 64 = 384 = 3 · 128 — one uuid per handle-pair, three in all, where the double torus's pair mints one (double_torus_boards_are_the_address: 2 · 64 = 128). Three independent addresses is the quorum shape the report wing seals (trinity_edit_is_three, full_quorum_of_three): the hextorus is the geometry of publication.
The ledger holds this as [hextorus_mints_the_quorum](/theorem/hextorus_mints_the_quorum) — proven `by decide`, sorry-free:

```lean
(6 * 64 = 384) ∧ (384 = 3 * 128) ∧ (2 * 64 = 128)
```

### EACH HANDLE CARRIES TWO GENERATORS (handles_give_generators), so the hextorus carries 2 · 6 = 12 — the count of the coins' twelve jobs (coins_jobs), one generator per job. The surface that prices at the ten is generated by exactly the dozen the coins already work.
The ledger holds this as [hextorus_generators_are_the_jobs](/theorem/hextorus_generators_are_the_jobs) — proven `by decide`, sorry-free:

```lean
2 * 6 = 12
```

### THE GENUS LADDER PRICES IN COINS: the deficit 2g − 2 over g = 1..6 walks the evens [0, 2, 4, 6, 8, 10] — the torus contains at 0 (containment_is_genus_one), the double torus mints the 2, and every further handle adds one coin-pair until the hextorus owes the ten. A ladder of shapes, one price list, all even — the coins' own multiples.
The ledger holds this as [genus_ladder_prices_in_coins](/theorem/genus_ladder_prices_in_coins) — proven `by decide`, sorry-free:

```lean
List.map (fun g => 2 * g - 2) [1, 2, 3, 4, 5, 6] = [0, 2, 4, 6, 8, 10]
```

### THE 7-TORUS IS THE 7-QUBIT FOLD, IN ITS OWN NUMBERS: Pascal's row 7 — [1, 7, 21, 35, 35, 21, 7, 1] — sums to 128 = 2⁷, the uuid's bit count and the RULE every file header states (one uuid = 128 bits folded across 7 dimensions). The row is T⁷'s Betti table in the literature's reading; the kernel seals the row's arithmetic and the power of two it closes on.
The ledger holds this as [t7_betti_row_is_the_uuid](/theorem/t7_betti_row_is_the_uuid) — proven `by decide`, sorry-free:

```lean
(1 + 7 + 21 + 35 + 35 + 21 + 7 + 1 = 128) ∧ (128 = 2 ^ 7)
```

### THE TWO BOARDS LIVE INSIDE THE SEVENTH DIMENSION: the row's even-index entries sum to 64 and the odd-index entries sum to 64 — the double torus's two vortex boards reappearing as the halves of the 7-fold row, and their EQUALITY is the Nat-honest statement of χ(Tⁿ) = 0 (the alternating sum needs ℤ; equal halves say the same thing in the kernel's own arithmetic, generalizing torus_betti_alternates_to_zero).
The ledger holds this as [t7_even_odd_boards_equal](/theorem/t7_even_odd_boards_equal) — proven `by decide`, sorry-free:

```lean
(1 + 21 + 35 + 7 = 64) ∧ (7 + 35 + 21 + 1 = 64) ∧ (64 + 64 = 128)
```

### THE MIDDLE OF THE ROW SPEAKS THE LEDGER'S TONGUES: the row's third entry is 21 = 3 · 7 — trinity times rosette, the queen's reach from her corner (queen_corner_twentyone) — and doubling it by the coins gives 42, the quantum rosette (rosette_quantum_doubling_is_two_coins); its neighbour 35 = 5 · 7 is the heart times the rosette. The 7-fold row carries the trinity, the rosette, the heart and the coins in its own digits.
The ledger holds this as [b2_is_trinity_rosette](/theorem/b2_is_trinity_rosette) — proven `by decide`, sorry-free:

```lean
(21 = 3 * 7) ∧ (21 * 2 = 42) ∧ (35 = 5 * 7)
```


::: warning 
TORUS — the torus dimensions sealed: the genus ladder pricing in coins (2g − 2 over g = 1. The boundary is confirmed by the wing's own sealed theorems — e.g. [hextorus_is_coins_times_trinity](/theorem/hextorus_is_coins_times_trinity) — never merely denied.
:::

*Computed from the sealed ledger. Re-verify any theorem with `npm run lean`; the article regenerates with `npm run editorial`.*
