---
title: The reading room
description: Read, find the facts, try the quotes — the books shelf, computed in your browser.
---

# The reading room <Badge type="tip" text="nothing pasted leaves the page" />

> The books the first thread surfaced — music, dance, singing — asked for a room. This is it: paste any
> public-domain passage and it becomes a first-class ledger object **in your browser** — its recomputable
> identity, its facts decided by the ear that can refuse (truth and falsehood wear different verdicts —
> [`silence_never_refutes`](/theorem/silence_never_refutes) guards what stays open), and its quotes standing the
> citation trial, each leaving **addressed** whatever the verdict. An unverified quote with an address is a door
> for the [open questions](/open-questions).

<BookRoom />

Fourteen handle strips, ten choices — the book is the combination, the handle is the door
([`literature_sonnet_measure`](/theorem/literature_sonnet_measure) is the product 14 · 10;
[`literature_sonnet_volume`](/theorem/literature_sonnet_volume) is the power 10¹⁴;
[`ve_fourteen_faces`](/theorem/ve_fourteen_faces) is the line count;
[`ve_faces_are_handle_hexbit_coins`](/theorem/ve_faces_are_handle_hexbit_coins) is that fourteen as tiles + width + coins;
[`station_ten_is_hexagram_plus_hexbit`](/theorem/station_ten_is_hexagram_plus_hexbit) is the ten stations;
[`universe_of_handles`](/theorem/universe_of_handles) is 16⁸ doors;
[`combinatorial_book_exceeds_handles`](/theorem/combinatorial_book_exceeds_handles) is the pigeonhole). Occupancy, not verse.

## Honest scope

Identity, arithmetic and citations — never meaning, never attribution, never taste. The ear hears sums in digits
and in words; what it cannot hear excludes nothing. After the literature-yield metric was refuted (it counted
only the arithmetic a book **asserts**), **Read** also counts what the pasted text **is**: words, distinct ranks,
letters, numerals. Facts that held on every book of the declared corpus — `rank1 exceeds rank2`, commonest word
`'the'` — are tagged vacuous-on-corpus: they hold, they identify nothing. The yacht door that *did* discriminate
is unit-equivalence on the 32-point rose ([`four_points_is_45`](/theorem/four_points_is_45)). The pure logic lives in
[`src/quantum/apps/categories/books/`](https://github.com/uuidna/uuidna/tree/main/src/quantum/apps/categories/books)
and is held by the same tests in Node that your browser just reran.
