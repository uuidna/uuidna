---
title: Changelog
aside: false
---

<!-- The site changelog is the root CHANGELOG.md, included verbatim — one source, so the page and the file never
     diverge. `next` audits it: the release gate fails unless CHANGELOG.md documents the current version and carries
     the current ledger receipt. Auditing self using the changelog. -->

<!--@include: ../CHANGELOG.md-->

---

Each release names the ledger receipt its proofs fold to — recompute it with `npm run lean`, or read the live fold on
[Trials](/trials). The version is a single-digit odometer; npm has only `0.1.0`, and `0.1.1` is the next release,
pending. [Source · CHANGELOG.md](https://github.com/uuidna/uuidna/blob/main/CHANGELOG.md).
